apiVersion: apps/v1
kind: Deployment
metadata: {name: entitlement-worker, namespace: spyglass-reference}
spec:
  replicas: 1
  selector:
    matchLabels: {app.kubernetes.io/name: entitlement-worker}
  template:
    metadata:
      labels: {app.kubernetes.io/name: entitlement-worker, app.kubernetes.io/part-of: spyglass}
    spec:
      serviceAccountName: entitlement-worker
      terminationGracePeriodSeconds: 60
      containers:
        - name: entitlement-worker
          image: registry.invalid/infinite-ocean/spyglass-engine:release-placeholder
          args: [entitlement-worker]
          ports: [{name: health, containerPort: 8081}]
          env:
            - {name: SPYGLASS_HEALTH_ADDRESS, value: ":8081"}
          envFrom:
            - configMapRef: {name: spyglass-global-runtime}
            - secretRef: {name: spyglass-entitlement-worker-secrets}
          resources:
            requests: {cpu: 100m, memory: 128Mi}
            limits: {cpu: "1", memory: 512Mi}
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            runAsNonRoot: true
            capabilities: {drop: [ALL]}
            seccompProfile: {type: RuntimeDefault}
          readinessProbe: {httpGet: {path: /health/ready, port: health}, periodSeconds: 5}
          livenessProbe: {httpGet: {path: /health/live, port: health}, periodSeconds: 15}
