# Phase 3 — Vue customer-surface and SPA plan

- Status: Approved; active construction plan after the local backend boundary
- Decision date: 2026-08-24
- Application: Infinite Ocean: Spyglass
- Predecessor: [Phase 3 local backend completion plan](phase-3-local-backend-completion-plan.md)
- Contract guide: [Spyglass API and MCP interaction guide](api-mcp-interaction-guide.md)
- Privacy/commercial extension: [Privacy, analytics and affiliate architecture](privacy-analytics-affiliates.md)
- Deployment work: Deferred until this plan's local product-surface exit gate passes

## 1. Objective

Replace the desktop-first private browser shell and the provisional acquisition experience with one coherent customer-facing Vue 3 and TypeScript product surface. The authenticated application operates as a single-page application; the public landing, feature and pricing routes are rendered or pre-rendered for discovery, performance and resilient first load. The complete experience is mobile-first, uses the generated HTTP contracts without inventing domain behavior, makes **Your Turn** the primary signed-in workflow, measures landing/checkout/onboarding through a consented first-party analytics boundary, and remains available to EU customers through GDPR-capable operation.

The product surface must feel deliberately designed at phone widths. Narrow layouts are not reduced desktop grids: navigation, information density, action placement and detail disclosure must change to match the user's immediate task.

## 2. Fixed decisions

- The private Spyglass application is a Vue 3, TypeScript SPA.
- The new UI program includes the public Infinite Ocean landing page, complete Spyglass feature/package breakdown, pricing, signup handoff, checkout initiation and checkout-return experience.
- GDPR-capable consent, analytics, data-subject rights and lifecycle evidence are required in this phase; EU support is not deferred behind a US-only launch.
- Launch analytics prioritize landing-page acquisition, checkout conversion and onboarding completion, with optional Your Turn usability events that never contain task content.
- The phase includes the minimal backend extension required for consent receipts, first-party analytics enforcement, Affiliate enrollment/codes, immutable referral attribution and recurring commission accounting.
- Affiliate attribution is commercial transaction state and remains valid when optional analytics consent is refused or withdrawn.
- Public acquisition and the private application remain separate security, caching and deployment surfaces. They share a Vue design system and customer journey, not private session data or cache policy.
- Public acquisition routes must be server-rendered or pre-rendered where practical; essential product, feature and pricing content cannot depend on client-side JavaScript to become discoverable.
- The SPA consumes `api/spyglass.openapi.json` through the generated TypeScript contract and types.
- Browser API traffic remains same-origin under the application origin wherever practical. Deployment may route `/api/` to the Go application without exposing session credentials cross-origin.
- The existing server-rendered browser shell is a functional and rollback reference, not the visual foundation of the new application.
- Mobile behavior is designed first and enhanced at wider breakpoints.
- **Your Turn is the default destination after authentication and Account selection whenever the user has Account access.**
- No GHCR publication, Hostinger Stage mutation, LKE deployment or production release is part of this construction plan.

## 3. Experience rules

### 3.1 Mobile-first layout

- Begin with a usable 320px-wide, single-column layout and enhance through content-driven `min-width` breakpoints.
- No core route may require horizontal page scrolling at 320px CSS width.
- Body text defaults to a legible mobile scale; metadata may be quieter but cannot become unreadably small.
- Interactive targets are at least 44 by 44 CSS pixels unless the target is inline text with an equivalent larger action.
- Primary actions remain reachable without precision pointing and without competing clusters of equally prominent buttons.
- Tables transform into task-oriented lists, summaries or drill-down routes on narrow screens; they are not merely squeezed.
- Filters and secondary fields move into dismissible sheets or disclosure regions when they would crowd the primary task.
- Content uses progressive disclosure: summary first, decision context second, full evidence and history on demand.

### 3.2 Navigation

- Mobile uses one compact sticky application header and one clearly labeled menu control.
- The menu opens a focus-contained navigation drawer with direct, plainly named destinations; it does not use nested fly-out menus.
- Your Turn is the first product destination and shows an actionable count when one is available.
- Account selection is accessible from the header and menu without occupying permanent page width.
- Package-disabled destinations communicate why they are unavailable without filling the menu with upgrade noise.
- Desktop may enhance the same information architecture into a collapsible rail. Mobile and desktop do not maintain different route taxonomies.
- Back behavior, deep links and browser history remain predictable; drawers and dialogs do not replace routes for durable detail views.

### 3.3 Page clarity

- Each route has one clear purpose, one primary heading and at most one visually dominant action.
- Dashboards do not place every available metric and command above the fold.
- Empty, loading, stale, offline, unauthorized, read-only and error states receive intentional layouts rather than generic placeholders.
- Secondary metadata and audit history cannot displace the current decision or task.
- Destructive, consequential and externally visible actions receive explicit confirmation proportional to their risk.
- Customer values render as text and no session, customer content or governed draft is placed in persistent browser storage.

### 3.4 Public acquisition, features and checkout

The public experience must be a first-class product surface, not a decorative splash page attached to the application.

#### Landing page

- Communicate what Spyglass does, who it helps and the primary business outcome within the first mobile viewport.
- Make Your Turn the central product story: Spyglass gathers operational work and brings people only the questions, reviews and approvals that require them.
- Show a credible visual product preview using controlled demonstration content; never expose a live customer Account or imply unavailable behavior.
- Explain the operating loop from captured context through governed action and human attention.
- Provide clear paths to explore features, compare plans, create a free Account, select a paid offer and sign in.
- Establish trust through concrete security, governance, privacy and reliability claims that are traceable to implemented capabilities. Do not invent testimonials, customer logos or performance claims.
- Keep the page visually distinctive without excessive motion, autoplay media, crowded card walls or repeated competing calls to action.

#### Full feature breakdown

- Publish a complete browsable feature index covering Your Turn, Work, Knowledge, Baseline, Agents, schedules, Finance, Marketing, Integrations, Account administration, security, export and lifecycle behavior.
- Organize features first by customer outcome and workflow, then expose package and technical detail. Do not present an undifferentiated inventory of backend endpoints.
- Give each launch package a durable landing route with its purpose, primary workflows, included capabilities, governance boundaries, limitations and applicable plan availability.
- Show cross-package workflows so users can understand how Work, Agents, Knowledge and Your Turn operate together.
- Source package names, availability and pricing claims from the published Catalog or reviewed versioned content; unpublished capabilities remain absent.
- Link feature descriptions to pricing without forcing users to restart their discovery context.

#### Pricing and checkout

- Pricing uses the public Catalog contract, displays free and paid offers consistently, and fails closed to the last-known published Catalog rather than leaking provider identifiers.
- Plan comparison explains included packages, meaningful limits, trial/grace behavior and what changes on downgrade in plain language.
- Selecting an offer preserves only an opaque, validated offer intent through registration and verification.
- Account creation remains free. Only an authenticated authorized Account owner can initiate the separately confirmed checkout mutation.
- The server revalidates the selected offer and creates or reuses the Stripe Customer and hosted Checkout Session; the browser never submits a Stripe Price ID.
- The customer is redirected to Stripe-hosted Checkout for payment details. Spyglass owns the coherent pre-checkout review, cancellation return, success return and pending/failed projection states.
- The return page never grants access from the redirect alone. It waits for signed webhook projection, explains pending state and refreshes effective packages only from the local entitlement snapshot.
- Checkout retry uses exact idempotency semantics and cannot create duplicate purchase intent from repeated taps, back navigation or network recovery.
- Billing management and later plan changes use the same understandable package language and hand off to the short-lived Stripe Customer Portal where required.

### 3.5 Privacy, analytics and Affiliates

- Use the consent behavior, lawful-purpose registry, event boundaries, GDPR lifecycle and Affiliate architecture in [Privacy, analytics and affiliate architecture](privacy-analytics-affiliates.md).
- Landing, checkout and onboarding instrumentation is complete only when consent-granted, consent-denied and consent-withdrawn journeys produce identical customer outcomes.
- Affiliate code entry appears in the authenticated checkout review and is confirmed before Stripe redirection.
- The Affiliate dashboard exposes code, terms and aggregate earning states without identifying referred customers.
- Recurring commission is earned only from the durable local projection of a qualifying paid subscription invoice; browser analytics and redirect state are never financial authority.

## 4. Your Turn — primary product workflow

Your Turn is the first complete feature slice and the acceptance reference for the rest of the SPA. It unifies information requests, assigned Work reviews, consequential approvals and uncertain-action recovery without erasing their different authority and safety rules.

### 4.1 Mobile queue

- Default to an actionable queue, not an overview dashboard.
- Each item states what is needed, why it needs the current user, its Account context, urgency and relevant age.
- Use short cards or rows with stable status language. Avoid dense multi-column tables.
- Keep common sorting and the active filter visible; place the full filter set in a sheet.
- Preserve scroll position and queue state when returning from an item.
- Provide useful empty states: nothing waiting, filtered result empty, temporarily unavailable and Account/package access unavailable.

### 4.2 Task detail and completion

- Narrow screens use a dedicated detail route rather than a permanent split pane.
- Lead with the requested decision or information, then show the minimum context required to act safely.
- Put evidence, policy, history and technical metadata behind clear disclosure sections without concealing required decision inputs.
- Keep the primary answer, review or approval control reachable near the bottom of the viewport when the task is actionable.
- Explain read-only, expired, superseded, conflicted and dual-control states in plain language.
- Preserve tab-scoped drafts across recoverable navigation and network failures without persisting customer material across browser restarts.
- On version conflict, reload the authoritative state and require a fresh human decision; never silently replay stale approval intent.
- After successful completion, announce the result, return to the queue and make the next item easy to open.

### 4.3 Your Turn acceptance

- A phone user can find, understand and complete every eligible Your Turn item with one hand and without horizontal scrolling.
- Information, review, approval and recovery flows preserve their generated API headers, authority checks, confirmation and recovery semantics.
- Keyboard, screen-reader and touch journeys reach equivalent outcomes.
- Queue refresh, optimistic concurrency, duplicate submission, offline recovery and session expiry have automated browser coverage.
- Your Turn passes the supported-device and accessibility matrix before later feature slices copy its interaction patterns.

## 5. UI architecture boundary

- Create a new top-level Vue UI workspace, separate from the legacy `website/`, `prototype/ui/` and Go browser-shell assets.
- Give the public acquisition site and private Spyglass SPA independent build targets with a shared design-system package, shared reviewed product terminology and generated API client boundary.
- Organize private code by product feature and route, with shared application-shell and design-system layers. Organize public code by acquisition journey and durable content route rather than mirroring private implementation folders.
- Use the generated TypeScript inventory and types as the only transport DTO source.
- Centralize session handling, selected-Account state, package/role capability projection, problem normalization, ETag handling, idempotency keys, opaque pagination and event-stream recovery.
- Server state stays in the query/cache layer; local component state is reserved for presentation and unsubmitted interaction state.
- Durable public and private detail pages use routes. Temporary confirmation, filtering and compact editing may use dialogs or sheets.
- The Go application remains the authority for identity, Account access, packages, roles, versions, commands and consequences. The SPA does not recreate authorization or domain transitions.

## 6. Construction order

### PA0 — Privacy, analytics and Affiliate backend extension

Affiliate privacy checkpoint (2026-08-26): the PA0 boundary now includes passkey-confirmed identity-owned Affiliate portability. A dedicated execute-only PostgreSQL projection and typed `GET /api/v1/affiliate/data-export` contract return enrollment, public-code history, sanitized lifecycle, aggregate attribution counts, provider-free commission entries, and sanitized support history. Referred-customer, checkout, subscription, payment-provider, staff-actor, and free-form reason data cannot enter the artifact. The mobile-first Privacy tools surface downloads it as no-store JSON without persistent browser storage; correction, restriction, objection, closure, and erasure remain reviewed rights workflows.

Checkpoint (2026-08-25): the consent, event-ingestion, browser-subject rights, raw-event retention, enrollment, attribution, recurring-invoice, reversal-ledger and statement kernels plus generated contracts are implemented and pass disposable PostgreSQL 17. Authenticated private consent subjects bind to the current User without allowing a shared-browser reference to cross-link two identities. The passkey-verified privacy-rights boundary accepts access, correction, erasure, restriction, objection and portability requests across identity, Account, Affiliate and analytics scopes, tracks a calendar-month deadline, rejects duplicate open pairs and preserves immutable customer events. The Vue Privacy route exposes that history beside immediate browser erasure and the existing Account export, lifecycle and identity-correction tools. A separate operator-authorized boundary records inspection, exact-version review and terminal response evidence through execute-only PostgreSQL functions; opaque evidence identity/digest and staff audit fields never enter the customer API. Verified Stripe events now produce PaymentIntent-bound exact-once earnings and immutable full-qualifying-amount Refund/lost-dispute reversals, including semantic Refund deduplication and adverse-before-earning reconciliation. A separate execute-only Affiliate operator boundary records inspections, exact-version suspension/reactivation and terminal closure while preserving locked attribution and statements. Structured customer appeals/commission reviews and their distinct execute-only staff resolution boundary are implemented without free text, uploads or automatic commercial mutation. The event/processing registry and operator runbook are revision controlled. Remaining PA0 release gates are commercial settlement approval and final legal/vendor/transfer approval. Feature flags remain closed while these decisions are unresolved.

- Approve the lawful-purpose/event registry, consent-policy version, retention schedule and analytics provider boundary.
- Implement consent receipts and server-side optional-event enforcement.
- Implement Affiliate enrollment, generated codes, checkout attribution, versioned commission rules and an immutable recurring commission ledger.
- Generate the HTTP contracts required by the Vue consent center, checkout review and Affiliate dashboard.
- Approve account-credit versus cash settlement before either is presented as available value.

Exit: privacy and Affiliate state has a tested local application boundary; analytics withdrawal cannot alter commercial attribution, and invoice replay cannot duplicate commission.

### UI0 — Foundation and contract proof

Checkpoint (2026-08-24): the top-level `ui/` workspace now builds separate Nuxt-rendered public and Vue-SPA private images, shares generated API contracts and design tokens/primitives, runs type-check/test/lint/build/audit gates in its Docker test target, and is wired into the local web/app origins. The first-party consent client refuses optional emission before consent, the rendered acquisition shell includes resilient privacy controls, and the private shell loads the current User, selected Account and Account choices with Your Turn as its default route. The Attention client proves assigned authenticated queries plus stable-idempotency/current-ETag mutations and conflict recovery without copied DTOs. Complete primitive/phone fixture coverage and applied device/browser acceptance remain UI0 exit work.

- Scaffold the Vue 3 and TypeScript workspace with independent public acquisition and private SPA build targets plus shared routing, test, lint, type-check and production-build gates.
- Consume the generated API types and prove one authenticated query and one idempotent, versioned mutation locally.
- Establish design tokens and accessible primitives for buttons, fields, links, status, alerts, dialogs, drawers, disclosure and live announcements.
- Establish the consent center, optional-event client boundary and generated Affiliate client types without loading an analytics SDK before consent.
- Add phone-width visual fixtures before building feature pages.

Exit: both UI targets build reproducibly, run through their local Docker origins and exercise the appropriate generated public/private boundaries without copied DTOs.

### UI1 — Session, Account and simple navigation shell

Checkpoint (2026-08-25): signup, verification/login and security setup expose the same equal-choice first-party privacy control on the application origin while retaining a host-only signed preference reference. Successful registration, verification, Account creation and completed passkey-plus-recovery-code enrollment emit reviewed content-free events only after their authoritative server transitions; first Vue application entry is likewise consent-gated and tab-deduplicated. A public-site choice is not silently widened across subdomains, and declining or failing the optional measurement path leaves every onboarding transition unchanged. The complete shell destination inventory is grouped into flat Workspace and Account regions; package projection keeps enabled/read-only destinations direct and replaces unavailable package rows with one Explore plans path. Applied signed-in phone/browser certification and any desired router-level package-guard consolidation remain open; every view still enforces its generated package/role boundary and the server remains authoritative.

- Implement authentication/session-expiry states, Account selection and package-aware route guards.
- Implement the sticky mobile header, single navigation drawer and desktop enhancement.
- Make Your Turn the authenticated default route.
- Establish loading, offline, error, unauthorized, locked and read-only application states.
- Instrument registration, verification, Account creation, security enrollment and first application entry through the reviewed onboarding event registry.

Exit: users can authenticate, select an Account and navigate the complete empty shell at supported phone, tablet and desktop widths.

### UI2 — Your Turn vertical slice

Checkpoint (2026-08-24): the hard-coded queue fixture has been replaced by entitlement- and role-aware live information, assigned-review, approval and recovery queues using generated types. Durable detail routes inspect exact version/digest/payload context; answer, review, approval, resolution-request and independent-confirmation forms use current versions, stable retry keys, tab-local drafts, conflict reload and consented content-free completion dimensions. Exact Knowledge facts are selectable for information completion, opaque cursors are drained through a bounded client, and stale queue data remains visible across offline/refetch failure before reconnect refresh. Unfinished legacy `/app/*` routes remain served by the Go shell instead of being prematurely swallowed by the SPA. Component tests prove live Account-scoped rendering, durable detail routing, offline stale-data behavior, exact-fact completion, Work change requests, explicit payload confirmation, evidence-based recovery requests and different-operator confirmation. Broader browser recovery coverage, applied accessibility/device acceptance and locally seeded HTTP end-to-end completion remain open.

- Build the queue, filters, detail routes and completion flows for information requests, Work reviews, consequential approvals and action recovery.
- Implement tab drafts, current-ETag mutation handling, fresh idempotency keys, conflict recovery, dual control, live announcements and next-item flow.
- Complete phone, keyboard, screen-reader, reduced-motion, zoom/reflow and reconnect acceptance.

Exit: Your Turn is locally feature-complete and becomes the interaction-quality reference for all later modules.

### UI3 — Public acquisition, feature catalog and checkout

Checkpoint (2026-08-24): the rendered public surface now tells the Your Turn-centered operating story, exposes a twelve-route feature and governance map, explains a cross-package workflow, and renders paid offer names, packages and current prices from the server-fetched public Catalog. Landing, feature, pricing, offer-selection and signup-handoff events remain consent-gated and content-free. Registration, verification, Account creation, security enrollment and first application entry now emit only reviewed content-free events after authoritative transitions and only when the applicable first-party consent permits them. The private SPA provides an owner/billing-administrator checkout review with an actively applied optional Affiliate code, explicit offer/referral confirmation, stable request idempotency and a dedicated Stripe return route that waits for the local signed-webhook subscription projection. Its ordinary-identity Affiliate dashboard exposes only the generated code and aggregate commission states. A referral proposed by a link is never silently applied, and analytics refusal does not change referral or checkout behavior. Free/paid journey HTTP certification, Stripe failure fixtures and approved commercial settlement/reversal policy remain open. Structured discovery, social-preview and Catalog last-known-good construction are closed by the UI7 checkpoints below.

- Build the public landing page around the governed Spyglass operating loop and Your Turn value proposition.
- Build the complete feature/package index, durable package pages, cross-package workflows and Catalog-backed plan comparison.
- Complete free signup handoff, selected-offer continuation, authenticated pre-checkout review, Stripe Checkout initiation and return/pending/failure states.
- Add Affiliate code entry and actively confirmed link intent, explicit attribution review and disclosure-safe referral behavior without coupling commercial credit to analytics consent.
- Add structured metadata, crawlable content, social previews, performance budgets, link checks and consented acquisition analytics that collect no customer business content.
- Certify the anonymous-to-free and anonymous-to-paid customer journeys at supported phone widths before later package pages are copied from these patterns.

Exit: a visitor can understand the complete product, select the correct offer, create an Account and safely reach a projected checkout outcome without encountering a visual or terminology break between the public and private surfaces.

### UI4 — Work and supporting context

Checkpoint (2026-08-24): the private Vue Work route now provides an Account-scoped paged queue, summary, durable item detail, create, assignment and lifecycle transitions with current-version conflict recovery and tab-only drafts. The governed Knowledge route separates proposed claims from accepted facts, exposes durable claim detail with the exact canonical value, digest and citations, and submits human, versioned, retry-safe decisions. Both clients consume generated contracts, and component tests cover their principal list/detail and mutation paths. Baseline context, explicit preservation of the originating Your Turn queue position, locally seeded signed-in HTTP completion and applied browser/device accessibility evidence remain UI4 exit work.

- Build Work queues, Work detail and lifecycle commands using the established mobile list/detail patterns.
- Add Knowledge and Baseline context required to understand and resolve work without overloading Your Turn.
- Preserve links back to the originating Your Turn item and queue position.

Exit: the core task loop from Your Turn through Work and governed knowledge is complete locally.

### UI5 — Agents and conversations

Checkpoint (2026-08-25): the private Vue Agents route now creates and lists Boardrooms, publishes new Personas and immutable successor versions, inspects exact policy/digest/manager configuration, presents durable conversation transcripts, and submits, polls and recovers Runs. The mobile-first Persona editor owns the complete server allowlists for read/additive tool grants and consequential proposal capabilities, supplies exact closed input schemas, exposes bounded model/token/cost/tool-step settings and makes clear that proposals still require human approval in Your Turn. Boardroom/Persona configuration is Owner/Administrator-only while members may run published specialists; package read-only mode retains inspection only. Boardroom and Conversation detail use durable routes, polling is bounded and canceled on unmount, and hidden proposal payloads never render in chat. Client/component tests cover retained publish identity, immutable latest-version binding, role separation, transcript loading and terminal Run recovery. Applied streaming/failure fixtures and supported-device/browser/assistive-technology acceptance remain UI5 exit evidence.

- Build mobile conversation, Persona, Boardroom and Run surfaces with bounded streaming and recovery.
- Keep consequential proposals routed through Your Turn rather than embedding unsafe shortcut approvals in chat.

Exit: users can inspect and operate Agent workflows on supported devices without bypassing governance.

### UI6 — Remaining product packages and Account management

Checkpoint (2026-08-26): the private Vue Affiliate route uses the ordinary authenticated identity, exposes the generated public code and identity-safe aggregate commission ledger, and renders pending, settled and reversed entries without referred-customer data. Enrollment is strongly authenticated and feature-flagged; Account-credit mode permits only an Account the User owns, while `unconfigured` settlement blocks enrollment and makes no cash/credit promise. A crawlable public launch-terms route accurately labels candidate economics and policy as inoperative while the flags remain closed. Verified Refund/lost-dispute projection and operator-controlled suspension/reactivation/terminal closure are complete locally with immutable evidence and fresh PostgreSQL coverage. The same mobile-first route now submits, lists and cancels structured enrollment appeals and entry-specific commission reviews without accepting free text or attachments; a distinct least-authority operator command reviews and resolves them. An audited execute-only risk inspection gives staff only content-free reservation/code-replacement aggregates and deterministic manual-review flags, with no referred-customer identifiers or automatic enforcement authority. A distributed, Account-scoped content-free budget now stops repeated code validation before lookup or provider access. Commercial settlement and legal approval remain backend release blockers.

Scheduling checkpoint (2026-08-24): `/app/schedules` and its durable detail route now use the generated Scheduling boundary for opaque pagination, create/revise, pause/resume, immediate trigger and deletion. The mobile-first editor makes IANA timezone, local recurrence, daylight-saving gap/overlap policy, missed-run policy and exact Boardroom/Persona context explicit; optional Account context is progressively disclosed and unsubmitted creation content remains tab-scoped. Commands bind the current durable version, retain one idempotency key across exact failed retries, require an operational reason, reload on conflicts and keep deletion behind explicit confirmation. Component/API tests cover durable navigation, recurrence presentation, conflict recovery, cursor opacity and retry identity. Schedules are therefore no longer a legacy-only UI6 surface; applied browser/device/accessibility evidence remains open.

Account-team checkpoint (2026-08-24): `/app/account` now provides a mobile-first selected-Account summary, strongly authenticated invitation creation, role-aware team roster, owner-only role and ownership controls, owner/administrator suspend/reactivate/remove controls, and ordinary-member self-service leaving. A new typed current-Membership query exposes the caller's exact durable Membership version without granting roster authority, so self-service never substitutes the unrelated Account version. Every change requires an operational reason; destructive transfer, removal and leaving require typed confirmation; version conflicts close the dialog and reload authoritative state; passkey/owner-enrollment failures lead explicitly to Security. Generated-contract, transport, API-client and component tests cover the read boundary, version binding, dual-version transfer, authority rendering, conflict recovery and strong-auth continuation. Account-team administration is therefore no longer a legacy-only UI6 surface; applied browser/device/accessibility evidence remains open.

Security API-parity checkpoint (2026-08-24): the typed Account API now exposes the authenticated identity's current verified contact plus token-free connected-MCP-client summaries and same-origin revocation. Production composition reuses the existing MCP authorization/revocation service, so the Vue migration does not need to remove verified-email or connected-client controls that exist in the legacy security page. Passkey ceremonies, recovery-code posture/rotation/consumption, password and passkey reauthentication, active sessions and security history were already typed. The legacy `/app/security` route remains authoritative until the Vue surface and its WebAuthn/session/recovery tests close the complete interaction boundary.

Security UI checkpoint (2026-08-24): `/app/security` now uses those complete typed boundaries in the Vue shell. The mobile-first identity surface covers password and passkey confirmation, WebAuthn passkey enrollment, rename/removal/compromise reporting, one-time recovery-code posture/rotation/consumption, current verified-contact change initiation, active-session and sign-out-everywhere controls, token-free connected-MCP-client revocation and recent security history. Recovery codes remain visible only in the immediate rotation response, all identity-wide destructive actions require a typed phrase, and current-session/compromise revocation returns to login. Component and API-client tests cover full-boundary rendering, one-time recovery presentation, destructive confirmation, verified-contact request shape and encoded identifiers. Local Docker builds and the TLS route smoke gate pass. Applied passkey hardware/browser ceremonies, dialog focus containment and phone/device accessibility evidence remain open acceptance work.

Portability/lifecycle checkpoint (2026-08-24): `/app/account-exports` and `/app/account-closures` now provide the complete customer-facing portability and recoverable-closure controls in the Vue shell. Export creation is owner-only and strongly authenticated; queued cancellation binds the current export version; available artifacts are retrieved through a newly minted short-lived header capability with cookies omitted and no token in a URL or persistent client state. Account closure binds the selected Account version and an explicit reason, freezes access for the seven-day cooling-off period, remains discoverable through an identity-global recovery list, and accurately distinguishes logical closure from governed erasure. Restore uses the closure's current Account version, while conflicts reload authoritative state. Component/API-client tests, clean production builds, the dependency audit and local TLS route smoke cover these paths. Applied browser/device/accessibility evidence and authorized staff rights-fulfillment operations remain open release gates.

Billing checkpoint (2026-08-24): `/app/billing` now shows the Account-global local subscription projection, distinguishes payment-provider customer state from managed subscriptions, resolves current offer names from the public Catalog, exposes period/cancellation/synchronization evidence and keeps billing controls limited to owners and billing administrators. Hosted Stripe management requires recent passkey confirmation, uses one opaque idempotency key across exact retries and accepts only an HTTPS destination. The provider return URL now targets the durable Vue Billing route, which accurately warns that signed-event projection may lag the hosted return. Checkout remains the only path for a new paid subscription. Component/API-client/domain tests, clean production builds and local TLS route smoke cover the route. Applied Stripe-portal/browser/device/accessibility evidence remains open.

Finance checkpoint (2026-08-24): `/app/finance` and durable Ledger, chart-account, journal-entry and reconciliation detail routes now use the complete generated cell boundary. The mobile-first workspace honors package suspension/read-only mode plus member-versus-manager role authority; provides opaque pagination; exposes posted income, expense, net and draft summaries; and covers Ledger creation/revision/period close/archive, chart-account creation/revision/archive, balanced multi-line draft creation/revision, posting, separately numbered reversal, evidence-bound reconciliation and zero-difference confirmation. Every mutation binds the exact durable version where required and keeps one idempotency key across exact failed retries; conflicts reload authoritative state. Typed phrases guard posting, reversal, close, archive and confirmation. Component/API-client tests and the local routing/build gates cover the principal durable and read-only states. Applied realistic ledger fixtures, browser/device/accessibility evidence and accounting-domain product acceptance remain open.

Integrations checkpoint (2026-08-24): `/app/integrations` plus durable connection, execution and authorization routes now cover the generated provider-neutral boundary. The mobile-first Connections, Executions and Research workspaces expose immutable non-secret scope, opaque credential generations, paged health/attempt history, passkey-gated Google consent and revocation, broker-attestation lifecycle, channel-compatible delivery preparation, different-manager outcome recovery and explicit Knowledge capture from scoped web results. Package read-only mode retains inspection and bounded search while omitting mutations; management remains Owner/Administrator-only and research capture additionally requires writable Knowledge. Versioned commands bind current ETags, exact retries retain one operation identity and conflicts reload durable state. Component/API-client tests and local routing/build gates cover durable, read-only, dual-control and capture states. Applied provider/OAuth fixtures, browser/device/accessibility evidence and product-domain acceptance remain open.

Baseline checkpoint (2026-08-24): `/app/baseline` and its durable assessment route now provide the complete generated assessment, plan, Work-maintenance and source-grant lifecycle in the private Vue shell. A new read-only current-assessment query exposes the database-enforced single non-archived assessment, so onboarding resumes across tabs, devices and sessions without browser identifiers. The mobile-first flow presents one question or requirement at a time, turns a new customer answer into attributable owner-statement evidence plus an explicitly accepted Knowledge Fact before binding it, preserves “unknown” rather than guessing, derives the frozen evidence inventory, records evidence/gap/not-applicable decisions, and binds Owner approval to the exact plan version and digest. Active assessment views detect materialized Baseline Work by immutable provenance, confirm only completed Work with reviewed evidence, constrain source grants to active Email/Drive connections, and expose readiness, maintenance and reassessment. Package/role gating, ETags, exact retry identities, component/client tests and the pinned local Docker UI gate cover the principal lifecycle; realistic seeded completion plus applied browser/device/accessibility acceptance remain open.

Marketing checkpoint (2026-08-24): `/app/marketing` plus durable campaign and release routes now replace the final legacy-only UI6 package surface. The mobile-first workspace covers campaign filtering and intent, immutable creative file admission, opaque pagination, exact campaign-version/channel/creative release snapshots, member-versus-manager authority and the draft/submit/cancel/activate/pause/complete/archive lifecycle. Submitted releases route the customer to Your Turn and never accept a pasted internal Approval identity; activation is explicitly separated from external delivery in Integrations. Package read-only mode retains inspection while removing mutations. Existing-resource commands bind current weak ETags, exact retries retain one operation identity, multipart upload omits internal object metadata and conflicts reload authoritative state. Client/component tests cover durable release navigation, read-only and role boundaries, multipart admission and exact snapshot creation. Applied realistic creative/delivery fixtures plus browser/device/accessibility evidence remain open UI7 work.

- Retain the completed Marketing, Baseline, Affiliate, Scheduling, Account-team, Billing, Finance, Integrations, Security, export and Account-lifecycle routes as UI6 reference implementations during consolidation.
- Apply the same route clarity, menu behavior, list/detail separation and responsive acceptance established by Your Turn.

Exit checkpoint: every UI6 package surface now has a Vue route using the generated boundary and no launch package depends on a prototype page. UI5 and UI6 customer-surface construction are complete. Applied cross-feature, streaming/failure, browser, supported-device and accessibility certification remains open before UI7 can exit.

### UI7 — Consolidation and local release boundary

- Complete cross-feature consistency, performance budgets, accessibility, device/browser and failure-recovery certification.
- Remove or disable private legacy routes only after Vue parity, automated coverage and a documented rollback point exist.
- Produce the final local product-surface completion report and update deployment inputs without publishing or deploying them.

UI7 checkpoint (2026-08-25): the first rendered consolidation pass is complete for the public acquisition boundary. The landing, feature index, pricing, privacy and Affiliate-terms routes passed 30 local browser checks at 320, 360, 390, 412, 768 and 1280 usable CSS-pixel widths: every route retained one main heading, no horizontal page overflow and no rendered button below 44 CSS pixels. The 320px landing pass separately confirmed its product statement, full-width primary actions, Your Turn preview, optional-consent fail-closed behavior and compact menu. The private shell now removes the closed drawer from keyboard focus, places focus inside the open drawer, contains forward/reverse Tab traversal, restores the menu control on Escape/close and focuses the main region after SPA route changes; component coverage locks that behavior. The local Caddy overlay exposes a loopback-only HTTP browser-QA listener because the in-app harness cannot trust Caddy's development CA, while the normal `8444` certificate-verified smoke gate remains authoritative and passes.

An enforced post-build performance gate now caps the complete private SPA at 140 KiB gzip JavaScript and 20 KiB CSS, and the complete public client asset set at 120 KiB JavaScript and 15 KiB CSS. Current results are respectively 107.5/10.0 KiB and 86.9/4.3 KiB. This gate runs after both production builds in `npm run verify`.

UI7 hardening checkpoint (2026-08-25): modal focus is now managed consistently across the private SPA: newly mounted modal dialogs receive focus, contain forward and reverse Tab traversal, and return focus to the invoking control when removed. Your Turn recovery coverage now proves initial-load retry, stale-data behavior through offline/reconnect and authoritative version-conflict reload. A conflict also clears the exact-payload approval checkbox, so an obsolete confirmation cannot survive the server-state change. The shared API client reports any expired-session `401` through one deduplicated application handler and preserves the current route as the post-login return destination. Privacy UI coverage proves equal rejection of both optional purposes and a two-step browser-subject erasure. Public content tests lock the complete twelve-route feature inventory, and the certificate-verified local smoke gate now inspects server-rendered headings, descriptions and workflow/governance copy on every durable feature route without client execution.

UI7 contrast/reflow checkpoint (2026-08-25): the shared public/private design tokens now map the application palette, focus indicator, borders, disabled controls, logo mark and shadows to system colors under `forced-colors: active`; reduced-motion behavior remains global. The rebuilt public landing and unauthenticated signup surfaces were inspected at a 320 CSS-pixel viewport, the WCAG reflow equivalent of 400% zoom on a 1280 CSS-pixel viewport: each retained one main heading, a 320-pixel document width with no page overflow and no rendered button below 44 pixels. The rendered public build also exposed the expected forced-color system palette and disabled-control boundary. This is evidence for the shared and unauthenticated surfaces; authenticated private-route reflow still requires the authorized local sign-in pass.

UI7 acquisition/privacy hardening checkpoint (2026-08-25): optional consent lookup failure can no longer prevent Catalog or Account billing review, and the public acquisition pages now share one effective consent projection so CTA measurement stops immediately after a successful withdrawal and older concurrent reads cannot restore it. Policy-version renewal reopens the equal-choice consent panel. Failed consent changes retain and accurately describe the previous server choice in both the Nuxt acquisition surface and server-rendered onboarding flow; the onboarding asset URL is cache-busted and rendered locally. Checkout no longer substitutes the first paid plan when a signup offer disappears: the customer must select and reconfirm a current published offer. Exact failed retries retain one checkout request identity, non-HTTPS hosted destinations are rejected, and signed return projections distinguish pending/incomplete, active/trialing, past-due/paused and expired/unpaid/canceled states without granting or claiming access from the redirect. Suspended or closed Affiliate enrollments keep historical aggregate ledger access but disable code copying and clearly stop promotion. Component tests cover consent unavailability, offer discontinuation, consent-independent referral, retry identity, delayed signed projection, terminal payment failure and code suspension.

UI7 public-discovery checkpoint (2026-08-25): all seventeen crawlable public routes now publish one production canonical URL under `https://www.infiniteocean.net`, index/follow directives, complete Open Graph and large-card metadata, and route-appropriate schema.org JSON-LD. The root, feature inventory, each durable feature detail and live Catalog pricing surface expose SoftwareApplication, ItemList, WebPage or Offer discovery data without embedding local runtime origins. `robots.txt` names the production sitemap; the generated sitemap contains the exact reviewed route inventory. A checked-in 1200-by-630 PNG social card is generated from an editable SVG by a pinned Linux renderer that fails if fonts or dimensions disappear. Unit tests lock canonical/social/schema escaping, while the certificate-verified local Docker gate fetches every sitemap location, validates PNG type/signature/size and follows every relative link discovered in the rendered public pages. The complete frontend gate passes 56 private, 6 public and 39 API tests; current budgets remain 107.5/10.0 KiB for private JavaScript/CSS and 87.7/4.3 KiB for public JavaScript/CSS.

UI7 Catalog-resilience checkpoint (2026-08-25): the rendered pricing boundary no longer loses its last verified publication on a transient Account API failure. Nuxt admits only a fully validated, provider-field-free projection, requires monotonic versions, rejects a changed body under the same version and gives the upstream request a two-second deadline. A recent publication may be reused for at most five minutes with explicit stale/cache-age headers, a short revalidation cache policy and visible customer copy that Checkout will revalidate the selected offer. With no recent snapshot, the endpoint returns `503`, `no-store`, `Retry-After` and an unavailable marker while the page retains free signup and hides paid prices. Shared Catalog state preserves the warning across SPA navigation, prevents retained data from leaving paid offers visible after a failed refresh and applies the same disclosure to feature availability. The opt-in local recovery gate exercises the real Nuxt/Caddy composition by priming fresh state, pausing only the local Account API, proving exact stale-body and rendered-warning behavior on pricing and feature pages, restarting the stateless website to prove the empty-cache path, then restoring the service and requiring fresh recovery. Unit coverage proves fresh/stale/expired, rollback, same-version mutation, provider-field rejection, keyed-payload reuse and failed-refresh handling. The complete frontend gate passes 56 private, 12 public and 39 API tests; the public JavaScript budget is now 86.4 KiB gzip and public CSS is 4.4 KiB. Stage and production are not contacted.

UI7 mobile-navigation checkpoint (2026-08-25): the Nuxt acquisition menu now behaves as a bounded modal drawer on narrow layouts rather than a visual-only dropdown. Opening it moves focus to an internal close control, makes the underlying page and consent surface inert, locks body scrolling and adds a non-keyboard scrim. Forward and reverse Tab traversal remain inside the menu, Escape and explicit dismissal restore the labeled trigger, route selection closes the menu and focuses the main landmark, and widening to the desktop breakpoint removes the modal state. Unmount cleanup cannot strand the document in a locked state. Happy DOM component tests pin those transitions, the rebuilt local public image serves the new shell through the UbuntuRojo Docker edge, and a desktop browser accessibility-tree inspection confirms the Primary navigation remains exposed while mobile-only controls remain hidden. The complete frontend gate passes 62 private, 16 public and 41 API tests; both targets typecheck, lint and build, and current gzip budgets are 108.8/10.1 KiB for private JavaScript/CSS and 88.9/4.4 KiB for public JavaScript/CSS. This is source, component and local integration evidence only; signed-in phone reflow, real-device input, browser-computed contrast, axe and assistive-technology certification remain open. Stage and production are not contacted.

UI7 consent-accessibility checkpoint (2026-08-25): the public acquisition banner now gives Accept analytics and Reject non-essential the same secondary-button treatment instead of visually preferring consent. Finishing either a simple choice or a managed-purpose save moves focus to the resulting Privacy choices control; reopening or expanding purpose management moves focus to the first purpose checkbox, so a removed trigger never strands keyboard focus on the document. A failed first choice keeps both optional purposes off, keeps the choice panel operable and announces the failure. Three mounted component cases pin equal treatment, explicit purpose payloads, successful focus transitions and the fail-closed error path. The public suite now passes 19 tests, Nuxt typechecking, repository lint and a production build; its gzip result remains 88.7 KiB JavaScript and 4.4 KiB CSS. The rebuilt public image is healthy behind the local UbuntuRojo Docker edge. Legal wording, browser/device/assistive-technology certification and the approved consent-policy release record remain separate launch gates. Stage and production are not contacted.

UI7 package-aware-menu checkpoint (2026-08-25): the authenticated drawer and desktop rail no longer expose the same sixteen-link inventory to every Account. One flat Workspace group always retains Your Turn and Baseline, adds direct destinations only for packages whose current entitlement is enabled or read-only, keeps a suspended package out of active navigation, and replaces all absent package rows with one compact Explore plans link showing the number of unavailable areas. A separate flat Account group retains Account, Billing, Security, export, lifecycle, Affiliate and Privacy administration without nested fly-outs or a second route taxonomy. Switching Accounts recomputes the projection from the authoritative selected Account snapshot; navigating directly still reaches the route's explicit unavailable/read-only state and all mutations remain server-authorized. Component coverage proves empty, enabled, read-only and suspended projections while retaining drawer focus behavior. The private suite now passes 63 tests, repository lint and the production build; current gzip budgets are 109.0 KiB JavaScript and 10.2 KiB CSS. The rebuilt private image is healthy behind the local UbuntuRojo Docker edge, whose unauthenticated application journey still correctly returns to sign-in. Signed-in phone layout and assistive-technology certification remain open because no test login was used. Stage and production are not contacted.

UI7 checkout-analytics lifecycle checkpoint (2026-08-25): Checkout remains fully usable when the optional consent request is slow, fails or never settles, while consented customers no longer lose the rendered-review or signed-return measurements in the timing gap between Catalog/billing completion and consent resolution. Page-lifecycle events are held only in memory until the existing decision resolves, discarded on refusal, renewal requirement or lookup failure, suppressed after unmount and deduplicated across signed-projection polling. A billing return now records a bounded `returned` outcome plus the authoritative local projection result (`active`, `attention` or `failed`); cancellation remains distinct. Hosted Checkout redirection is never delayed to wait for optional measurement, and no customer, Account, payment, subscription or referral-code identity enters an event. The complete frontend gate passes 59 private, 12 public and 39 API tests, lint, both production builds, the 108.4/10.1 KiB private and 86.4/4.4 KiB public JavaScript/CSS budgets, and a high-severity dependency audit with zero findings. Stage and production are not contacted.

UI7 onboarding-analytics reliability checkpoint (2026-08-25): the active Vue Security route now records first owner-security readiness only after an authoritative post-mutation posture reload changes from incomplete to complete; replacing an already-complete recovery-code set does not inflate onboarding. First authenticated application entry is covered after session resolution and deduplicated within the tab. Public acquisition uses a shared consent resolver whose optional transport failures cannot overwrite an effective consent choice, and the free-pricing handoff joins the measured funnel. Analytics delivery uses same-origin `keepalive` requests outside the ordinary API/session-expiry boundary, so an optional 401 or provider failure cannot redirect or interrupt the customer. Server-rendered registration, verification and Account-creation markers carry transition-time, stable derived event identities; retries and refreshes reuse the exact envelope, accepted partial retries remain database-idempotent, and only bounded transient failures are retried. Events that occurred before consent are not backfilled after a later acceptance. The complete frontend gate passes 62 private, 14 public and 41 API tests, lint, both production builds, the 108.8/10.1 KiB private and 88.2/4.4 KiB public JavaScript/CSS budgets, and a high-severity dependency audit with zero findings. Stage and production are not contacted.

PA0 Affiliate launch-configuration checkpoint (2026-08-25): Affiliate settlement mode, accepted terms version and immutable commission-rule version are now explicit Account API deployment inputs instead of hardcoded transport values. Both feature gates remain closed and settlement remains `unconfigured` locally. Process startup rejects an unknown settlement mode, rejects either enrollment or attribution while settlement is unconfigured, and requires the selected database rule to exist before either gate can open. Enrollment independently rejects a missing owned Account for Account-credit mode and rejects an Account identifier in cash mode, so a forged client cannot bypass the Vue form's settlement shape. Compose defaults, local and Stage environment templates, the Stage-secret generator, runtime configuration and machine process inventory carry the same fail-closed inputs. The candidate `$10` on a qualifying `$50` renewal is still not an operative promise: Catalog amount, settlement mechanism, tax and commercial policy approval remain release-owner decisions.

UI7 mobile-navigation and privacy-control checkpoint (2026-08-25): the mobile drawer now gives its long destination list an internally scrolling region while keeping Account selection reachable, exposes modal navigation semantics only while open, removes its visual scrim from keyboard and accessibility traversal, and makes the background inert until close. Focus coverage now uses the real first and last enabled drawer links in both directions rather than a disabled Account fixture. Every Vue modal dialog also isolates the rest of the application with reversible `inert` state while preserving initial focus, bidirectional containment and opener restoration. Standalone Affiliate review/cancel controls meet the 44-pixel product target. In the private GDPR center, `Reject non-essential` now persists both refusals in one action; a failed save restores the last effective server decision and never announces success. The complete frontend gate passes 60 private, 12 public and 39 API tests, lint, both production builds, the 108.6/10.1 KiB private and 86.4/4.4 KiB public JavaScript/CSS budgets, and a high-severity dependency audit with zero findings. The complete uncached Go suite and process/Compose configuration checks pass. Stage and production are not contacted.

GDPR fulfillment checkpoint (2026-08-25): privacy-rights staff fulfillment is no longer an untracked manual mutation. The new short-lived operator mode requires phishing-resistant action authorization and exact environment confirmation, records every inspection, binds review and resolution to the current request version, and accepts only terminal `completed`, `partially_completed` or `declined` outcomes after review. Resolution stores an opaque evidence UUID and SHA-256—not the artifact or customer content—in an immutable event. Execute-only security-definer functions keep the declared operator role off the request tables, while migration compatibility triggers preserve customer submit/cancel writes during a rolling binary overlap. Domain, bootstrap and fresh-PostgreSQL coverage prove invalid input, stale conflicts, evidence persistence, immutable events, least privilege and old/new write compatibility. The customer continues to see only the request lifecycle state in the Vue Privacy route.

UI7 aggregate-analytics checkpoint (2026-08-25): collected landing, checkout and onboarding events now have an operator-usable JSON report without exposing the raw event store. The database security-definer function returns only time bucket, reviewed event/surface/dimension, event count and distinct-subject count; enforces reviewed dimensions, bounded windows and a minimum cohort of five; and has no PUBLIC execute grant. A dedicated reporter role can execute that function but cannot select raw events. The one-shot Go command validates the same boundary, local Docker provides `make analytics-report`, and unit plus fresh-PostgreSQL tests prove aggregation, suppression, invalid-dimension rejection and least privilege. Independent event counts are explicitly not represented as same-subject conversion or Affiliate authority. The architecture's one-time public-to-private conversion receipt remains open, so this is not the analytics exit checkpoint. Stage and production are not contacted.

UI7 conversion-continuity checkpoint (2026-08-25): a consented public signup-handoff event now issues a separate signed analytics receipt cookie for the exact configured Infinite Ocean parent domain and `/api/v1`. It is HTTP-only, Secure, SameSite=Lax, expires within 24 hours and contains only the anonymous public subject, exact handoff event and expiry. Reviewed registration, verification, Account creation, security, checkout, subscription and first-entry milestones are mirrored under that anonymous source, so the aggregate report can expose a same-source `conversion` surface. The conversion table has no private subject, User, Account or private-event identifier, one receipt records each milestone at most once, and public erasure cascades through the mirror. Private erasure also removes the public acquisition subject while the live receipt proves the association. Token HMAC domain separation, parent-domain validation, transport behavior, target-identity absence, cohort suppression and erasure pass unit and fresh-PostgreSQL tests. The ordinary preference cookie remains host-only, refusal still issues no handoff, and Stage/production are not contacted.

UI7 public rendered-accessibility checkpoint (2026-08-25): the frontend Docker gate now starts the exact production Nitro output and certifies all seventeen public landing, feature, pricing and policy routes. Every route must return rendered HTML with English language metadata, a non-empty title, one header/main/footer/level-one-heading frame, a first-body skip link to a programmatically focusable main landmark, unique IDs and no axe-core WCAG 2.0/2.1/2.2 A/AA finding that jsdom can evaluate. The gate deliberately excludes only `color-contrast`, which requires a real browser's computed layout and remains open below. All 123 private/public/API package tests, type checking, lint, both builds, gzip budgets and the high-severity dependency audit pass in pinned Node 24 Docker; npm reports zero vulnerabilities. This closes exact-build structural automation for the public surface, not browser-computed contrast, zoom/reflow, real-device or assistive-technology certification. Stage and production are not contacted.

UI7 Vue template-semantics checkpoint (2026-08-25): the Docker lint gate now applies the recommended Vue accessibility rules to both customer applications. It accepts both standards-valid native label associations, while the only interaction-rule suppressions are line-local and document containers whose key handlers operate only when their conditional mobile-dialog role is present. Public and private navigation containers no longer retain dialog labels in desktop state; Affiliate and Work summary clusters and the Your Turn filter expose explicit group semantics; and the Your Turn loading skeleton is a named status whose visual cards are hidden from the accessibility tree. Component tests pin the conditional dialog labeling and the new group roles. The complete frontend gate remains green with 63 private, 19 public and 41 API tests, both builds, the seventeen-route rendered gate, gzip budgets and zero npm vulnerabilities. Real-browser and authenticated-device evidence remains open; no release environment is contacted.

UI7 private rendered-state accessibility checkpoint (2026-08-25): one shared fixture verifier now runs the axe-core WCAG 2.0/2.1/2.2 A/AA rules that happy-dom can evaluate against detached Vue output safely cloned into the document. Thirty-nine scans cover the application shell and all eighteen private view families, including Your Turn list/detail, checkout, GDPR Privacy, Affiliate, Account, Work, Knowledge, Baseline, Agents, schedules, Finance, Marketing, Integrations, Billing, Security, exports and lifecycle. Multi-test mount helpers also exercise alternate durable routes, roles, package modes, program states and workflow states. Cleanup occurs in `finally`, and fake-timer checkout cases keep their scan at the stable non-timer fixture boundary. `color-contrast` remains excluded because happy-dom has no rendered pixel model. All 63 private tests pass with the gate active. This is deterministic component-state evidence, not an authenticated browser, responsive-layout, screen-reader or real-device certificate; no credentials or deployment environment are used.

UI7 launch-critical browser checkpoint (2026-08-25): the normal `website-test` Docker build now cannot succeed without a digest-pinned Playwright 1.62.1 pass against the exact private production artifact. A deliberately synthetic same-origin API boundary supplies only reviewed content-free session, Catalog, billing, consent, rights, attention and Affiliate fixtures; any unhandled request or browser runtime error fails the test, and no identity, credential, database, provider or deployed origin is used. Seventeen applicable checks run at 1280×900 in Chromium, Firefox and WebKit plus 390×844 Chromium phone emulation. They certify Your Turn, deliberate Checkout referral application, equal GDPR rejection, verified-rights framing, closed Affiliate economics, horizontal reflow, mobile focus containment/restoration and browser-computed axe WCAG A/AA rules including color contrast. The first run exposed a real 4.3:1 Checkout package-state label defect, now corrected with the darker design token. The integrated gate passes all 123 package tests, seventeen public rendered routes, type checking, lint, builds, budgets and the zero-vulnerability audit. This checkpoint does not replace authenticated identity/provider journeys, mobile Safari/TalkBack, zoom/forced-colors, physical devices or assistive technologies.

UI7 public acquisition, security-onboarding, Account-administration and operating-context browser checkpoint (2026-08-25): the same digest-pinned job now also boots the exact production Nitro output against a content-free Catalog and synthetic same-origin consent/event boundary. All seventeen landing, feature, pricing, privacy and Affiliate-terms routes run in Chromium, Firefox and WebKit desktop; Chromium phone emulation at 360×800, 390×844 and 412×915; Chromium reflow at 320×800; a touch-enabled Chromium tablet at 768×1024; and Chromium forced-colors and reduced-motion emulation at 1280×900 with an asserted motion media state; a separate Chromium profile enlarges text to 200% at 1280×900 across seventeen primary private and seventeen public routes. The checks prove silence without consent and after rejection, reviewed event delivery after acceptance, destination-preserving signup handoff, opaque offer transfer independent of analytics, complete feature/policy rendering, horizontal reflow, rendered axe including color contrast, and phone-menu focus containment/restoration. Private GDPR controls now prove tracked Affiliate-scoped erasure submission, open-request deduplication, cancellation and an exact `/app/privacy` return target when passkey reauthentication is required; the ceremony itself remains an external acceptance item. The private Security route separately proves that owner-security completion is absent while setup is incomplete, appears only after the recovery-code mutation reloads authoritative ready state, remains distinct from first application entry, and retains the one-time codes for the customer. Account, Billing, exports and lifecycle prove owner authority, paid-account state, portability availability and closure consequences remain understandable. Multi-Account selection adopts only the server-confirmed context, while a failed selection restores the prior Account and reports that no authority changed. Durable Work, governed Knowledge, resumable Business Baseline, Agents/Boardrooms/conversations, Schedules, Finance, Integration connections/executions and Marketing campaigns/releases preserve list/detail operating context across all ten standard profiles and the primary route set additionally reflows under 200% text. Read-only package fixtures retain governed evidence while removing mutations; absent Integrations and Marketing packages expose explicit upgrade paths; and a 503 Account-load failure retains a usable, accessible sign-in recovery path. Your Turn retains the last queue while offline, refreshes authoritatively on reconnect, preserves its exact route through session-expiry sign-in and sends only one mutation when duplicate submit events arrive during a pending decision; capacity denial retains the customer draft, stale Work, Marketing, Membership and Schedule mutations reload current authority before a visible retry instruction, Stripe failure leaves payment and Account state unchanged, and a scoped Integration-provider failure retains the exact customer query and reviewed connection. This work exposed a real 2.82:1 landing-step contrast defect; four separate 4.3:1 Checkout, member-email, Agent-confidence and Marketing-release contrast defects; a narrow-screen governed-identifier overflow; an unreachable scrollable Baseline progress track; a desktop navigation CTA that bypassed the acquisition event boundary, and fixed-count feature, Privacy, Account, action, master/detail and Security grids that overflowed under text enlargement. All are corrected. The combined public/private job now has 291 applicable passes and eleven intentional skips on profiles where compact navigation is not rendered. No identity, credential, database, provider or deployed environment is used. Identity entry ceremonies, remaining package-domain mutation conflicts, capacity denials and downstream failures, 200% text-only zoom on connected identity pages, 400% browser zoom/reflow, physical devices and assistive technologies remain open.

UI7 connected-local identity checkpoint (2026-08-25): a separate test-profile container now shares the rebuilt local Caddy network namespace and reaches the exact Go-rendered login, signup, password recovery/reset, registration-verification and verified-contact pages at their configured TLS origin. Twelve checks run in the same three desktop engines and Chromium phone profile. They certify skip-link focus, responsive overflow, browser axe including contrast, equal/reversible analytics rejection, offer continuity, native validation and safe incomplete-link recovery without creating a User or submitting a password. The first run proved that the optional-consent card covered signup's primary action; it now stays over the story pane on desktop and enters normal flow on single-column layouts. Successful email/password/passkey and owner-enrollment ceremonies, deeper denial/recovery states, zoom/forced-colors, physical devices and assistive technologies remain open.

UI7 package-recovery checkpoint (2026-08-25): exact-build browser interactions now prove authoritative conflict reloads for Knowledge decisions, immutable Agent Persona publication, Finance posting and Integration disable commands across all ten standard profiles. The Knowledge path retains its reconsiderable reason while replacing the stale claim; the modal command paths close stale editors and expose retry from the refreshed record. Opening the real Persona editor also found and corrected an invalid Unicode-set HTML pattern. The combined job now has 301 applicable passes and eleven intentional profile skips.

UI7 connected-identity text checkpoint (2026-08-25): a fifth connected-local profile enlarges text to 200% across all six login, signup, verification and recovery routes. The exact Go-rendered pages retain their primary headings, horizontal reflow, browser-computed axe results and zero runtime errors. Together with the existing three desktop engines and phone profile, the connected job now has thirteen passes and still creates no User or submits a password.

UI7 capacity/downstream checkpoint (2026-08-25): the exact-build matrix now proves that Agent concurrent-run capacity denial preserves the Boardroom subject, question and Persona selection. Simulated downstream failures preserve retryable intent for Knowledge decisions, Baseline answers, Finance posting, Marketing revision, Integration commands and Schedule actions across all ten standard profiles. The run exposed and corrected transparent modal surfaces that failed contrast only while an error kept the modal open, and Schedule errors that rendered behind their retry dialog. The combined job now has 311 applicable passes and eleven intentional profile skips.

UI7 Affiliate/GDPR adversarial checkpoint (2026-08-25): all ten standard profiles now prove the active aggregate Affiliate renewal ledger without referred-customer identity, then a suspended reload that disables code sharing and new attribution while preserving history and appeal access. Checkout proves the deliberately applied code remains visible and retryable after backend self-referral denial even with analytics denied, and that no optional event is emitted. The Go projector explicitly proves final lost-dispute reversal; existing service and connected-PostgreSQL evidence covers account-owner self-referral denial, invoice replay, recurring cycles, Refund reversal/replay, adverse-before-earning reconciliation, suspension and immutable customer-detached history. The exact-artifact matrix now has 331 applicable passes and eleven intentional profile skips. Program flags and settlement remain closed.

UI7 Your Turn safe-navigation checkpoint (2026-08-25): the consequential decision form now blocks route changes while its authoritative mutation is pending and announces that state; dirty drafts invoke the browser's native leave confirmation, remain exact when the customer cancels and permit navigation only after confirmation. A browser-unload safeguard covers both protected states, and the successful application-controlled transition has a one-use bypass. The exact-artifact matrix now has 341 applicable passes and eleven intentional profile skips, with the complete component, type, lint, build, rendered-public-accessibility, budget and dependency-audit gate green. This closes safe navigation for Your Turn only. The remaining feature editors still require the same audit before the cross-feature acceptance item can close.

UI7 core-workspace safe-navigation checkpoint (2026-08-25): Work, Knowledge, Business Baseline and Agents now use the same protected-state boundary. Existing all-profile recovery journeys prove retained input after canceled leave and deliberate departure after confirmation; Work also denies route changes during an unresolved create request. Agent Persona policy changes are surfaced from the child editor, and successful app-controlled redirects use the one-shot bypass. The matrix remains 341 applicable passes and eleven intentional skips because the coverage strengthens existing tests. Account administration and the remaining package/security/privacy editors still require audit.

UI7 package-editor safe-navigation checkpoint (2026-08-25): Schedules, Finance, Integrations and Marketing now protect open editors/commands and unresolved mutations with the same route and unload boundary; Integrations additionally protects a changed research query until a successful search records it as submitted. Successful internal detail redirects and required strong-auth redirects use the one-shot bypass. The all-profile downstream-failure journey proves native Back dismissal retains exact retry input and acceptance leaves, while the scoped research failure proves custom SPA confirmation, visible canceled-navigation status and retained query. Focused type checking, eleven component checks and twenty exact-artifact browser checks pass. The matrix count remains 341 because existing tests gained stronger assertions. Account administration, Billing/Checkout, Security, Privacy, exports and lifecycle still require audit.

UI7 customer/account safe-navigation checkpoint (2026-08-25): Account administration, Billing/Checkout, Security, Privacy, Account exports/lifecycle and Affiliate now complete the same guard audit. Protected state includes invitation/team input, reviewed offer/referral/confirmation, consent changes, browser-erasure confirmation, Affiliate enrollment, open commands and every unresolved mutation. Newly generated recovery codes are treated as unsaved one-time state until the customer deliberately confirms leaving. Fifty exact-artifact checks across all standard profiles and twenty-nine focused component checks pass; a source audit finds no private Vue view with a form or `v-model` outside the guard. Cross-feature safe navigation is complete. The full matrix count remains 341 because existing journeys gained assertions.

UI7 400% browser-scale checkpoint (2026-08-25): the pinned Chromium matrix now has a dedicated project whose device-metrics override must report a 1280×900 surface, 320×225 CSS viewport and DPR 4. Its private and public inventory checks cover seventeen routes each, assert primary content, horizontal reflow and browser-computed axe, and retain the public pre-consent silence rule. Both pass. The exact-artifact matrix is now 343 applicable passes and eleven intentional skips. Automated 400% zoom/reflow is complete; real hardware and assistive technology remain external acceptance work.

UI7 local-provider preparation checkpoint (2026-08-25): local Compose may now receive a Stripe-only provider override from an explicitly supplied mode-600 file on UbuntuRojo's native filesystem. The deterministic checked-in environment remains provider-free, the Hostinger endpoint secret is not a valid local-listener secret, and omission of the override preserves existing tests. This prepares but does not certify hosted Checkout. Real Stripe test Prices, governed local Catalog mappings, a local webhook listener and externally issued Catalog operator authorizations remain required; no Stage or production resource was changed.

UI7 Affiliate/GDPR completion-audit checkpoint (2026-08-25): the active Affiliate dashboard now supplies the architecture's missing referral-link control. It contains only the generated public code, explains that the customer must still apply it, and disappears with both copy actions disabled when attribution is suspended. Confirmed browser-subject erasure now enters the shared pending-state boundary, rejects duplicate activation and blocks route changes until the deletion response completes. Component coverage raises the private suite to 67 checks; ten all-profile browser checks raise the exact-artifact matrix to 353 applicable passes plus eleven intentional skips. Type checking, lint, both production builds, rendered public accessibility, bundle budgets and the high-severity dependency audit pass. Provider, physical-device and assistive-technology gates remain separate.

UI7 Affiliate public-code replacement checkpoint (2026-08-25): an active Affiliate can now replace the public code for future referrals through an explicit two-step Vue action. The server requires recent passkey confirmation and exact enrollment version, publishes only a server-generated successor and permanently reserves every retired code through migration 49. The former code and link stop future referral lookup; locked subscription attribution and commission history remain unchanged. The UI preserves its confirmation as protected state, blocks route changes while the request is pending and sends weak authentication to Security with an exact return route. Service, HTTP, generated-contract, API-client, component, all-profile exact-artifact and fresh-PostgreSQL tests cover success, stale/weak/suspended rejection, pre- and post-lock replacement, no reuse and immutable history. The contract has 208 operations; the unchanged browser matrix passes 353 applicable checks plus eleven intentional skips, with 68 private, 19 public and 42 API component checks.

UI7 Affiliate statement-completeness checkpoint (2026-08-25): the dashboard now fulfills the architecture's aggregate referred-subscription and monthly-history requirements. The API counts only locked attributions and returns no referred-customer identifier; one repeatable-read snapshot binds that count to the returned ledger. Vue groups the Affiliate's immutable entries into UTC calendar months with earned/reversed subtotals and retains overall server totals plus opaque entry-bound review. Service, generated-contract, Vue, all-profile exact-artifact and fresh-PostgreSQL checks pass; the matrix remains 353 applicable browser passes plus eleven intentional skips and 68/19/42 private/public/API checks.

UI7 Affiliate acquisition-continuity checkpoint (2026-08-25): an unauthenticated Affiliate checkout link now retains its complete same-origin checkout destination through sign-in and both identity branches: free-Account registration/verification and returning-customer password recovery/reset. Both encrypted queued emails carry the destination through password setup and the final sign-in redirect. Every browser-rendered `return_to` value is revalidated as a relative same-origin target; focused service, notification, SMTP and browser-server tests cover the complete handoffs without weakening recovery anti-enumeration. The public code remains a proposal after return—Checkout still requires an explicit **Apply** and confirmation—and refusal of optional analytics emits no event. First private application entry now uses the reviewed `checkout`, `your_turn` or `deep_link` taxonomy instead of mislabeling direct Checkout traffic as Your Turn; the ingestion registry rejects unreviewed values. The exact-artifact gate passes 363 browser checks plus eleven intentional skips and 69/19/42 private/public/API tests; private/public JavaScript and CSS are 113.0/10.3/88.2/4.5 KiB gzip with zero npm vulnerabilities. This is local acquisition construction only and does not open the Affiliate flags or authorize deployment.

UI7 analytics-registry completion checkpoint (2026-08-25): the server-side launch registry now enforces the documented event surface as well as the field list. Public acquisition events cannot be admitted under a private consent subject, private onboarding/product events cannot be admitted under a public subject, and every categorical field has an executable allowlist matching the revision-controlled processing registry. Plausible but unreviewed device, security, referral, Checkout, application-entry and Your Turn categories fail ingestion instead of silently fragmenting reports. Focused analytics, ingestion, conversion and HTTP tests plus vet and formatting checks pass. No browser artifact, provider or deployed environment changed.

UI7 connected identity success checkpoint (2026-08-25): the local identity certificate now completes PostgreSQL-backed registration, encrypted queued verification delivery, password creation, native login into the real Vue Your Turn route, anti-enumerating recovery intake, encrypted queued reset delivery, password replacement, old-password rejection and replacement-password login in Chromium, Firefox, WebKit and the Chromium phone profile. The fifth profile retains 200% text coverage; a sixth Chromium virtual-authenticator profile completes first-passkey enrollment, recovery-code creation, authoritative owner readiness, discoverable login, simulated authenticator loss, password-plus-code replacement while the lost credential remains recorded, and replacement-key reauthentication. All eighteen checks pass. The work exposed opaque native mutation origins under `no-referrer`, asynchronous passkey redirects reading a cleared `event.currentTarget`, and backend recovery grants ignored whenever any credential row remained. The response policy, all three passkey redirect handlers and the retained-credential recovery authorization are corrected. Failed registration completion records only its internal error locally, never the email, token or password. Synthetic Accounts are uniquely named, consumed messages are deleted immediately, identity traces are disabled and the canonical script resets only local anonymous login/recovery budgets. Physical passkeys, real mobile hardware and assistive technology remain external certificates; no deployed environment was contacted.

UI7 mobile consent prominence checkpoint (2026-08-25): rendered 390-by-844 phone inspection found that the public first-choice consent sheet displaced both acquisition actions and that the server-rendered identity card consumed enough vertical space to hide the onboarding form heading. It also found an unacceptable visual preference for **Accept optional** over **Reject optional** on identity entry despite their equal semantic authority. Both surfaces now use concise content-free analytics copy, compact dynamic-viewport-bounded panels and equal two-column accept/reject controls; the public landing keeps both hero actions wholly above the consent sheet at 320, 360, 390 and 412 CSS-pixel widths, and the identity form heading remains in the initial 390-by-844 viewport. The exact-artifact gate passes 363 applicable checks plus eleven intentional skips and the connected identity gate passes eighteen checks. A later screenshot captured the documented fail-closed consent warning while the UbuntuRojo Docker server was unreachable; that image is not accepted as endpoint evidence. After the bridge recovered, independent certificate-routed GETs to both public and private consent endpoints returned `200` with their undecided, optional-purposes-off projections. Physical devices, assistive technology and provider-backed journeys remain open.

UI7 consent-lifecycle visibility checkpoint (2026-08-26): the public privacy route and private Privacy workspace now expose the immutable optional-purpose decisions for their own host-only browser subject, newest first, without rendering internal receipt or subject identifiers. The public preference panel links directly to that history and adds confirmed public-browser erasure; successful erasure clears the public consent/analytics subject, keeps identity, Account, billing and Affiliate records outside its scope, fail-closes measurement and immediately restores the equal Accept/Reject first layer. Private history lookup now performs the same identity-link/replacement check as current consent before returning decisions, so a stale browser reference already owned by another signed-in User is replaced rather than disclosed across identities. Focused HTTP and component tests cover the boundary. The exact-artifact gate passes 373 applicable browser checks plus eleven intentional skips, 70 private, 22 public and 42 API tests, type checking, lint, both builds, all seventeen rendered public routes, 113.3/10.3/90.5/4.7 KiB private/public JavaScript/CSS budgets and a zero-vulnerability audit. No provider or deployed environment was contacted.

UI7 has not exited. Authenticated rendered route coverage, required phone/tablet widths, 400% browser-scale reflow, forced-color and reduced-motion checks, implemented package conflicts and capacity/downstream recovery, 200% connected-identity text enlargement, synthetic Affiliate/GDPR adversarial journeys, and cross-feature safe navigation are automated. Real iOS Safari and Android Chrome, assistive-technology sessions, and provider-backed checkout/security journeys remain open. These are construction/certification tasks, not authorization to publish or deploy.

Exit condition: the application is feature-complete locally and ready for the separate Stage/production release plan only after the remaining UI7 evidence above is complete and every launch flag, settlement policy and provider journey has its required approval and certificate.

## 7. Required test matrix

At minimum, automated responsive coverage includes 320, 360, 390 and 412 CSS-pixel phone widths, a representative tablet width and desktop widths. Real-device certification covers current iOS Safari and Android Chrome in addition to desktop browsers.

Every feature slice covers:

- loading, empty, partial, stale and server-error states;
- offline, reconnect, request cancellation and session expiry;
- unauthorized, locked, package-disabled and read-only behavior;
- keyboard-only operation, visible focus, screen-reader names/order and live announcements;
- 200% and 400% zoom/reflow, reduced motion, contrast and forced colors;
- long customer text, localization-safe wrapping and no horizontal page overflow;
- exact mutation idempotency, current ETag use and conflict recovery; and
- safe navigation with unsaved or in-flight work.

Public acquisition and checkout additionally cover crawlability without client execution, metadata, structured content, broken links, Catalog fallback, offer discontinuation during signup, repeated checkout initiation, Stripe cancellation, delayed webhook projection and payment failure.

Privacy, analytics and Affiliate acceptance additionally covers zero optional emission before consent, immediate withdrawal, registry field enforcement, GDPR access/erasure/objection/retention behavior, consent-independent referral survival, self-referral denial, code suspension, invoice replay, refund/dispute reversal and referred-customer concealment.

## 8. Final exit gate

Phase 3 product-surface construction is complete only when:

- Your Turn is the default, fully certified primary workflow;
- the landing page clearly communicates the product and Your Turn value proposition on mobile;
- every launch feature and package has an accurate, browsable public explanation;
- free signup and paid checkout form one complete, safe and locally certified acquisition journey;
- landing, checkout and onboarding analytics are GDPR-capable, consented, content-free and lifecycle-complete;
- Affiliate enrollment, code attribution, recurring commission and aggregate dashboard behavior are locally complete under an approved settlement policy;
- every launch use case is available through the Vue SPA;
- the mobile menu and route hierarchy remain consistent across packages;
- supported phone routes are clear, touch-usable and free of horizontal page scrolling;
- generated contracts, authorization outcomes and mutation safety remain unchanged;
- the complete local Docker, frontend, Go, contract, accessibility and browser suites pass; and
- Stage, GHCR and LKE work can begin without further application construction.
