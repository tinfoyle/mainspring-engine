# Phase 3 final construction plan

- Plan date: 2026-08-20
- Entry date: 2026-08-21
- Starts after: completed [Phase 2.5 realignment](phase-2-5-closeout-report.md)
- Purpose: complete every remaining application capability, migrate validated prototype behavior, certify the final LKE deployment and release the complete product
- Production rule: **all construction phases and advertised packages must be complete before production release**

## Current Stage review window

Adaptive Baseline checkpoint (2026-09-03): the new-customer Baseline is now a durable conversation with a built-in `Operations Guide`, not the former seven-question/evidence-review interface. The Agent begins with the business and its revenue model, then chooses relevant follow-ups for the kind of company it discovers. Exact owner replies become accepted Knowledge through the existing governed boundary; Agent interpretation does not. Useful automation suggestions require an explicit later approval before one idempotent, conversation-linked setup Work item is created and assigned to the Operations Guide. Agent execution can pause that Work with a plain-language question in Your Turn; the owner can answer there without navigating through Knowledge administration, and the accepted answer resumes the Work. Domain validation prevents non-`baseline.*` answer keys and rejects a ready result with low business confidence, fewer than four captured topics, outstanding missing topics or another unanswered question. Focused Go/provider/Vue/accessibility tests, generated-contract checks, all frontend unit/type/lint gates, exact production builds, a Chromium exact-artifact adaptive browser journey and the connected local commercial/Baseline PostgreSQL certificate pass. The former checklist browser journey has been removed. This implementation is deployed to Hostinger Stage as RC.32; a connected provider-backed browser certificate of the complete adaptive conversation and resumed Agent Work remains before launch acceptance.

Connected Stage adaptive-Baseline checkpoint (2026-09-03): RC.30 was built from source revision `ca385a9839d049319138e6134e5ab1468847759f`, published as three exact GHCR digests with BuildKit provenance/SBOM attestations, and admitted by zero-finding high/critical vulnerability and secret scans. Manifest commit `0f979a5b057cf092a5202235d8e82f5ff42bcf5b` is the clean checkout selected by `/opt/spyglass-stage/current`. The Stage verifier passed before and after rollout; all 48 healthchecked workloads are healthy among 49 running containers, the retained global/cell migration ledgers remain 68/80/80, and the public, login and MCP metadata origins return `200`. The deployment changes no production resource and is authorized for authenticated owner review, not LKE promotion.

Baseline compatibility checkpoint (2026-09-03): the first authenticated RC.30 review exposed a blank Baseline main panel when an existing assessment contained `null` rather than empty arrays for Agent-result collections. RC.31 fixes both sides of the contract: Go canonicalization preserves empty JSON arrays for new results, while Vue normalizes historical null collections before any render-time length check. Focused regression tests exercise all four nullable collections; the complete Go suite, vet, 92 private-UI tests, type checking, lint and production build pass. RC.31 was built from source revision `02758392f8ac169ae0364542143e44ad64ef4fd2`; tracked manifest/checkout revision `746bce1f6c90044eeca99bb438d16283dc965ae0` is selected by `/opt/spyglass-stage/current`. All images carry provenance/SBOM attestations and passed high/critical vulnerability and secret scanning with zero findings. Post-deploy verification reports 49 running containers, 48 healthy healthchecked workloads, one internal edge without a healthcheck, no unhealthy/starting workload, no severe startup log line, unchanged migration ledgers 68/80/80 and `200` from public, login and MCP metadata origins. The previously affected authenticated assessment now visibly renders the adaptive interview, reply control and business notebook from the RC.31 bundle without a new console error.

Baseline prompt-placement checkpoint (2026-09-03): RC.32 keeps the Agent's structured `next_question` inside the conversation even when the provider's contribution omits that question, while avoiding duplication when it is already present. The reply textarea no longer borrows Agent content as its placeholder; it remains empty with the neutral `Type your answer here…` hint. All 93 private-UI tests pass in a serialized run, including the new presentation regression, and type checking, lint and the production build pass. Source revision `22df0ef272bb68fcd17b555a4914ec4e84671a61` is recorded by manifest/checkout revision `f4d05636b07034558922db88ea58bcbce64d525e`. The three RC.32 images carry provenance/SBOM attestations and passed high/critical vulnerability and secret scanning with zero findings. Post-deploy verification reports 49 running containers, 48 healthy healthchecked workloads, no unhealthy/starting workload, migration ledgers 68/80/80 and healthy public endpoints. Authenticated browser evidence confirms the real pending question inside the Operations Guide bubble, an empty human-only reply field, and an empty console.

Local required-security-choice checkpoint (2026-08-30): the owner setup wizard now asks a plain three-way question before Account authority opens: passkey (recommended), text-message code, or email code (not recommended). The two code paths are full backend factors rather than UI placeholders: six-digit challenges expire in ten minutes, stop after five attempts, are limited to five sends per User per hour, keep SMS destinations encrypted, and can provide recent session-bound confirmation for privileged work. Local Docker discards SMS transport and exposes the code only in the authenticated development response. Stage is unchanged and must not receive this release until its protected notification-worker input contains an approved HTTPS SMS gateway, bearer credential, and sender identity and the provider-backed journey passes. Focused Go, generated-contract, Vue type, 86-component, and strong-auth browser tests pass; the fresh-PostgreSQL and complete exact-artifact gates remain before this checkpoint is locally closed.

Agent-only local certification checkpoint (2026-08-28): a fresh synthetic business owner completed registration, local email verification, password login, real software-backed ES256 WebAuthn enrollment, recovery enrollment, strong Account selection and OAuth S256 PKCE without using the customer browser UI. The authorized external client then operated Work, Your Turn, Boardrooms, Personas, Agent Runs, Schedules, Baseline, Knowledge, Finance, Marketing, Integrations, consequential-action approvals and Account exports through documented HTTPS and MCP boundaries. A deterministic local provider proved failure, human-authorized retry, metered completion and insufficient-AI-Token rejection before provider contact. Missing authority, cross-Account access, stale versions and self-approval failed closed. All 27 journey checks passed; all 94 published MCP tools reached their authenticated Account handlers, with 49 complete executions and 45 safe rejections on deliberately incomplete or inapplicable synthetic input. The run found and fixed Docker runner identity, retry-digest precision, dispatch terminalization and secure-local MCP workload-identity defects, each with regression coverage. Exact evidence and the remaining human/provider gates are in the [agent-only platform certification](agent-only-platform-certification.md). Stage, GHCR, Hostinger, LKE and production were untouched.

Phase 3 construction status after that certificate: no missing agent-only transport or core backend package capability remains. Product construction is at the human-review boundary. Remaining Phase 3 acceptance consists of the authenticated customer-UI review, Operations Console review, real Stripe/notification/model/integration provider journeys, physical-device and assistive-technology acceptance, privacy/Affiliate legal and Support approvals, and any retained prototype/internal-Account migration plus backup/restore/rollback/resilience evidence. Release packaging and Stage/LKE deployment remain the final deployment track rather than unfinished local feature construction.

Local Operations Console checkpoint (2026-08-27): the launch-blocking staff boundary is now locally feature-complete as a separate passkey-only Vue application and least-authority API. It provides privacy-bounded aggregate analytics, exact User/Account/billing lookup, timed and visibly marked read-only support inspection, customer-visible support history, billing failure/replay/refresh controls, privacy-rights queue/inspection/versioned resolution and Affiliate inspection/risk/lifecycle controls. Staff identity is never replaced by a customer identity; every action carries a ticket/reason and existing operator events record `operations/<staff-user-id>`. Five distinct least-authority database roles separate staff identity, read-only support projection, billing, privacy and Affiliate access; the last three are execute-only. Account erasure removes customer-linked support evidence only inside the exact approved erasure transaction while ordinary mutation/deletion remains denied. PostgreSQL authority/immutability/erasure tests, generated-contract checks, all UI unit/type/lint/build gates and the real local Docker browser journey pass. The runbook is [Operations Console operations](operations-console-operations.md). Stage remained RC.9 at that checkpoint; the later RC.30 customer release does not include the separate Operations Console.

The [Phase 3 local backend completion plan](phase-3-local-backend-completion-plan.md) and the local exact-artifact portion of the [Phase 3 Vue SPA product-surface plan](phase-3-vue-spa-plan.md) have reached their Stage-review boundary. The mobile-first customer application leads with Your Turn and the adaptive Operations Guide Baseline now runs on Hostinger Stage as RC.32.

Historical connected Stage checkpoint (2026-08-27): the matched RC.9 Go application, Nuxt public UI and Vue private UI were built from source revision `4e9b45e69aa7882ba8cb35eaf11c3185bc1bd529`, locally admitted, pushed to GHCR and deployed by exact digest from checkout `ebe0c4505d239d484d7540c94880022b98ce2ed8`. RC.9 retained the notification and landing corrections, replaced the Features card wall with a Your Turn-led product story, and added a revision-controlled UbuntuRojo publisher. All three AMD64 indexes carried attached BuildKit SBOM/provenance and passed the pinned Trivy vulnerability/secret gate with zero findings. All 46 healthchecked workloads were healthy among 47 long-running containers; global/cell migrations were 61/79/79. Live browser acceptance confirmed the new Features lead, all twelve detail links, canonical metadata and no horizontal overflow. Catalog v3 and the prior public/private origin acceptance remain active.

This is a Stage review authorization only. RC.32 carries SBOM/provenance and passed high/critical vulnerability and secret scanning, but it remains AMD64-only and unsigned. LKE and production remain untouched. Human acceptance of the adaptive Baseline, Affiliate renewal-credit, analytics/consent, external MCP, physical-device/assistive-technology and compatible rollback evidence remain before the complete-product production release. The Stage-only Catalog bootstrap identity is not acceptable for production.

Local UI checkpoint (2026-08-24): the separate Nuxt-rendered acquisition site and Vue private SPA are active in local Docker. The live Your Turn boundary, rendered landing/feature/pricing routes, authenticated Catalog checkout review, consented landing/checkout/onboarding event path, ordinary-identity Affiliate dashboard and passkey-verified privacy-rights intake are implemented and revision controlled. Authenticated private consent subjects bind to one User, browser privacy erasure remains immediate, and identity/Account/Affiliate/analytics access, correction, erasure, restriction, objection and portability requests have deadline and immutable-event evidence. Work now has paged queue/detail/create/assignment/lifecycle flows; Knowledge separates governed proposed claims and accepted facts with exact citations and human decisions; Baseline provides resumable guided onboarding, attributable owner-statement capture, evidence inventory/gap review, exact plan approval, Work/evidence closure, narrow source grants and reassessment; Agents provides Boardroom creation, Persona-policy inspection, durable conversations and bounded Run recovery while routing consequential proposals through Your Turn. Schedules adds a mobile list/detail/editor over the existing versioned, idempotent Scheduling boundary, including explicit local-time/DST policy and conflict-safe pause, resume, trigger and deletion controls. Account team administration now covers invitation, roster, role/state/removal, ownership transfer and ordinary-member leaving against exact Membership versions, including a typed self-Membership read that does not widen roster authority. Local Caddy serves and smoke-checks every durable route for those Vue surfaces. At this checkpoint, Marketing migration, responsive/browser certification, Affiliate commercial/reversal/support policy and staff rights-fulfillment operations remained construction work. Nothing in this checkpoint was released to GHCR or applied to Stage/LKE.

Security migration prerequisite (2026-08-24): the Account API added typed reads for the current verified identity contact and token-free connected MCP grants plus same-origin grant revocation. These closed the two parity gaps that would otherwise have made the subsequent Vue Security route lose legacy functionality.

Security Vue checkpoint (2026-08-24): the complete identity-security route now lives in the Vue shell with WebAuthn enrollment/confirmation, passkey lifecycle, password confirmation, recovery-code rotation/consumption, verified-contact initiation, active-session revocation, connected-MCP-client revocation and security history. One-time recovery material is never persisted by the client, destructive identity-wide actions require explicit phrases, and the exact Account API/private-UI Docker images plus local TLS route gate pass. Applied physical-passkey and responsive browser/accessibility evidence remains open; no Stage or production artifact was changed.

Account portability/lifecycle Vue checkpoint (2026-08-24): Account owners can now request, inspect, cancel and download governed Account exports from the private Vue SPA. Downloads mint a short-lived capability used only in the authorization header of a cookie-free artifact request; the capability is never put in the query string or persistent browser state. The identity-global lifecycle route requests version-bound Account closure with an operational reason, explains the immediate access freeze and seven-day cooling-off period, preserves recovery access after the freeze, and supports conflict-safe restoration without claiming immediate data erasure. Generated-contract clients, component tests, clean Docker production builds and local TLS route checks cover both surfaces. Applied responsive/browser/accessibility evidence and staff-operated rights fulfillment remain open; Stage and production were not changed.

Billing Vue checkpoint (2026-08-24): the Account-global Billing route now presents the local Stripe subscription projection, current Catalog plan identity, billing periods, scheduled cancellation and last verified synchronization. Visibility remains available to Account members while checkout and hosted management stay owner/billing-administrator only. Stripe portal creation requires recent passkey evidence, keeps one opaque idempotency key across exact retries and redirects only to HTTPS; its server-controlled return now lands on `/app/billing` and explains projection delay. Component/API-client/domain tests and the local TLS route gate cover this behavior. Real Stripe-portal and responsive browser/accessibility evidence remains open; no Stage or production artifact was changed.

Finance Vue checkpoint (2026-08-24): Finance now has durable Vue routes for Ledgers, chart accounts, journal entries and reconciliations over the generated cell contracts. It preserves package read-only mode and participant/manager authority while covering paged inspection, Ledger governance, chart-of-accounts management, arbitrary balanced journal lines, draft revision, posting, reversal, evidence-bound statement comparison and zero-difference confirmation. Commands bind current versions and preserve one retry identity; destructive accounting transitions require typed confirmation and conflicts reload current state. Component and API-client coverage plus local production build/routing gates exercise the durable and read-only paths. Realistic accounting fixture, browser/device/accessibility and product-domain acceptance remain open; Stage and production were not changed.

Integrations Vue checkpoint (2026-08-24): Integrations now has durable connection, external-execution and Google-authorization Vue routes plus a mobile Research workspace over the complete generated customer boundary. Account members can inspect immutable non-secret scope, credential generation, paged health and delivery attempts; package read-only mode removes every mutation while retaining scoped search. Owners and administrators manage connector revisions, opaque broker attestations, lifecycle and passkey-gated Google consent/revocation. Preparation freezes only channel-compatible active connections, and uncertain effects retain different-manager confirmation without resending. Scoped web results require explicit typed confirmation before admission as internal Knowledge evidence. Exact failed retries retain one operation identity, versioned commands bind current ETags and conflicts reload authoritative state. Component/API-client tests and local production-build/TLS-route gates cover principal durable, read-only, dual-control and capture paths. Applied Google/provider journeys, realistic fixtures, browser/device/accessibility and product-domain acceptance remain open; Stage and production were not changed.

UI7 consolidation checkpoint (2026-08-25): rendered local browser evidence now covers the landing, feature index, pricing, privacy and Affiliate-terms routes across the required phone widths plus tablet and desktop. Thirty route/width checks found one main heading per route, no horizontal page overflow and no sub-44px rendered buttons. The private mobile drawer's off-screen focus defect is fixed with initial focus, focus containment, Escape/close restoration and route-level main focus, backed by a component test. Production builds now fail when complete private/public gzip asset budgets are exceeded; the current private SPA is 107.5 KiB JavaScript and 10.0 KiB CSS, while all public client assets are 86.9 KiB JavaScript and 4.3 KiB CSS. The rebuilt private UI and local HTTPS Docker smoke gate pass. Authenticated private-route browser evidence, zoom/forced-colors, failure injection, real devices and external-provider journeys remain open, so this is not the UI7 exit or a release authorization.

UI7 recovery/accessibility hardening checkpoint (2026-08-25): one private-SPA modal manager now provides initial focus, Tab/Shift+Tab containment and opener-focus restoration for every rendered modal contract. Your Turn component evidence covers failed initial load and retry, offline stale-state preservation, reconnect refresh and authoritative conflict reload; approval conflicts now invalidate the prior exact-payload confirmation before another submission is possible. Any API `401` triggers one application-wide session-expiry return to login with the current route preserved. The consent-center UI now has explicit tests for equal non-essential rejection and confirmed browser-subject erasure. Public feature tests lock all twelve reviewed feature routes, while the local TLS smoke certificate verifies server-rendered metadata, headings and workflow/governance copy for each route without JavaScript. The complete UI verification, gzip budgets, rebuilt private image and expanded local Docker gate pass. Authenticated rendered route/device checks and external-provider journeys remain open; Stage and production remain untouched.

UI7 contrast/reflow checkpoint (2026-08-25): shared design tokens now provide explicit Windows/high-contrast system-color behavior for text, links, surfaces, focus, borders, disabled controls and logo identity under `forced-colors: active`. The rebuilt landing and signup surfaces pass the 320 CSS-pixel reflow check, equivalent to 400% zoom from a 1280 CSS-pixel viewport, with one main heading, no horizontal page overflow and no rendered control below 44 pixels. The public runtime exposes the intended forced-color rule. The full UI verification and expanded local Docker smoke certificate remain green. Authenticated-route reflow and applied system-color rendering remain open until the local test identity can be used; no release environment changed.

UI7 acquisition/privacy hardening checkpoint (2026-08-25): checkout now treats consent lookup as optional, never substitutes a different paid offer after signup intent becomes unavailable, retains one idempotency identity across provider retry and rejects non-HTTPS hosted destinations. Its return state waits for the signed local projection and separately renders pending, active/trialing, payment-attention and failed terminal states. Public acquisition routes share one effective consent projection so successful withdrawal immediately disables later CTA measurement, stale concurrent consent reads cannot reactivate it, and policy renewal reopens the equal-choice panel. Both the Nuxt and server-rendered onboarding controls preserve and accurately describe the previous choice after a failed update. Suspended Affiliate enrollment disables code copying while retaining identity-safe aggregate history. The private/public/API suites pass 56/4/39 tests; the focused Go analytics/privacy/Affiliate/commercial/browser packages, rebuilt local images and Docker smoke certificate pass. The rendered signup page loads the cache-busted consent asset and exposes equal accept/reject/manage controls. Provider-backed and authenticated rendered certification remain open; no release environment changed.

UI7 public-discovery checkpoint (2026-08-25): all seventeen public landing, feature, pricing and policy routes now emit production canonicals under `https://www.infiniteocean.net`, explicit crawl directives, complete Open Graph/Twitter large-card metadata and route-specific schema.org JSON-LD. The generated sitemap matches that exact route inventory and `robots.txt` points crawlers to it. A checked-in 1200-by-630 PNG social preview has an editable SVG source and a pinned Linux render command that rejects missing-font or wrong-size output. Unit coverage locks canonical/social/schema behavior and safe JSON-LD serialization; the expanded certificate-verified local Docker gate checks every sitemap location, the social image type/signature/size and every rendered relative public link. The complete frontend gate passes 56 private, 6 public and 39 API tests with current private/public JavaScript budgets at 107.5/87.7 KiB gzip and CSS at 10.0/4.3 KiB. Authenticated private browser certification and provider-backed journeys remain open; Stage and production remain untouched.

UI7 Catalog-resilience checkpoint (2026-08-25): the public Nuxt server now retains only a validated, provider-field-free, monotonically versioned Catalog publication and bounds its upstream request to two seconds. A transient failure can serve that exact publication for at most five minutes with explicit stale age/cache headers and visible pricing/feature copy; rollback or same-version mutation is treated as failure and cannot replace it. Shared state preserves that warning across SPA navigation, and a failed refresh hides retained paid offers. With no recent snapshot, `/catalog.json` returns `503`, `no-store` and `Retry-After`, while pricing preserves free signup and hides unverified paid offers. The dedicated local recovery certificate primes fresh state, pauses only the local Account API, proves byte-identical stale and rendered-warning behavior, restarts the stateless website to prove empty-cache failure, then restores the API and requires fresh recovery. Both services return healthy afterward. The complete frontend gate passes 56 private, 12 public and 39 API tests; public JavaScript is 86.4 KiB gzip and CSS is 4.4 KiB. Authenticated browser and provider-backed Checkout work remains open; no release environment changed.

GDPR fulfillment checkpoint (2026-08-25): verified privacy-rights requests now carry an internal optimistic version and have a short-lived `privacy-rights-admin` operator mode for audited inspection, review start and terminal completion, partial completion or decline. Every operator action requires the standard phishing-resistant authorization envelope and exact environment confirmation. Resolution is bound to an opaque evidence UUID and SHA-256 rather than accepting a case artifact or customer content. Security-definer PostgreSQL functions serialize transitions and preserve immutable events; the declared `global.privacy-rights-operator` role has execute-only authority and no direct request-table access. Compatibility triggers preserve submit/cancel behavior while old and new Account API binaries overlap. Fresh PostgreSQL tests prove exact-version conflicts, evidence persistence, event immutability, execute-only authority and rolling writes. The full local Docker test gate, race pass, Vue gate and dependency audit pass. At this checkpoint, Affiliate refund/dispute projection, enrollment lifecycle, support/appeal and commercial/legal/vendor/transfer decisions remained open; Stage and production were not contacted.

Affiliate operations checkpoint (2026-08-25): the verified Stripe billing inbox now projects PaymentIntent-bound qualifying earnings, succeeded Refund evidence, and final lost-dispute evidence into the immutable Affiliate ledger. Semantic Refund deduplication, aggregate full-amount refund qualification, no refund/dispute double-counting, exact-once reversals and adverse-before-earning reconciliation pass fresh PostgreSQL tests without retaining customer or payment-method data. A short-lived `affiliate-admin` mode records inspections and exact-version suspension/reactivation or terminal closure through execute-only security-definer functions. Initial enrollment and every operator action have immutable lifecycle evidence; suspension/closure stops new public-code lookup while locked attribution and historical statements remain intact. The private Vue dashboard now submits content-free enrollment appeals and commission-entry reviews, lists their lifecycle and permits cancellation before review. A distinct `affiliate-support-admin` mode records inspection, exact-version review and approved/denied resolution through execute-only functions without automatically changing enrollment or ledger state. The standard phishing-resistant operator envelope and exact environment confirmation bind every staff action. Feature flags remain closed. Settlement/commercial approval, final legal/vendor/transfer review, provider journey certification and authenticated rendered certification remain open; Stage and production were not contacted.

Analytics reporting checkpoint (2026-08-25): the consented landing, checkout and onboarding event store now has a one-shot aggregate JSON report with a dedicated execute-only database role. The report emits UTC bucket, reviewed event/surface/dimension, count and distinct-subject count only; limits daily/hourly windows; rejects arbitrary dimensions; and suppresses every group below five subjects. Fresh PostgreSQL proves aggregation, suppression, no PUBLIC function access, reporter execution and denial of raw-event reads. Local Docker exposes the same path through `make analytics-report`. The report deliberately does not claim same-subject conversion or replace commercial Affiliate attribution. A short-lived public-to-private conversion receipt is still required or must be explicitly removed from the approved architecture before UI7 can exit. No Stage, GHCR or LKE system was contacted.

Analytics conversion-continuity checkpoint (2026-08-25): a consented public signup handoff now creates a signed, maximum-24-hour, parent-domain receipt that lets reviewed private onboarding/checkout milestones appear on the aggregate report's `conversion` surface. The receipt contains only the anonymous public subject, source event and expiry. Mirrored persistence contains no private consent subject, User, Account or private event ID, preventing the mechanism from becoming a durable cross-site identity profile. One source records each milestone once; the source consent/event foreign key governs cascade erasure; and private browser erasure also removes the source while the receipt remains available. Parent-domain validation, HMAC domain separation, cookie flags/path, refusal behavior, same-source aggregation, five-subject suppression and erasure pass focused unit and fresh-PostgreSQL tests. The host-only preference cookie remains unchanged, and no release environment was contacted.

Public rendered-accessibility checkpoint (2026-08-25): the Nuxt acquisition site's Docker gate now boots the exact production Nitro build and examines every one of its seventeen rendered routes. It fails on missing language/title metadata, duplicate or missing header/main/footer/level-one-heading structure, a misplaced or broken skip link, a non-focusable main target, duplicate IDs or any axe-core WCAG 2.0/2.1/2.2 A/AA violation that a layout-free DOM can evaluate. `color-contrast` remains explicitly assigned to the real-browser matrix because jsdom cannot compute rendered pixel colors. The complete UI gate passes 63 private, 19 public and 41 API tests, type checking, lint, both production builds and gzip budgets; npm's high-severity audit reports zero vulnerabilities. This closes repeatable exact-build structural evidence for the public surface only. Browser-computed layout/contrast, real devices, assistive technologies and authenticated private journeys remain open, and no release environment was contacted.

Vue template-semantics checkpoint (2026-08-25): the recommended Vue accessibility rules are now part of the mandatory Docker lint gate for both the Nuxt acquisition site and private SPA. Native wrapping labels and explicit-ID labels are both accepted. Two narrowly documented suppressions cover key handlers whose containers acquire dialog semantics only in mobile-open state; no global interaction rule was disabled. Dialog labels are now conditional with their roles, summary/filter clusters expose group semantics, and the Your Turn skeleton exposes one status while hiding decorative cards. Component assertions prevent those roles and conditional labels from regressing. All 123 package tests, lint, builds, rendered public accessibility checks, budgets and the zero-vulnerability audit pass. Browser/device/assistive-technology certification remains open, and no release environment was contacted.

Private rendered-state accessibility checkpoint (2026-08-25): the private Vue suite now applies the layout-independent axe-core WCAG 2.0/2.1/2.2 A/AA rules through one shared verifier. Thirty-nine fixture scans cover the application shell and every one of the eighteen private view families, including alternate routes, roles, package modes, Affiliate/program states and workflow states reached through existing mount helpers. Detached wrappers are cloned into the active document and always removed; checkout's fake-timer cases retain one stable pre-timer scan so axe cannot contaminate timer isolation. The only excluded rule is pixel `color-contrast`, which happy-dom cannot compute and the real-browser matrix still requires. All 63 private tests pass with this gate active. No identity was created, no credential was used and no deployment environment was contacted.

Launch-critical private-browser checkpoint (2026-08-25): `website-test` now depends on a digest-pinned Playwright stage that serves the exact built Vue artifact and fails on any unhandled synthetic API call, browser runtime error, responsive overflow or axe WCAG 2.0/2.1/2.2 A/AA violation. Chromium, Firefox and WebKit run at desktop 1280×900, with Chromium phone emulation at 390×844. The suite covers Your Turn, Checkout referral review/application, GDPR preferences and rights framing, the fail-closed Affiliate launch state, and mobile drawer focus containment/Escape restoration. Browser-computed color contrast is active; it identified and drove correction of a genuine 4.3:1 Checkout package-state label. Seventeen applicable checks pass, alongside all 123 package tests, seventeen built public routes, lint, type checking, builds, budgets and a zero-vulnerability audit. The session and API data are content-free fixtures: no identity, password, cookie, database, Stripe account, Stage or production environment is touched. Full private-route/state coverage, authenticated provider journeys, mobile device engines and assistive-technology/device certification remain open.

Public acquisition, security-onboarding, Account-administration and operating-context browser checkpoint (2026-08-25): the digest-pinned Playwright stage now boots the exact built Nuxt/Nitro acquisition artifact as well as the private Vue artifact. A content-free Catalog fixture and synthetic same-origin consent/event boundary exercise all seventeen landing, feature, pricing, privacy and Affiliate-terms routes in Chromium, Firefox and WebKit desktop; Chromium phone emulation at 360×800, 390×844 and 412×915; Chromium reflow at 320×800; a touch-enabled Chromium tablet at 768×1024; and Chromium forced-colors and reduced-motion emulation at 1280×900 with an asserted motion media state; a separate Chromium profile enlarges text to 200% at 1280×900 across seventeen primary private and seventeen public routes. Analytics remains silent without a decision and after rejection; accepted landing navigation emits the reviewed CTA and signup-handoff events without changing the destination; pricing transfers only the opaque offer code regardless of analytics; every public route passes browser-computed axe including contrast and horizontal-overflow checks; and the phone menu contains and restores focus. Private GDPR controls now prove tracked Affiliate-scoped erasure submission, open-request deduplication, cancellation and an exact `/app/privacy` return target when passkey reauthentication is required; the ceremony itself remains an external acceptance item. The private Security route additionally proves its completion event is absent in incomplete state, appears only after the recovery-code mutation reloads authoritative owner-ready state, remains distinct from first application entry, and keeps the newly issued one-time codes visible. Account, Billing, exports and lifecycle prove owner authority, paid-account state, portability availability and closure consequences remain understandable. Multi-Account selection adopts only the server-confirmed context, while a failed selection restores the prior Account and reports that no authority changed. Durable Work, governed Knowledge, resumable Business Baseline, Agents/Boardrooms/conversations, Schedules, Finance, Integration connections/executions and Marketing campaigns/releases preserve list/detail operating context across all ten standard profiles and the primary route set additionally reflows under 200% text. Read-only package fixtures retain governed evidence while removing mutations; absent Integrations and Marketing packages expose explicit upgrade paths; and a 503 Account-load failure retains a usable, accessible sign-in recovery path. Your Turn retains the last queue while offline, refreshes authoritatively on reconnect, preserves its exact route through session-expiry sign-in and sends only one mutation when duplicate submit events arrive during a pending decision; capacity denial retains the customer draft, stale Work, Marketing, Membership and Schedule mutations reload current authority before a visible retry instruction, Stripe failure leaves payment and Account state unchanged, and a scoped Integration-provider failure retains the exact customer query and reviewed connection. The checkpoint found a 2.82:1 landing-step contrast defect; four separate 4.3:1 Checkout, member-email, Agent-confidence and Marketing-release contrast defects; a narrow-screen governed-identifier overflow; an unreachable scrollable Baseline progress track; a persistent desktop navigation CTA that bypassed acquisition analytics, and fixed-count feature, Privacy, Account, action, master/detail and Security grids that overflowed under text enlargement. All are corrected. The combined exact-artifact job passes 291 applicable checks with eleven intentional skips on profiles where compact navigation is not rendered. No identity, credential, database, provider, Stage or production environment is used. Identity entry ceremonies, remaining package-domain mutation conflicts, capacity denials and downstream failures, physical passkey ceremonies, 200% text-only zoom on connected identity pages, 400% browser zoom/reflow, real devices and assistive technologies remain open.

Connected-local identity browser checkpoint (2026-08-25): a separate test-profile container shares the rebuilt local Caddy network namespace and exercises the exact Go-rendered login, signup, password recovery/reset, registration-verification and verified-contact pages at `https://app.infiniteocean.localhost:8444`. Twelve checks pass in Chromium, Firefox and WebKit desktop plus Chromium phone. They prove skip-link focus, responsive overflow, browser axe including contrast, equal/reversible analytics rejection, Catalog-offer continuity, native validation and safe incomplete-link recovery without creating a User or submitting a password. The first run found that the optional-consent card covered signup's primary action; desktop placement now stays over the story pane and the card enters normal flow at single-column widths. Successful email/password/passkey and owner-enrollment ceremonies, deeper denial/recovery states, zoom/forced-colors, real devices and assistive technologies remain open.

Package recovery and connected-identity text checkpoint (2026-08-25): the exact built Vue application rejects stale Knowledge decisions, Agent Persona publication, Finance posting and Integration disable commands, reloads the authoritative record before presenting a retry, and retains only local intent that remains safe to reconsider. Agent concurrent-run capacity denial now preserves the exact Boardroom subject, question and Persona selection; simulated downstream failures preserve Knowledge decision reason, Baseline unknown rationale, Finance posting confirmation, Marketing campaign revision, Integration command and Schedule operational reason across all ten standard profiles. The exercise exposed and corrected an invalid Unicode-set Persona pattern, transparent non-`decision-card` modal surfaces that failed contrast after an error, and Schedule errors rendered behind their open retry dialog. The combined job records 311 applicable passes with eleven intentional profile skips. The separate connected-local identity job records thirteen passes, including 200% text across all six identity routes. Remaining local certification work is 400% browser zoom/reflow, real devices and assistive technologies; implemented package conflicts, capacity/downstream recovery and connected identity text enlargement are no longer open. No identity, credential, provider, database or deployed environment was used.

Affiliate/GDPR adversarial-journey checkpoint (2026-08-25): the exact built Vue application now proves the enabled Affiliate state as well as the launch-closed state. Across all ten standard profiles, an active enrollment exposes its generated code, recurring-cycle aggregate and pending/settled/reversed totals without a referred customer's name, email or business identity; a suspended reload disables code copying and new attribution while preserving the immutable ledger and structured status review. Checkout separately submits the deliberate Affiliate code with optional analytics denied, surfaces the backend self-referral denial, retains the exact code and confirmation for a safe retry, and emits no optional analytics event. The Go projector suite now explicitly covers final lost-dispute reversal in addition to succeeded Refund reversal, while the connected PostgreSQL lifecycle already proves invoice replay identity, cumulative Refund qualification, adverse-event replay, adverse-before-earning reconciliation, suspension denial, immutable evidence and customer-account detachment without ledger loss. All Go packages pass. The exact-artifact UI job records 331 applicable browser passes and eleven intentional profile skips, 66 private component tests, 19 public tests, 41 API tests, all seventeen rendered public accessibility routes, passing budgets and zero npm vulnerabilities. Feature flags and settlement remain closed; provider-backed Stripe, real-device and assistive-technology acceptance remain open. No identity, credential, provider, database or deployed environment was used for the new browser evidence.

Your Turn safe-navigation checkpoint (2026-08-25): a shared Vue guard now protects the primary consequential-decision form from accidental loss. While a mutation is pending, route changes are denied and the screen announces that the decision must finish; an unsaved decision uses the browser's native confirmation, preserves the exact draft when canceled and permits the requested route only when confirmed. Browser unload is protected for either state, while the screen's own authoritative success transition has a one-use bypass. The freshly built Vue/Nuxt artifact passes 341 browser checks with eleven intentional profile skips across the ten standard profiles, plus 66 private, 19 public and 41 API tests, type checking, lint, both production builds, all seventeen rendered public accessibility routes, gzip budgets and a zero-vulnerability audit. This closes safe navigation only for Your Turn; equivalent review for the other feature editors, actual 400% browser zoom/reflow, real devices, assistive technologies and provider-backed journeys remain open. No release environment was changed.

Core-workspace safe-navigation checkpoint (2026-08-25): Work creation, Knowledge claim review, Business Baseline answers/reviews and Agent Boardroom/Persona editing now join Your Turn on the shared route and browser-unload boundary. The Work capacity journey holds an unresolved create request, proves that navigation is denied without duplicating the mutation, then proves the failed draft survives both server denial and canceled leave confirmation. Existing all-profile Knowledge conflict, Baseline downstream and Agent capacity paths similarly retain exact customer input after canceled leave and proceed after confirmation. The Persona child reports dirty immutable-policy state to its parent; successful internal redirects bypass the guard once, while completed Baseline evidence/source forms clear their submitted local values. The browser count remains 341 because these are strengthened existing journeys. Remaining private mutation surfaces and actual 400% browser zoom/reflow are still open; Stage, GHCR and LKE remain untouched.

Package-editor safe-navigation checkpoint (2026-08-25): Finance, Marketing, Integrations and Schedules now use the same protected-state boundary for open commands/editors and unresolved mutations; unsent Integration research terms are protected independently of optional analytics. The all-profile downstream-failure journey now dismisses native Back confirmation, verifies that exact retry input remains, then accepts the confirmation and leaves; the scoped-research failure journey separately verifies the custom SPA-route confirmation and visible retained-query status. Browser-native unload copy is treated as user-agent content, while the stable `beforeunload` contract is asserted. Focused type checking, all eleven affected component tests and the twenty affected exact-artifact browser checks pass. The overall matrix count remains 341 because existing journeys gained assertions. Account/Billing/Checkout, Security, Privacy, exports and lifecycle still require safe-navigation review; Stage, GHCR and LKE remain untouched.

Customer/account safe-navigation checkpoint (2026-08-25): Account invitations/team commands, Billing portal preparation, Checkout choices/referrals/confirmation, Security input/revocations/new one-time recovery codes, GDPR consent/browser erasure and rights requests, Account exports/lifecycle and Affiliate enrollment/support now complete the shared protected-state audit. The one-time recovery-code screen warns on every leave because those values cannot be shown again. The exact-artifact browser matrix adds assertions to five existing journeys across all ten standard profiles: canceled navigation retains the exact state, confirmed navigation proceeds, and unresolved Affiliate support blocks routing without a second mutation. All fifty focused browser checks, twenty-nine affected component checks and type checking pass; a source audit finds no form-bearing private view outside the guard. Cross-feature safe navigation is complete locally. Actual 400% browser zoom/reflow, real devices/assistive technology and provider-backed checkout/security remain open; Stage, GHCR and LKE remain untouched.

Automated 400% browser-scale checkpoint (2026-08-25): a dedicated Chromium project now asserts the browser-emulated 400% metrics—1280×900 device surface, 320×225 CSS viewport and DPR 4—before traversing all seventeen primary private and seventeen public routes. Both inventory checks require level-one content, no horizontal page overflow and browser-computed axe WCAG A/AA results; public acquisition remains silent before consent. Both pass, bringing the exact-artifact matrix to 343 applicable passes and eleven intentional compact-menu skips. Automated 400% zoom/reflow is closed; physical iOS/Android, assistive technology and provider-backed checkout/security remain open. No deployed environment or provider was contacted.

Local Stripe-input checkpoint (2026-08-25): the UbuntuRojo Compose entry point now accepts an optional, explicitly named provider override after the deterministic checked-in local environment. Its validator requires a regular non-symlink mode-600 file on the native Linux filesystem, Stripe test mode, a test secret key and the endpoint-specific local-listener webhook secret; it rejects placeholders, live keys, unrelated values and `/mnt` paths without printing credentials. The default stack still uses deliberate no-provider placeholders. No secret was copied from Hostinger and no deployed environment was changed. Provider-backed Checkout remains blocked until a local listener issues its own webhook secret, the reviewed Stripe test Prices exist, the local Catalog has governed mappings (currently zero), and the external operator-authorization issuer approves that Catalog sequence.

Affiliate/GDPR completion-audit checkpoint (2026-08-25): active Affiliates now receive a labeled referral link containing only the public code and copy that accurately states the customer must still apply it at checkout; suspension removes the URL and disables both copy actions without hiding historical ledger/support state. Confirmed browser-subject erasure is now one pending consequential request: duplicate activation is inert and route changes are denied until the server confirms deletion. The focused component gate passes 67 private, 19 public and 41 API checks. Ten new exact-artifact browser assertions cover erasure in every standard profile while the existing Affiliate journey covers the link in every profile; browser-computed axe and overflow remain green. The matrix is now 353 applicable passes and eleven intentional skips. Private JavaScript/CSS remain within budget at 112.3/10.3 KiB gzip; npm reports zero vulnerabilities.

Affiliate public-code replacement checkpoint (2026-08-25): the missing replaceable-code promise is now implemented end to end. `POST /api/v1/affiliate/code-replacements` requires the authenticated Affiliate, recent passkey confirmation and the exact enrollment version; migration 49 keeps every current and retired code in immutable no-reuse history. Fresh PostgreSQL 17 evidence proves pre-lock invalidation, post-lock attribution/ledger preservation, permanent retired-code collision denial and history update/delete denial. The active Vue Affiliate surface adds a deliberate two-step warning, blocks navigation while replacement is pending, and hands weak authentication to Security with the exact return route. The generated contract now has 208 operations. The exact-artifact gate remains 353 applicable browser passes plus eleven intentional skips and now passes 68 private, 19 public and 42 API component checks, type checking, lint, both builds, budgets, rendered public accessibility and a zero-vulnerability audit. No provider or deployed environment was contacted.

Affiliate statement-completeness checkpoint (2026-08-25): the statement API now derives an authoritative aggregate from locked subscription attributions, and the Vue dashboard exposes that referred-subscription count without customer identity. Immutable commission entries are grouped into UTC calendar-month statements with earned and reversed subtotals while preserving overall pending, settled and reversed server totals and entry-bound support review. Service, generated-schema, component, all-profile exact-artifact and fresh PostgreSQL evidence cover the consistent count and multi-month presentation. The gate remains 353 applicable browser passes plus eleven intentional skips and 68/19/42 private/public/API checks; private/public JavaScript and CSS are 112.9/10.3/88.2/4.5 KiB gzip with zero npm vulnerabilities. This is local UI7 construction only; Affiliate flags, economics and settlement approval remain closed.

Affiliate self-service portability checkpoint (2026-08-26): `GET /api/v1/affiliate/data-export` now requires a recent passkey confirmation and returns a no-store, identity-owned JSON artifact through a dedicated execute-only PostgreSQL projection. It includes enrollment, immutable public-code history, sanitized lifecycle, aggregate attribution counts, a provider-free commission ledger, and sanitized support history; referred Account, checkout, subscription, payment-provider, staff-actor, and free-form reason fields do not exist in the artifact schema. The mobile-first Privacy route downloads it without persistent browser storage and preserves the exact return path through passkey reauthentication. Fresh PostgreSQL 17 proves enrolled and empty exports, least-privilege function access, and prohibited-value absence. The generated contract now has 209 operations; clean UbuntuRojo Docker passes all workspace type checks, 71 private, 22 public and 42 API tests, lint, and the private production build. No provider or deployed environment was contacted; correction, restriction, objection, closure, erasure, Affiliate economics, and settlement remain governed separately.

Affiliate portability browser checkpoint (2026-08-26): the exact built private and public artifacts now prove the direct Affiliate download and the exact passkey-confirmation return path in every applicable standard profile. The privacy-safe synthetic artifact carries `no-store` and attachment disposition, produces a date-stamped local download, and contains no referred-customer or provider identity. The complete UbuntuRojo Docker matrix passes 393 applicable checks with eleven intentional compact-navigation skips across Chromium, Firefox, WebKit, three phone widths, 320-pixel reflow, tablet, forced colors, reduced motion, 200% text enlargement and 400% browser-scale reflow. No provider, deployed environment, Kubernetes configuration or remote infrastructure was contacted.

Consent-purpose minimization checkpoint (2026-08-26): the launch registry has no Marketing tracking purpose, processor, event, cookie or destination, so the public, private and server-rendered preference surfaces no longer solicit a meaningless Marketing consent. They expose the actual optional first-party analytics choice and explain that Marketing tracking is not used. The compatibility field is fixed to `false` in the OpenAPI contract, and the application service rejects `true` before a receipt or cookie can be created. Historical decisions remain immutable and inspectable. The complete local UbuntuRojo gate passes 393 applicable browser checks with eleven intentional compact-navigation skips, 71 private Vue tests, 22 public Nuxt tests, 42 API-package tests, all seventeen rendered public accessibility routes, production builds and bundle budgets, plus a zero-vulnerability dependency audit. This is a local GDPR data-minimization and consent-specificity correction; it does not affect the separate customer Marketing package or Affiliate attribution.

Privacy-rights deadline checkpoint (2026-08-26): month-end requests now receive the last valid day of the following month instead of Go's normalized date in the month after it; January 31 is therefore due February 28 or 29. Global migration 54 adds an execute-only `list-open` operator boundary so staff can discover the bounded open deadline queue without direct table access or receiving User, email, Account, Affiliate or customer-content fields. Migration 55 repairs any preexisting overstated response date and makes the UTC-clamped calendar month a database invariant as well as an application invariant. The explicit cutoff and 1–100 result limit are authorization-bound, every listing appends immutable access evidence, and exact request inspection remains a separate audited step. Focused domain, service, repository, command and race tests plus the complete fresh-PostgreSQL suite across all 135 migrations pass locally. This closes deadline computation, storage enforcement and queue discovery; approved staff fulfillment procedures and legal retention decisions remain release gates.

Affiliate acquisition-continuity checkpoint (2026-08-25): neither identity branch now loses the public referral proposal. A new customer retains the complete relative Checkout target through signup, encrypted verification delivery, password setup and sign-in; a returning customer retains it through forgot-password entry, anti-enumeration recovery, encrypted reset delivery, password replacement and sign-in. Both work across the emailed link, and unsafe external targets are discarded at every browser boundary. Checkout still requires deliberate referral application and confirmation, so continuity does not create attribution. Consent-denied browser coverage remains silent, while `application_entered` now records the reviewed `checkout`, `your_turn` or `deep_link` entry category and the server rejects any other value. Focused Go tests cover both services, encrypted queue, SMTP URLs and browser handoffs. The exact-artifact gate passes 363 browser checks plus eleven intentional skips and 69/19/42 private/public/API tests; type checking, lint, both production builds, all seventeen rendered public accessibility routes, 113.0/10.3/88.2/4.5 KiB private/public JavaScript/CSS budgets and the zero-vulnerability audit pass. No provider or deployed environment was contacted.

Analytics-registry completion checkpoint (2026-08-25): the consent-bound ingestion registry now enforces each event's reviewed public/private surface and the complete categorical vocabulary for device, security enrollment, referral, Checkout outcome, application entry and Your Turn measurement. Valid consent on the wrong surface is insufficient, and plausible unreviewed categories fail closed instead of creating uncontrolled report dimensions. The launch processing registry records the exact values. Focused analytics, ingestion, conversion and HTTP tests plus vet and formatting checks pass; no frontend artifact, provider or deployed environment changed.

Connected identity success checkpoint (2026-08-25): the exact local Caddy/PostgreSQL/notification/Vue composition now completes registration, encrypted TLS Mailpit verification, password creation, native login into Your Turn, anti-enumerating recovery, encrypted reset delivery, old-password rejection and replacement-password login in Chromium, Firefox, WebKit and the Chromium phone profile. The 200% text profile and a dedicated Chromium virtual-authenticator profile bring the connected job to eighteen passes. The latter completes first-passkey enrollment, recovery-code creation, authoritative owner readiness, discoverable passkey login, simulated loss, password-plus-code replacement while the inaccessible credential remains recorded, and replacement-key reauthentication. This work corrected three release-critical defects: opaque native form origins under `no-referrer`, passkey redirects reading a cleared asynchronous `event.currentTarget`, and valid recovery grants being ignored whenever any credential row remained. `same-origin` preserves verifiable mutations without off-origin referrers; passkey handlers capture navigation state before awaiting; recovery grants now authorize the documented retained-credential loss path. Unique synthetic Accounts prevent slug collision, consumed messages are deleted, traces are disabled and the canonical local job resets only its anonymous actor test budgets. Physical passkeys, real devices and assistive technology remain open; no deployed environment was contacted.

Mobile consent prominence checkpoint (2026-08-25): a rendered phone audit exposed two launch defects that structural accessibility checks did not: the public consent sheet obscured the landing's acquisition actions, and the server-rendered identity panel pushed the onboarding form below the first viewport. Identity entry also styled optional acceptance as the prominent green action while optional rejection was secondary, contrary to the equal-choice GDPR contract. Compact dynamic-viewport-bounded panels, shorter content-free copy and identical accept/reject controls now preserve both landing actions above consent at every compact profile and keep the form heading visible at 390 by 844. The exact UI artifact passes 363 checks plus eleven intentional compact-menu skips; the connected local identity artifact passes all eighteen checks. Docker later became unreachable while producing a screenshot, so the captured fail-closed consent warning is not treated as proof of an application response. Once the UbuntuRojo bridge recovered, independent certificate-routed GETs to the public and private consent endpoints both returned `200` and the expected undecided, optional-purposes-off projections. This local correction does not close physical-device, assistive-technology or provider-backed acceptance.

Consent-lifecycle visibility checkpoint (2026-08-26): the consent-history API was already typed and backed by immutable receipts, but neither active customer surface made it usable. Public Privacy now shows this browser's public-site choices, links there from preference management and provides a two-step erasure that returns optional tracking to an undecided fail-closed state without claiming to erase identity, Account, billing or Affiliate records. Private Privacy shows the corresponding private-host choices alongside current preferences and rights-request history. Neither surface renders receipt or subject UUIDs. The history transport now resolves the current decision and applies private identity ownership replacement before reading history; a stale reference owned by another User therefore produces a new same-choice subject for the current User instead of exposing the prior subject's decisions. All Go tests/vet/contracts and the exact UI artifact pass: 373 browser checks plus eleven intentional skips, 70/22/42 private/public/API tests, both builds, accessibility, budgets and zero vulnerabilities. No deployed or provider environment was contacted.

## Phase 3 scope

Phase 3 consolidates the historical Phase 3-8 backlog into one final body of work. It is not limited to Work and Attention. It includes:

1. Work and Attention.
2. Knowledge and Baseline.
3. Agent workspace and durable execution.
4. Scheduling, Finance, Marketing and integrations.
5. HTTP, MCP and the complete Vue customer surface: public acquisition, feature/package education, GDPR-capable analytics, Affiliate attribution/commission, pricing/checkout and the private mobile-first SPA.
6. Prototype data/behavior migration and retirement.
7. Production hardening, Linode deployment, canary and release.

No incomplete package is hidden to justify production launch. Feature flags and unpublished Catalog entries remain useful during development, but the production release gate requires the full intended application.

## Starting position after Phase 2.5

The repository already contains strong foundations:

- typed Work lifecycle, forced-RLS persistence, routed HTTP commands/queries and a private Work surface;
- Boardrooms, immutable Persona versions, Conversations, Agent Runs, dispatch/projection queues and encrypted runner exchange;
- package admission, action authorization/ledger foundations and content-safe observability;
- generated OpenAPI contracts and compatibility enforcement.
- global passkey incident response and restore-gated bounded identity retention with content-free operational metrics;
- two independently signed, attested and vulnerability-admitted application/website pairs (active RC.5 platform baseline and source-compatible RC.4 rollback), a live repeatable Hostinger deployment with isolated per-cell Docker runner paths and provider egress, and CI-rendered two-cell Linode overlays with a dedicated workload-mTLS tool router;
- a successful RC.5 -> RC.4 -> RC.5 connected-stage rollback, two 11-check anonymous boundary certificates, interruption-free single-replica recovery, and live Stripe sandbox/webhook, non-production OpenAI and SMTP TLS readiness.

The foundations do not yet constitute the full product. In particular:

- the Work-to-Agent claim/heartbeat/reconcile bridge is absent even though the lower-level Agent dispatch queue/worker exists;
- the Attention domain is absent;
- Knowledge/Baseline production modules are absent;
- Agent orchestration is limited to one durable sequential Boardroom pass; manager synthesis, delegation and resumable owner questions remain;
- production MCP is constructed and deployed on Stage; its signed-in external-client acceptance sequence remains open;
- Finance, Marketing and most integrations remain prototype-only or unimplemented;
- the private application is not yet the final Vue SPA product surface;
- no complete prototype migration/cutover has occurred.

The Hostinger Phase 2.5 entry gate is complete. Stage is intentionally pristine: there are no users, Accounts, provider-price mappings, billing profiles or subscriptions. This prevents temporary Catalog/customer state from being mistaken for final-product release evidence. Phase 3 must publish reviewed stage mappings through the signed `catalog-admin` boundary and then run real email/passkey/Stripe journeys against the product capabilities being accepted. The LKE kubeconfig is first needed in P3.7: its initial use is a read-only cluster inventory, followed by explicit replacement of every fail-closed storage/CNI/API/add-on/secret/runtime placeholder before any apply.

## P3.0 — establish the Phase 3 acceptance baseline

Before feature construction fans out:

1. Treat RC.5 as the platform baseline, not as the final product release candidate.
2. Define the final launch Catalog and obtain exact-scope operator authorization for its stage Stripe price mappings; do not mutate protected Catalog tables directly.
3. Create revocable synthetic stage identities and mailboxes, with no customer data, for repeatable email, WebAuthn/passkey, Membership, billing and recovery journeys.
4. Use the revision-controlled [product-journey certification driver](journey-certification.md), which records content-free results while keeping session material, WebAuthn keys, provider payloads and identifiers outside Git.
5. Run the relevant customer journey after each package/surface becomes acceptance-ready, then rerun the complete matrix against the final immutable application/website pair.

Exit: Phase 3 has safe fixtures and evidence formats without treating unfinished product behavior as a release gate.

## P3.1 — Work and Attention

### Finish Work

- Add Persona foreign keys and Persona assignment policy.
- Add assignment editing, provenance attachment and conversation-link commands.
- Add representative query-plan, pagination property and concurrency stress tests.
- Preserve drafts and provide accessible reason/command interactions.
- Characterize and migrate prototype Work data with capacity reconciliation.

Implementation checkpoint (2026-08-21): the composite Account/Persona foreign key, active published Persona assignment policy, routed Persona assignment input, additive provenance and Conversation-link commands, Account-scoped Conversation/Run foreign keys, typed redacted mutation events, generated client contracts, erasure-safe deferred integrity behavior, representative query-plan fixtures, tie-heavy pagination properties, concurrent completion/assignment stress, assignment editing, explicit reason dialogs, tab-scoped create drafts and accessible command announcements are complete. Applied private-route browser/assistive-technology certification and prototype migration remain.

### Implement typed Attention

Create separate Account-scoped aggregates:

- `InformationRequest` for explicit fact requirements and exact parent resumption;
- `WorkReview` for version-bound review decisions;
- `ConsequentialApproval` for canonical payload, policy, evidence, expiry and cancellation.

Add forced-RLS persistence, optimistic versions, immutable events, queries, redacted DTOs and authorization matrices. Answering shared information may complete only eligible requests; altering a proposal invalidates its approval.

Implementation checkpoint (2026-08-21): the three aggregates now have separate typed kernels and persistence restore boundaries. Information requests bind exact fact key/scope and parent Work, reviews bind an assigned User to a Work version and proposal digest, and consequential approvals bind canonical duplicate-key-free JSON, evidence, capability, operation/invocation, policy, expiry and optional independent review. Four Account-owned cell tables add forced RLS, composite Work/Conversation/Agent references, queue/detail indexes, immutable redacted event shapes, movement write fences and cascade-safe exact erasure counts. Classified PostgreSQL repositories restore every row through the domain kernel, provide bounded stable keyset queries, make identical creates idempotent, enforce expected-version updates and append redacted events atomically. Package-authorized application commands/queries now apply the narrower role/object matrix, require an active eligible assigned reviewer and expose content-minimized queue views. Exact shared-fact completion is a bounded serializable transaction and returns only waiting parent Work items with no open information blocker; concurrent duplicate submissions converge without duplicate events. The routed cell runtime resolves assigned reviewers through the exact signed request proof and a read-only global broker, preserving the cell-only `app-api` credential. It passes identifier-only completion plans into one atomic Work-owned transition boundary, and replay reconstructs the original answer cohort so an interrupted resumption can safely finish. An approved aggregate records its exact execute-only runner authorization in the same transaction, and approved-proposal invalidation cancels that projection; conflicting runner state rolls back the aggregate and event. Nineteen customer HTTP operations now cover redacted queues, authorized detail, creation, decisions, cancellations and action recovery with route-bound idempotency, ETag concurrency where applicable, exact package allowlists and generated typed Go/TypeScript contracts. The 19-tool MCP adapter reuses those application services with Bearer-only protocol authentication, exact per-call Account/package/role authority, typed structured results, safe error parity and raw canonical-payload preservation. The private Your Turn surface combines open information, signed-in-reviewer Work, Owner/Administrator approval and unknown/manual action-recovery queues with exact detail, read-only modes, tab drafts, stable retries, version-conflict recovery, independent confirmation, keyboard movement and live announcements. Tests cover role/object/package authority, exact information eligibility and parent planning, routed reviewer eligibility, Work resumption/replay, review/proposal invalidation, approval projection/time/binding rollback, payload/evidence/policy invalidation, action recovery/redaction/dual control, repository replay/concurrency/conflict/isolation/redaction, RLS, cross-Account constraints, event immutability, movement participation, erasure/restore, HTTP/MCP boundary behavior and private-shell contracts. The production MCP token/Account-routing gateway, applied browser/assistive-technology certification and final mobile-first Vue SPA consolidation remain. See [Attention module](attention-module.md) and [MCP transport](mcp-transport.md).

### Connect Work to Agent execution

Implement a Work-owned claim/start/link/heartbeat/release/resume/reconcile state machine. Every crash point must converge to one linked active Run or one safely requeued Work item. Existing Agent dispatch and runner capacity are reused but do not replace this boundary.

Implementation checkpoint (2026-08-21): Persona assignment now freezes an immutable, Account-owned execution intent in the same Work transaction, including Work version/text, initiating User, exact Persona version, Boardroom policy version and deterministic Conversation/Run identities. A lease-fenced worker resolves current Membership, placement, enabled Agents mutation access, entitlement version and concurrent-Run limit through a private mTLS admission endpoint without presenting a browser bearer token or impersonating the User. Execute-only cell functions claim, heartbeat, retry/release and atomically create the complete one-turn Agent plan, enqueue the existing dispatch path, link Conversation/Run provenance, advance Work to `in_progress`, append a redacted Work event and retire the execution lease. Unknown commit replay reconciles the same deterministic linked pair; reassignment revokes an already-held lease; capacity and transient authority failures requeue with bounded backoff; permanent Work, Persona or authority drift dead-letters without creating a Run. Forced RLS, Account movement fencing, exact erasure counts, least-privilege Docker/Kubernetes composition and crash-window PostgreSQL tests are complete. Applied stage execution and final product-journey certification remain release evidence, not construction gaps. See [Work-to-Agent execution](work-agent-execution.md).

### Finish consequential actions

- Make Attention the owner of approval projections consumed by the action ledger.
- Add the executor registry, definite-failure retry policy, unknown reconciliation and dual-controlled manual resolution.
- Add at least one real consequential adapter with idempotency and side-effect-free lookup.
- Add redacted customer/operator views, alerting and recovery runbooks.

Implementation checkpoint (2026-08-21): Attention's approval transaction is now the sole producer of the exact runner authorization projection. The action ledger consumes a versioned content-free executor/retry registry, freezes executor and policy versions per operation, retries only registry-allowlisted definite no-effect failures with bounded delay, and forces all uncertain/completed replay through reconciliation. Unknown outcomes can enter a two-person manual-resolution record whose requester cannot confirm it. The first real executor, `stripe.customer.create`, uses the operation UUID as Stripe's idempotency key and reconciles through side-effect-free exact Account/operation metadata search; the Stripe credential never enters runner Jobs. Content-free broker metrics, paging rules and a recovery runbook are complete. A shared Owner/Administrator application service now supplies content-redacted stable pages/detail and replay-safe manual resolution commands through generated HTTP contracts, four optional MCP tools and Your Turn. Human evidence is retained only as a SHA-256 digest on these surfaces, and confirmation remains independently authorized. Applied stage execution and final journey certification remain release evidence. See [Consequential action execution](consequential-actions.md).

Exit: Work, Your Turn and approved external actions are complete through domain, persistence, HTTP, MCP and UI.

## P3.2 — Knowledge and Baseline

Implementation checkpoint (2026-08-21): the prototype inventory is complete and the final Knowledge boundary is fixed around immutable evidence, reviewable canonical claims and monotonic accepted fact revisions. The typed kernel freezes Account, source kind/reference/revision, SHA-256, capture time and actor; rejects duplicate/trailing/oversized JSON; models Account/Work/Conversation scopes and sensitivity; permits workloads to propose but only humans to decide; and refuses authoritative acceptance when Agent derivation is the only supporting evidence. The first cell schema adds evidence, claims, citations, current facts, immutable fact revisions and content-redacted events with forced RLS, composite Account references, database acceptance/projection guards, movement fences and exact erasure accounting. The classified PostgreSQL repository provides exact replay, domain restoration, serializable claim decision/fact projection, supersession, stable redacted fact pagination and event redaction. Fresh PostgreSQL tests prove cross-Account denial, immutable history, agent-only-evidence rejection, repository replay and accepted fact creation. Routed HTTP/MCP/private UI surfaces, document infrastructure, retrieval and the Baseline state machine remain in this block.

Routed-surface checkpoint (2026-08-21): evidence registration, claim proposal/detail/decision and sensitivity-filtered fact listing now cross the signed app-router/cell boundary through the shared Knowledge service. All mutations use routed UUID idempotency, decisions require weak version ETags, responses are typed in OpenAPI, and generated Go/web contract inventories are current. The optional MCP transport exposes the same five operations with Account/package authorization and bounded opaque cursors. A discoverable proposed-claim review queue and private Knowledge page remain before this surface is considered complete.

Review-surface checkpoint (2026-08-21): a sixth shared operation provides a stable, value-redacted claim queue filtered by state, scope, key prefix and caller sensitivity. The private `/app/knowledge` page uses that queue to discover proposals, fetches exact claim value/citations only on selection, and submits human accept/reject decisions with a fresh idempotency UUID and the claim ETag. The accepted-fact projection remains value-redacted in its list. Read-only package mode removes decision controls. Document admission, retrieval, citation-to-document validation and the Baseline lifecycle remain next.

Document-lifecycle checkpoint (2026-08-21): [ADR-0004](decisions/0004-knowledge-document-lifecycle.md) selects containerized MinIO in `ubunturojo`/Hostinger and Linode Object Storage in LKE behind one S3-compatible port. The Account-owned kernel, trusted admission, ClamAV/Tika processing, deterministic chunks, ready-only publication, content-free processing/deletion queues, exact-version receipts and atomic physical finalization are complete under forced RLS, movement fencing and erasure accounting. The existing least-privilege per-cell worker fairly services both queues and reports their independent age/dead-letter state. A generated PostgreSQL full-text vector and GIN index now support shared HTTP/MCP retrieval without another search store: query text stays in the request body, PostgreSQL filters Account, current-ready revision and sensitivity before ranking, and responses contain at most 20 integrity-bound 4 KiB chunks. Exact citation lookup binds Account/document/revision/chunk, offsets, digest and index generation and fails once a citation is stale or deleted. Generated contracts now cover 98 customer operations. Cross-Account search, restricted filtering, exact resolution, stale/deleted rejection, unknown-delete replay and full local Docker dependencies are tested. The document upload/deletion and retrieval/citation bullets below are constructed; Baseline is next.

Baseline-foundation checkpoint (2026-08-22): the typed assessment aggregate and first normalized cell schema are complete. The aggregate freezes catalog/scope-policy versions; records interview answers as exact accepted-Fact revisions or explicit unknowns; freezes requirement responsibility and renewal policy; binds immutable human decisions to Knowledge Evidence; requires every requirement to be explicitly satisfied, a gap or not applicable before plan submission; binds Owner/Administrator approval to the exact plan ID, SHA-256 and pre-plan assessment version; refuses readiness with gaps; and preserves superseded assessments through reassessment. Six Account-owned tables add forced RLS, exact Fact/Evidence and Persona references, immutable decision/event history, stable queue/renewal indexes, movement fencing and exact erasure/restore counts. Pure replay tests and the full `ubunturojo` Docker gate pass. Repository/application commands, deterministic Work-plan creation, routed surfaces and prototype transformation remain open.

- Implement source-attributed facts, claims, evidence, revisions, scope and confidence.
- Implement document upload, malware/type/size checks, extraction, chunking, indexing, retention and deletion.
- Implement Account-scoped retrieval, citation validation and bounded result contracts.
- Implement the Baseline interview/state machine, evidence decisions, readiness, renewal and reassessment.
- Implement evidence-source selection and plan generation without allowing unsupported Agent output to become authoritative fact.
- Migrate and reconcile prototype knowledge/documents/baselines with object, index and citation checks.

Exit: source-attributed business memory and baseline claims are complete and production-certifiable.

Baseline persistence checkpoint (2026-08-22): the Baseline aggregate is now carried through the canonical authorized application boundary and a classified, Account-scoped PostgreSQL repository. The repository persists each optimistic state transition with a redacted audit event, exact accepted Fact revision and Knowledge Evidence references, immutable plan binding and atomic reassessment. A PostgreSQL 17 lifecycle test covers idempotent start, stale-write conflict, complete restore, cross-Account denial, frozen and immutable history, event redaction, approval/readiness and one-current-assessment enforcement. The next P3.2 construction slice is the supported HTTP/MCP boundary and deterministic creation of proposed Work from the exact approved assessment/plan digest; this checkpoint does not declare P3.2 complete.

Baseline surface/planning checkpoint (2026-08-22): 12 routed HTTP operations now cover start/get, answers, inventory, evidence decisions, explicit dispositions, plan submission/approval/materialization, readiness and reassessment with strict JSON, Account-bound operation IDs, idempotency keys and optimistic ETags. Generated Go/TypeScript contracts and three MCP tools expose the same application outcomes. Plan submission no longer trusts a supplied digest or count: the aggregate deterministically hashes its frozen, sorted gap proposals and restore verifies the binding. An exact approved plan is materialized through Work's existing authorization, capacity reservation and idempotent repository boundary with deterministic Work/correlation IDs; a partial or unknown result is completed by replaying the same operation. P3.2 remains open for governed evidence catalogs and source grants, completed-Work evidence feedback, renewal workers and prototype transformation.

Baseline governed-scope checkpoint (2026-08-22): the versioned repository-owned catalog now defines seven explainable interview questions, twenty evidence definitions and deterministic software, field-service, professional-services and retail profiles, including a solo-business adjustment. Every required question must carry an exact accepted-Fact revision or explicit reasoned unknown before inventory; the Account-RLS repository resolves the exact historical revision and the application rejects unknown question keys, duplicate references, mismatched Fact keys and unsuitable canonical values. The application—not HTTP or MCP callers—now freezes current catalog/policy versions on start and reassessment and derives requirement IDs, titles, responsibilities and renewal periods from resolved facts. The three formerly caller-shaped operations accept only strict empty JSON objects and generated clients have dropped the obsolete inputs. PostgreSQL coverage proves exact resolution and cross-Account denial. P3.2 remains open for scoped source grants/evidence selection, completed-Work evidence feedback, renewal/reassessment scheduling and prototype transformation/reconciliation.

Baseline source/evidence checkpoint (2026-08-22): immutable-scope, revocable email and Google Drive grants now permit only selected folders plus optional email date bounds after plan approval, with no credential storage. Owner/Administrator application policy, forced-RLS persistence, redacted events, stable listing, exact erasure/movement participation, three generated HTTP operations and two MCP tools are complete under the Integrations package; Integrations now explicitly depends on Knowledge. An exact app-router allowlist now makes all Baseline routes reachable and assigns lifecycle routes to Knowledge and grant routes to Integrations. Evidence decisions resolve the Account-owned Knowledge Evidence source before changing the assessment. Human evidence registration is limited to owner statements, while Agent derivations and integration records without the future connector-to-grant capture binding fail closed and cannot satisfy requirements. The following checkpoint closes completed-Work evidence feedback and deterministic renewal/reassessment materialization; connector execution remains in P3.4.

Baseline completion/maintenance checkpoint (2026-08-22): approved Baseline plans now persist immutable normalized Work lines behind the frozen digest/count, preventing later requirement completion from invalidating the historical plan or changing exact-retry materialization. A linked requirement is satisfied only when the application resolves both an exact completed Account Work item carrying that requirement's frozen Baseline provenance and a separate acceptable Knowledge Evidence identity; completion alone, Agent derivation and unbound integration records remain non-authoritative. The reasoned confirmation is an optimistic Baseline event and renews the requirement from the human decision instant. Readiness freezes a 90-day reassessment date. The 30-day maintenance window deterministically materializes renewal and reassessment Work, uses urgent priority after the due instant and derives cycle-specific replay IDs from the exact obligation/due date. Two additional routed HTTP operations and the existing MCP mutation boundary bring the customer contract to 115 operations. PostgreSQL plan-line RLS, immutability, movement/erasure participation, completed-Work resolution, cross-Account denial and restore are covered. P3.2 now remains open for scheduler-driven invocation and prototype transformation/reconciliation; connector execution remains in P3.4.

Prototype transformation checkpoint (2026-08-22): a new read-only operator command snapshots an exact retained prototype tenant under PostgreSQL repeatable read, verifies all stored document revision checksums and emits a deterministic, content-sealed private bundle. The manifest reconciles exact source table counts, schema versions and transaction checkpoint to normalized fact candidates, one reconstructed text object per non-deleted legacy document revision, regenerated final chunk expectations, governed Baseline question/requirement review plans and deterministic target IDs. A standalone unresolved report accounts for missing owner actor identity, unbound email/Drive/public captures, legacy evidence decisions, interview messages, research results and Baseline/Work links. No ambiguous record becomes authoritative, Agent-derived material stays non-authoritative, deleted documents are not resurrected, and export writes no destination state. Atomic no-overwrite publication plus a rollback checkpoint prevent partial bundles. Pure and PostgreSQL 17 tests cover deterministic ordering, checksum/tamper rejection, exact numeric confidence conversion and full inventory scanning. P3.2 remains open for the destination receipt/import/reconciliation operation and owner review; unattended maintenance invocation belongs to the workload scheduling construction boundary.

Prototype destination checkpoint (2026-08-22): the immutable release image now includes a strict local bundle loader and replay-safe `prototype-import` operator binary. Import is performed only as the named `prototype-migration` workload through the final Knowledge services and S3 object boundary. It admits unpublished reconstructed documents plus reviewable Evidence/Claims, never owner statements or accepted Facts. A forced-RLS run/receipt/event ledger binds the exact manifest, source checkpoint, expected counts, deterministic targets and reconciliation digest; movement fencing, immutable history and exact Account-erasure participation are tested. Post-processing reconciliation opens both exact object versions, compares bodies and SHA-256 values, regenerates every chunk, validates all Evidence/Claim/citation records and refuses missing or extra receipts. A first run safely stops in `imported` while the normal document worker processes; exact retry converges to `reconciled` and writes a private no-overwrite certificate. PostgreSQL 17 coverage proves receipt replay/conflict behavior, immutable terminal state, pending-to-ready retry and exact erasure. A dedicated global/cell database role, separate prefix-scoped object identity, exact cell-placement check and dual restore gates now constrain deployment. The executed internal cohort processed eight reconstructed revisions, reconciled 16 exact object versions and every deterministic chunk, accounted for all 539 unresolved records, and sealed reconciliation SHA-256 `2ad508e3d704bdc5c68bc058ed832dadc69d8b27070e0eb52c6952410728c1c9`. It exposed and closed a forced-RLS event-replay grant and Tika plain-text normalization mismatch. P3.2 remains open only for the real Owner to resolve or decline the private review packet and sign its review certificate; no connected environment was mutated.

Baseline scheduler checkpoint (2026-08-22): a content-free per-cell technical queue now transactionally tracks the earliest 30-day lead window for every ready assessment's reassessment or satisfied-requirement renewal obligation. `baseline-maintenance-worker` is the sole unattended identity: it leases with `SKIP LOCKED`, revalidates exact Account placement, package authority and assessment version across dedicated least-privilege global/cell credentials, and calls the same capacity-governed Work service used by humans. Deterministic cycle IDs make daily retries and crash recovery duplicate-free; stale leases fail, bounded failures enter dead-letter state, and no workload path can decide evidence, approve plans or impersonate a User. Restore-gated Docker local/Hostinger stage topology and two-replica LKE cell manifests are included. PostgreSQL 17 tests cover trigger scheduling, early exclusion, lease fencing, recurring replay, dead-letter stats and exact erasure. Construction and the sealed internal migration cohort are complete; P3.2 now remains open only for the human Owner review certificate.

## P3.3 — Workspace and Agent execution

Cost-control checkpoint (2026-08-22): the provider-neutral usage contract now includes gateway-calculated micro-unit cost. Model-gateway starts only with a syntactically valid operator price book, requires an exact entry for the requested model and fails closed before provider invocation when no entry exists. Provider responses cannot inject cost. The bounded runner carries the immutable Persona maximum, accumulates token and cost usage across every sequential model/tool turn with overflow checks, and stops with a stable `cost_limit_exceeded` failure once the ceiling is crossed. Successful projection stores cost beside token counters, binds it into idempotent replay comparison and exposes the completed usage on the generated HTTP/TypeScript Agent Run contract. Stage secret preparation, Compose and LKE secret contracts now require the price book without committing actual provider prices.

Context-snapshot checkpoint (2026-08-22): StartRun now accepts explicit bounded Work-item, accepted Knowledge-Fact and Baseline-Assessment attachments. The cell repository captures all selected sources in one repeatable-read transaction, freezes exact source versions and canonical content into the existing Account-owned Run row, and binds per-item plus aggregate SHA-256 digests. Active-fact and membership sensitivity rules are rechecked before capture, missing or oversized selections fail atomically, idempotent retries compare the original attachment identities, and failed-run retry copies the original bytes rather than reading newer source state. Dispatch validates the canonical envelope and digest, prepends it as clearly labeled untrusted evidence, and otherwise uses the existing Conversation sequence watermark. The generated HTTP/TypeScript contract publishes only attachment kind, identity, version and digest. This closes the deterministic Work/Knowledge/Baseline/Conversation context boundary without adding a new erasure/movement table. Orchestration/synthesis, model fallback, schedules, document attachments, governed proposals and remaining queue recovery work were still open at this checkpoint.

Sequential-orchestration checkpoint (2026-08-22): an ordered Run no longer dispatches every Persona from the original user-message watermark. Only turn one is initially eligible. Successful projection atomically commits its validated Persona Message, advances exactly the next turn's frozen Conversation watermark to that Message sequence and enqueues exactly that invocation. The later dispatch snapshot therefore contains every preceding contribution in order while retaining the Run's original Work/Knowledge/Baseline bytes and Persona versions. A failed turn atomically cancels every still-queued downstream turn with `prior_turn_failed` and makes the Run terminal (`failed` before any contribution, otherwise `partially_failed`), so a plan cannot strand undispatchable invocations. Customer retry recreates the failed turn and its entire canceled successor cohort as a new sequential plan from the failed turn's last valid watermark rather than dropping work or reading newer context. Migration refuses to change sequencing while an older multi-turn Run is active. Fresh PostgreSQL migration/integration certification covers deferred dispatch, watermark advancement, downstream cancellation, exact recovery and idempotent projection. This completes the durable sequential Boardroom pass; manager synthesis, delegation, resumable owner questions, model fallback, schedules, document attachments, governed proposals and remaining queue recovery work are still open.

Document-attachment checkpoint (2026-08-22): the same StartRun context contract now accepts explicit Knowledge Document identities. Capture resolves only each document's ready current revision under Account RLS, rechecks restricted sensitivity against the initiating Membership, freezes the revision identity, title, media type, text digest, index generation and every ordered integrity-verified chunk, and exposes only the document identity/revision/digest reference on Run reads. No attachment is silently truncated: a non-ready, inaccessible, corrupt or aggregate-over-48-KiB selection fails the entire Run transaction. Exact retry compares the selected document identities and failed-Run retry copies the already frozen bytes, so later publication, deletion or reindexing cannot change the execution. Generated HTTP/TypeScript contracts and fresh local PostgreSQL dispatch certification cover the attachment path. Larger documents remain available through bounded retrieval tools; model fallback, manager/delegation flows, resumable owner questions, schedules and governed proposals remain open.

Fallback-protocol checkpoint (2026-08-22): capability execution failures can now preserve one validated content-free machine classification across the private broker transport while retaining the existing generic failure type. The gateway emits no provider body or private error detail, malformed reason codes collapse to `execution_failed`, and transport/client tests bind `model_provider_unavailable` end to end. This is prerequisite plumbing, not fallback completion: immutable ordered target policy, preallocated attempt identities, selective retry, accumulated usage and projected selected-model audit remain open.

Fallback-completion checkpoint (2026-08-22): immutable Persona policy now freezes one primary model and at most two ordered same-provider fallbacks under the same reasoning, token, cost and tool policy. Every possible model step/target pair receives a deterministic operation identity before dispatch. The runner may advance targets only when the first provider call returns the exact content-free `model_provider_unavailable` classification; every other failure is terminal, and the first successful response pins that model for all provider-continuation steps. Usage and cost remain cumulative across successful model steps. The Account-owned invocation freezes all permitted targets, successful projection validates and stores the selected target atomically, exact replay compares it, and Agent Run reads expose it without provider error detail. The Work-to-Agent producer uses the same target and operation-shape boundary. Fresh PostgreSQL migration, upgrade-role, forced-RLS, Work-link, projection, API-contract, website and full local Docker certification pass. Manager/delegation flows, resumable owner questions, schedules, governed proposals and remaining queue recovery remain open.

Result-policy checkpoint (2026-08-22): result publication now applies a named, frozen version-1 policy after structural/provider validation and before any customer Message commit. The content-free projection claim carries the immutable Persona citation/action policy, only later-turn Persona identities from the frozen Run plan and exact document/chunk identities from the frozen context envelope. Required citations must bind one of those exact pairs, `none` suppresses citations, duplicate or invented citations/delegates fail closed, delegations may target each later Persona at most once and `none` suppresses proposed actions. New document snapshots include the exact chunk identity as well as its integrity fields. An accepted targeted delegation is deterministically inserted after its source contribution as labeled untrusted task content when the later Persona dispatches; models still cannot enqueue Personas or mutate the plan. The policy version is stored on every invocation so replay cannot silently adopt a later implementation. Pure policy, projection rejection, targeted-history, fresh PostgreSQL migration/least-privilege claim and full local Docker/race certification pass. Deterministic delegation expansion beyond the initially frozen plan, manager configuration/final synthesis, owner-question wait/resume and projection of proposed actions into Attention remain open.

Manager-synthesis checkpoint (2026-08-22): a Boardroom now has optimistic, Account-scoped manager configuration that accepts only an active Persona with a published version in that exact Boardroom. The mutable manager identity and Boardroom policy version affect only future plans. `manager_led` Run creation freezes the caller-selected specialists in order and the manager's exact current immutable Persona version as the final turn; selecting the manager as a specialist is rejected, and the existing sequential watermark/projection path makes every prior specialist contribution available to that final synthesis. The Run mode is durable, exposed in the generated HTTP/TypeScript contract and preserved on failed-turn retry while the retry continues to reuse the original frozen Persona versions and context bytes. Public HTTP requires an explicit mode; legacy trusted one-turn producers normalize to `selected`. The private Agents surface can set the synthesis manager, selects explicit run mode and excludes the manager from the specialist picker. Fresh PostgreSQL coverage proves optimistic replay, specialist-to-manager order and manager-version freezing after later publication. Account movement remains acyclic because same-Boardroom/active publication validation is performed transactionally in the command path rather than by a reverse foreign key. Full local Docker, PostgreSQL, account-movement, erasure, race, OpenAPI, browser, website and vulnerability certification pass. Deterministic expansion beyond the frozen plan, owner-question wait/resume, proposed-action projection and remaining queue/operator recovery remain open.

Governed-action checkpoint (2026-08-22): new Agent invocations now freeze result-policy version 2, while already queued version-1 work remains replayable and message-only. Persona versions may enumerate at most 32 immutable action capabilities. A v2 result may propose no more than eight actions and every action kind must match that exact frozen allowlist; `action_policy=none`, unknown capabilities and malformed or excessive proposals fail before publication. The projection worker derives stable approval, operation and audit-event identities from the invocation, binds canonical input and evidence/result digests, and uses one security-definer transaction to commit the Persona Message and independent-review Attention approvals. Work-originated Runs retain their Work identity; direct Boardroom Runs produce Account-scoped approvals. Projection never executes the proposed effect. Exact retry must find the same immutable approval and event or fails as a conflict, and the worker retains execute-only authority with the superseded projection surfaces revoked. OpenAPI and generated TypeScript expose action-capability configuration. Pure policy/projection tests plus fresh PostgreSQL 17 integration prove allowlist denial, deterministic identities, one approval/event on replay, Account isolation, least privilege and rollback of the entire Message/Run/Attention transaction. Full local Docker, race, website build/lint/audit, object-policy, movement and erasure certification pass. Owner-question wait/resume, deterministic plan expansion and remaining queue/operator recovery remain open.

Owner-question checkpoint (2026-08-22): result-policy version 3 now permits at most eight owner questions per completed turn while preserving replay of versions 1 and 2. For a Work-originated Run, successful projection atomically commits the Persona Message, creates one exact Account-scoped Attention `InformationRequest` per question, transitions Work from `in_progress` to `waiting`, and moves the Run to `waiting_input`. Stable request, Attention-event and Work-event identities derive from the invocation; each fact key is the SHA-256 prefix of the exact UTF-8 question and PostgreSQL independently recomputes that binding. Immutable approval and question timestamps derive from the authenticated runner submission, so a projection retry after an uncertain commit cannot drift. A direct Boardroom Run remains conversational and publishes questions without fabricating a Work lifecycle. Once the latest request cohort is answered through independently supported accepted Knowledge Facts, the Work-owned resumption boundary closes the waiting Run, returns Work to `in_progress` and queues a new deterministic Persona execution with a bounded, explicitly untrusted owner-fact appendix. Fresh pure-policy/projection and PostgreSQL 17 integration coverage proves the pause/request/fact/resume/continuation path, Work-versus-Boardroom behavior, deterministic identities and the eight-question bound. The complete `ubunturojo` Docker certificate passes normal and race tests, migrations, vet, formatting, API contracts, website build/lint/audit and object-policy checks. Deterministic plan expansion, schedules and remaining queue/operator recovery remain open.

Frozen-plan decision (2026-08-22): [ADR-0005](decisions/0005-frozen-agent-run-plans.md) closes dynamic delegation expansion as an intentionally rejected design, not unfinished construction. Allowing model output to add a Persona would rewrite the immutable plan/digest, disturb preallocated invocation/tool identities, risk moving a frozen synthesis manager and make replay depend on mutable Persona state. Delegations therefore remain bounded routing hints to later Personas already frozen into the Run; additional expertise is requested through a separately authorized user, schedule or approved application Run command. Schedules and remaining queue/operator recovery are the open Agent-workspace items.

Agent-queue Stage-recovery checkpoint (2026-08-22): RC.2 exercised both dispatch and projection dead-letter paths in the connected Hostinger cell through the actual signed `agent-queue-admin` image and temporary execute-only/no-table-read roles. Populated and empty inspection, exact requeue, wrong-target denial, wrong-queue denial, duplicate/state-fence rejection and immutable-event mutation denial passed. Restarted workers reclaimed both jobs; the deliberately incomplete fixture failed closed as `snapshot_unavailable` and `payload_unavailable` without a runner or provider call. The normal erasure function accounted for both queue rows, four Account-attributed operator events, the runner row and complete Agent aggregate, leaving zero synthetic Account rows; a second erasure removed the isolated wrong-queue fixture. The temporary roles, passwords and authorization artifacts were erased and both workers returned healthy. The content-free certificate SHA-256 is `1c2da6f0d753d8d64419561e5690a52fcfa818b7a3168403a2e6dc512f62c1e9`. Dispatch/projection dead-letter handling, payload-retention fencing and connected operator recovery are complete; production Job grants and backup restore replay remain P3.7 environment evidence.

Scheduling-kernel checkpoint (2026-08-22): the new provider-neutral Scheduling module defines daily and explicit-weekday weekly recurrence at an exact local wall time, requires an IANA timezone, and makes DST gap (`skip`/`next_valid`), DST overlap (`first`/`second`) and missed-run (`skip`/`catch_up_one`) policy explicit. Its search operates on real UTC instants around each local calendar date, so behavior never inherits the container host timezone. The first immutable execution template bounds and freezes an Agent Boardroom, selected Personas, run mode, subject, prompt and Work/Knowledge/Document/Baseline attachment identities; each occurrence will still enter the canonical Agents command so then-current entitlement, capacity, manager, Persona versions and context authorization are rechecked. Optimistic pause/resume behavior and `America/New_York` spring/fall transition fixtures are executable. [Scheduling module](scheduling.md) records the construction order. Forced-RLS persistence, an identifier-only due queue, non-impersonating workload dispatch, trigger-now, routed surfaces and operational recovery remain before customer schedules are enabled; the package-surface inventory therefore continues to fail closed with `customer-schedule` absent.

Scheduling-persistence checkpoint (2026-08-22): cell migration 51 now persists Account-owned schedule definitions and immutable content-redacted events under forced RLS, movement fencing and exact erasure accounting. A classified serializable repository provides exact-replay create, Account-isolated get and optimistic pause/resume while transactionally requiring active same-Boardroom published Personas and valid manager configuration. An active definition maintains one identifier/time-only due row; pause deletes it. A distinct least-privilege worker role can only call lease-fenced claim, bounded retry/dead-letter failure and content-free statistics functions and cannot read definitions or the cross-Account queue. Fresh PostgreSQL tests prove due ordering, exact lease binding, repository replay, cross-Account denial, event immutability and pause cancellation. The complete `ubunturojo` Docker certificate passes normal and race suites, migrations, formatting, vet, generated-contract checks, website build/lint/audit and object-policy verification. No occurrence or Agent Run is dispatched by this checkpoint: non-impersonating workload authorization, deterministic occurrence identities, successful completion/recurrence advance, `catch_up_one`, trigger-now, customer transports/UI and operator recovery remain, so `customer-schedule` stays absent from the package inventory.

Scheduling-occurrence checkpoint (2026-08-22): the pure execution processor now derives stable occurrence, Conversation, Run and message UUIDs from the schedule plus exact UTC instant, creates a locally dated Conversation subject and applies bounded missed-run behavior. A recurrence is considered missed only after a later selected occurrence is also due; `skip` records no Run, while `catch_up_one` creates one Run and advances beyond current time, so neither can replay an unbounded backlog. Cell migration 52 adds immutable forced-RLS occurrence history, explicit `workload/schedule-execution-worker` initiation, exact schedule/instant uniqueness, lease heartbeat and movement/erasure participation. The trusted repository holds schedule/queue locks, canonical Agent Run and context capture, occurrence/event insertion, optimistic recurrence advance and next-due refresh in one serializable transaction. Schedule creator identity is retained only as current-authorization subject and `created_by` provenance, not presented as the workload initiator. Fresh PostgreSQL integration proves atomic Run/occurrence/next-due commit, deterministic linkage, exact erasure counts and denial of definition, queue and occurrence table reads to the lease-only role. The production worker is intentionally not composed yet: private cell mTLS execution transport, global current-authority admission, certificate/process inventory and failure metrics remain before a runtime may poll the queue; trigger-now, customer surfaces and operator recovery also remain, and `customer-schedule` stays unpublished.

Scheduling-runtime checkpoint (2026-08-22): a dedicated `schedule-execution-worker` now polls with a queue-function-only PostgreSQL credential and a cell-specific SPIFFE client identity. It cannot read schedule, occurrence or queue tables. Load/dispatch/skip cross the exact cell's private app-api boundary; fresh Membership, placement, Agents mutation access, referenced Work/Knowledge access, restricted-data role and concurrent-Run limit cross the global admission boundary without a browser credential. App-api retains the entire serializable customer-data transaction, and deterministic commands make one safe transport retry reconcilable. Local, workload-TLS and Hostinger Compose topology, certificate generation, process inventory, metrics scraping and least-privilege role provisioning now include both cell workers. The LKE reference adds cell deployments, PDBs, queue-aware HPAs, exact NetworkPolicy paths, service accounts and environment origin replacements; both Linode overlays render without mutable images or plaintext Secrets. The complete `ubunturojo` Docker certificate passes normal and race tests, fresh PostgreSQL migrations, vet, formatting, generated contracts, website checks and object policies; applied ordinary-local and mutual-TLS stacks report both workers healthy, all route canaries pass, and the generated Hostinger stage contract validates. Customer update/delete/trigger-now commands, routed API/UI, operator dead-letter recovery and an applied synthetic occurrence/failure rehearsal remain, so the package surface remains unpublished.

Scheduling-customer checkpoint (2026-08-22): migration 53 completes optimistic definition revision and soft deletion while preserving explicitly named time/state constraints and transactionally removing every non-active due row. Seven routed lifecycle operations plus the separate trigger-now operation publish generated OpenAPI, Go and TypeScript contracts. The Account browser workspace supports daily/weekly local-time recurrence, explicit DST and missed-run policy, active Boardroom/Persona resolution, bounded context identities, revision, pause/resume/delete and replay-safe trigger-now; read-only Accounts retain views without mutation controls. Migration 54 gives trigger-now an immutable Account-RLS request and a second identifier-only lease queue. The same workload authorization and Agent Run transaction dispatches a deterministic `triggered` occurrence without advancing version or `next_run_at`; pause, revision or deletion cancels stale queued triggers. Fresh PostgreSQL proof executes one recurring and one immediate occurrence against the same definition and verifies exact retry, unchanged recurrence and least-privilege queue separation. `customer-schedule` is now registered under Agents; dead-letter operator recovery and applied environment failure rehearsal were still pending at this checkpoint.

Scheduling-recovery checkpoint (2026-08-22): migration 55 adds immutable Account-attributed operator evidence plus execute-only inspection and requeue functions for independent `recurring` and `trigger` dead letters. The new one-shot `schedule-queue-admin` mode requires exact environment confirmation and signed scope over action, queue, bound, Account, Schedule and optional trigger. It has no table grants and returns only identifiers, occurrence time, attempts, bounded error code and timestamps. Exact requeue preserves the original occurrence, resets leases/failure metadata and rejects stale or non-terminal targets. Database integration proves both queues, empty inspection evidence, duplicate conflict, evidence immutability, no direct reads and exact Account erasure participation. Applied local/stage failure rehearsal was still pending at this checkpoint.

Scheduling-local-rehearsal checkpoint (2026-08-22): the persistent `spyglass-local` Docker stack applied migrations 53–55 and the current application image with every service healthy. A synthetic due row exercised the live cell-A worker retry path and an induced terminal `authorization_unavailable` recovery drill. The real `schedule-queue-admin` image verified separate short-lived Ed25519 authorizations for inspection and exact requeue through a temporary execute-only/no-table-grant role; immutable evidence retained the previous attempt and error, the queue reset to pending, and the live worker reclaimed it. The Schedule was then soft-deleted, its due row disappeared, the temporary role was removed, and the local smoke certificate passed again.

Scheduling-stage-rehearsal checkpoint (2026-08-22): signed and admitted `0.3.0-rc.1` was deployed from clean checkout `263bb16d5cde51c7ce35ce6d19c7139f1750ee2a` with both cell schemas at migration 55, 40 workloads running and all 39 healthchecked workloads healthy. A deployment failure exposed that fresh secret generation could not rotate credentials already bound into retained databases; the secret-preparation contract now carries persistent credentials into additive topology upgrades and generates only missing workload identities. The corrected deployment used secret set `2026-08-22-02`. A real stage recovery drill then drove both recurring and trigger queues through retry and terminal failure, separate signed populated/empty inspection, exact requeue and worker-restart reclaim using a temporary execute-only/no-table-read role. Wrong-Account, duplicate-requeue and immutable-audit writes failed closed. Erasure-fenced cleanup removed the synthetic Account and the role, both workers returned healthy, and the complete stage verifier passed. The mode-600 evidence certificate is `/opt/spyglass-stage/evidence/0.3.0-rc.1/schedule-queue-rehearsal.json`, SHA-256 `68a3e20c5a445ce21c1bdb33b01178af0b3af209550c2ba345180f2c4b795ae3`. Scheduling construction and local/stage operational rehearsal are complete; production grants and Job wiring remain P3.7 release preparation.

- Complete workspace/Boardroom configuration and immutable Persona/version lifecycle.
- Complete structured recovery; immutable multi-turn orchestration, manager synthesis, bounded forward-only delegation and resumable owner questions are constructed.
- Implement deterministic context assembly from Work, Knowledge, Baseline and Conversation watermarks.
- Complete provider-neutral invocation contracts, budget/cost/token enforcement and model fallback policy.
- Complete bounded sequential tool execution, tool-output validation, citation binding and result publication.
- Add schedules; document attachments and governed consequential proposals are complete.
- Add workflow/replay durability where long-lived orchestration requires it.
- Preserve the completed dispatch/projection dead-letter, payload-retention and operator-recovery paths; apply their short-lived production Job grants and restore replay during P3.7.

Exit: Agent behavior is durable, replay-safe, Account-isolated and recoverable across every crash/provider boundary.

## P3.4 — Scheduling, business packages and integrations

Marketing-scope/kernel checkpoint (2026-08-23): [ADR-0007](decisions/0007-marketing-governed-release.md) defines Marketing as Account-owned campaign intent, immutable creative revisions and exact human-approved release snapshots—not an email/web provider, public-site CMS or analytics store. The provider-free typed kernel now governs optimistic draft/active/paused/completed/archived campaign lifecycle; freezes supported channel intent, content digest/reference/accessibility metadata and monotonic asset revisions; and binds every release to one exact campaign version, normalized channel set and unique asset-revision set. Agents may prepare drafts only with exact Run/invocation provenance and cannot submit, approve, activate or deliver. Only an Owner/Administrator may bind an Attention approval and activate the exact release. Pure tests cover channel policy, provenance, immutable revision sequencing, role denial, stale versions and release/campaign binding. Forced-RLS persistence, application/repository boundaries, Catalog behavior, HTTP/MCP/UI, Agent proposals, Integration execution, retention and applied acceptance remain, so `marketing` correctly stays non-executable in the package inventory. See [Marketing module](marketing-module.md).

Marketing-persistence checkpoint (2026-08-23): cell migration 59 adds eight Account-owned forced-RLS tables for campaigns/channels, stable assets, immutable content-addressed revisions, exact release snapshots and content-redacted events. Exact `(Account, invocation, Run)` foreign keys bind Agent provenance. Per-asset advisory fencing admits only the next revision; snapshot/revision/event mutation is rejected. Guarded optimistic campaign transitions resolve the same-Account approved release, exact prior campaign version and identical normalized channel set, then recheck the Attention approval is still approved and unexpired. Active-release cancellation requires pausing first. Every table participates in namespace write fencing, acyclic Account movement and exact erasure accounting. Fresh PostgreSQL 17 tests prove revision order, immutable/redacted records, cross-Account RLS, complete movement topology and whole-cell erasure counts. Classified repository/application services and all executable surfaces remain next; `marketing` stays non-executable.

Marketing-application checkpoint (2026-08-23): the canonical Marketing package authorizer and classified Account transaction boundary now cover replay-safe campaign create/get/revise, immutable asset revision creation, exact release create/get/submit/approve/cancel and campaign activate/pause/complete/archive. Human drafting permits Owner/Administrator/Member; governance remains User-only and manager transitions require Owner/Administrator. Agent drafts inherit no human role and must bind the authenticated `runner-invocation` to the exact supplied invocation/Run tuple. The repository rejects altered request-ID replay, stale versions, cross-Account records and mismatched release assets; activation locks the exact release before the database rechecks Attention. Fresh PostgreSQL 17 tests prove two revision cycles, exact replay, complete submit/approve/activate/pause/cancel behavior and cross-Account concealment. Stable list pages/cursors, generated HTTP/MCP, private UI, Agent proposal tools and Integration delivery remain, so `marketing` stays non-executable.

Marketing-query checkpoint (2026-08-23): the package-authorized read boundary now provides bounded campaign pages over stable `(updated_at DESC,id)`, per-campaign release pages over stable `(created_at DESC,id)` and campaign/optional-asset revision pages over stable `(asset_id,revision DESC)`, with validated filters, application-owned defaults/maxima and structurally complete cursors. The repository fetches one extra identity and emits a continuation only when another aggregate exists, then restores every complete snapshot under one read-only Account transaction. Migration 60 adds the matching keyset indexes. Fresh PostgreSQL 17 tests prove exact two-page traversal for all three families. Generated transports remain; Marketing is still non-executable.

Marketing-HTTP-read checkpoint (2026-08-23): five generated session-authenticated cell operations now expose stable campaign list/detail, per-campaign asset-revision and release pages, and release detail through the signed request-bound Account authority. The cell runtime constructs the classified Marketing repository/service; cross-Account path targets are concealed and package authorization is rechecked inside the application boundary. Versioned kind-bound opaque cursors reject unknown fields and cross-collection replay, detail resources emit weak version ETags, and asset SHA-256 values are normalized to lowercase hex without exposing content or provider credentials. OpenAPI generates matching Go and TypeScript inventories/types, all response families pass contract validation, and the complete Go suite plus focused race/vet gates pass. Mutation HTTP, MCP, private UI, Agent proposals, Integration execution, Catalog/retention and applied acceptance remain; Marketing stays non-executable.

Marketing-HTTP-mutation checkpoint (2026-08-23): eleven generated customer commands now cover campaign create/revise/archive, immutable asset-revision append, release create/submit/approve/cancel and campaign activate/pause/complete. Each command binds the public idempotency header to the signed route operation UUID; every versioned transition requires one weak `If-Match` ETag and returns the resulting version. Browser drafts carry human provenance only, SHA-256 input is canonical lowercase hex, JSON is closed to unknown/trailing input, and cross-Account targets remain concealed. The application boundary remains the authority for human-only governance, manager roles and exact Attention approval binding. Focused tests validate every typed response plus missing-version, malformed-digest and Account failures. MCP, private UI, Agent proposals, Integration execution, Catalog/retention and applied acceptance remain; Marketing stays non-executable.

Marketing-MCP checkpoint (2026-08-23): the Bearer-only protocol surface now publishes 16 typed tools covering five reads, three human/Agent draft creates and eight human-governed lifecycle commands. Each tool has a global Marketing route requirement and repeats authorization in the cell; operation UUIDs, expected versions, stable cursors and structured output reuse the canonical application services. Human drafts reject Run provenance. Workload drafts derive invocation identity only from the authenticated `runner-invocation` principal and require an explicit valid Run, while application policy continues to deny workload governance and delivery. Asset hashes remain lowercase hex, cursor kinds cannot cross collections and safe error mapping redacts backend detail. Protocol tests prove deterministic complete schemas, route claims, human/Agent provenance, cursor opacity, version failure and error redaction. Private UI, Attention proposal UX, Integration execution, Catalog/retention and applied acceptance remain; Marketing stays non-executable.

Marketing-private-application checkpoint (2026-08-23): `/app/marketing` now provides the selected Account a discoverable package-aware desktop workspace over the generated HTTP boundary. Campaign filtering/detail, immutable creative-revision inspection and release snapshots remain visible in read-only mode; enabled mode adds campaign create/revise/archive, asset-revision append, release create/submit/cancel and campaign activate/pause/complete around already approved snapshots. Existing-resource commands use current versions through weak `If-Match`, each mutation has a new route-bound idempotency UUID, customer values render only through DOM text nodes and no session or draft material enters browser storage. Locked Accounts receive no Marketing script and read-only Accounts receive no mutation forms. Template/client tests, JavaScript syntax validation and the complete Go gates cover the boundary. The requested mobile visual redesign remains deferred to the final supported-device acceptance pass. Attention proposal UX, Integration execution, Catalog/retention and applied acceptance remain; Marketing stays non-executable.

Marketing-Attention-activation checkpoint (2026-08-23): published Personas may now propose the exact `marketing.release.activate` action over only campaign/release identities and expected versions; it is not a directly callable runner tool. An approving Owner/Administrator projects the canonical payload into the existing durable worker, which derives immutable child event IDs from the approval operation and atomically binds the Attention approval to the submitted release plus activates the matching campaign. The database rechecks Account, campaign, frozen version/channel, decision actor and expiry. Side-effect-free reconciliation requires both exact events, so approval cannot leave a partial campaign transition and uncertain transport outcomes cannot execute twice. The browser no longer asks for pasted Approval UUIDs and instead routes submitted work to Your Turn. Focused handler tests and a fresh PostgreSQL 17 integration test prove spoof rejection, atomic activation and exact reconciliation. Narrow runner Marketing draft tools, Integration execution, Catalog/retention and applied acceptance remain; Marketing stays non-executable.

Marketing-Agent-draft checkpoint (2026-08-23): published Personas may now receive three bounded Marketing reads for campaigns, immutable asset revisions and releases plus three additive campaign/asset/release draft tools. The private tool router reauthorizes current Account placement and Marketing package mode on every call, caps reads at 100 records and routes mutations only to dedicated internal draft endpoints. The cell derives invocation identity from the signed workload principal and combines it with the explicit Run; the canonical application service and existing database foreign key independently enforce the exact `(Account, invocation, Run)` provenance tuple. Unknown fields, invalid identities, human callers and direct governance remain denied. The generated Persona tool-capability enum now matches the six additions, and focused policy/router/cell tests prove classification, path/body binding and provenance. Marketing construction step 5 is complete; Integration execution, Catalog/retention and applied acceptance remain, so Marketing stays non-executable.

Integrations-scope/kernel checkpoint (2026-08-23): [ADR-0008](decisions/0008-integration-connector-execution.md) resolves the missing provider boundary: Marketing approval freezes creative/channel intent, while Integrations separately freezes the exact connector scope and credential generation used for an external effect. The provider-neutral kernel now governs manager-only connections, immutable email/web-publication scope revisions, opaque secret-broker credential attestations, monotonic rotation/revocation and content-free health observations. Delivery records bind an exact Marketing release version, Attention approval, connector revision, credential generation, capability and payload digest. Definitive absence after reconciliation may permit bounded re-execution; timeouts, ambiguous responses and lost leases admit reconciliation only and reach manual resolution after three total attempts. Pure tests cover cross-kind capability denial, HTTPS/path normalization, immutable authority, credential lifecycle, exact execution binding, retry safety and bounded unknown outcomes. Forced-RLS persistence and the connector worker remain next; both `integrations` and `marketing` stay non-executable.

Integrations-persistence checkpoint (2026-08-23): cell migration 62 adds seven Account-owned forced-RLS record families for connector identity/revisions, opaque credential bindings, content-free health, frozen external executions, immutable attempts and redacted events, with a private content-free due queue. Deferred guards preserve atomic setup/rotation while freezing exact revision and credential-generation authority. The execute-only lease boundary rechecks active placement, active Marketing release/channel, exact unexpired `marketing.release.activate` approval and current connector authority immediately before work is returned. Unknown results and lost leases admit reconciliation only; definite no-effect reconciliation may schedule one bounded re-execution path, and an expired third lease enters manual resolution without an illegal intermediate state. Account movement now refuses unfinished effects and copies all Integration state; whole-Account erasure reports all eight Integration table counts. Fresh PostgreSQL 17 coverage proves authority binding, immutable history, RLS, credential ending safety, execute/reconcile ordering, movement and exact erasure. Application services, secret-broker binding, provider worker/adapters, generated surfaces, Catalog/retention and applied acceptance remain, so `integrations` and `marketing` stay non-executable.

Integrations-connection-application checkpoint (2026-08-23): the canonical Integrations service and PostgreSQL repository now cover manager-only connection creation, immutable scope revision, opaque credential-attestation activation/rotation, disable/enable, revocation and stable bounded reads. Every operation rechecks the current Integrations package; reads admit Members, while mutations require a human Owner/Administrator and reject workloads. Credential input contains only a provider code and SHA-256 broker-reference attestation. Atomic Account transactions preserve monotonic revisions/generations, end the bound credential on revocation and append content-redacted events. Deterministic child identities and exact event/input comparison make unchanged request retries safe and altered replay conflicting. Fresh PostgreSQL 17 coverage proves normalized restoration, full lifecycle, credential ending, stable pagination and cross-Account concealment. Broker handoff, health/execution observability, Marketing preparation, worker/adapters and generated surfaces remain; `integrations` and `marketing` stay non-executable.

Integrations-observability checkpoint (2026-08-23): package-authorized connection detail now returns the current immutable non-secret scope and latest content-free health, with stable bounded health history. External execution pages use closed capability/state filters, and detail restores the exact frozen authority/digest plus ordered immutable attempts through the typed kernel. Provider payloads, responses, credentials and broker references remain absent. Malformed persisted health/attempt evidence fails closed. Fresh PostgreSQL 17 coverage proves latest-health selection, health pagination, manual-resolution execution detail, attempt ordering, filtered listing and cross-Account concealment. Integrations construction step 3 is complete; broker handoff, Marketing preparation, worker/adapters and generated surfaces remain, so `integrations` and `marketing` stay non-executable.

Integrations-Marketing-preparation checkpoint (2026-08-23): a human Owner/Administrator may now select one active channel-compatible connection for an active approved release and prepare its immutable external execution. Matching current Account placement and entitlement versions from enabled Marketing and Integrations package checks are required; read-only or suspended access cannot prepare an effect. PostgreSQL independently rechecks the active campaign/release and exact version, unexpired Attention approval, release channel, current connector revision and credential generation. A canonical content-free manifest over release/version, capability, frozen connector authority and sorted immutable asset digests supplies the execution SHA-256 idempotency boundary without storing creative content, audiences, broker references or provider credentials. Exact retries replay even after later connector change; altered request reuse conflicts. Applied PostgreSQL coverage prepares rather than seeds the record and proves queueing plus the full uncertainty path. Construction step 4 is complete; broker handoff and worker/mock/provider adapters remain, so `integrations` and `marketing` stay non-executable.

Integrations-worker-kernel checkpoint (2026-08-23): a dedicated connector-execution service and PostgreSQL claim/completion adapter now validate every frozen execution binding, require current authority before payload reconstruction, keep provider material only in connector-runtime memory and route closed capabilities to bounded connectors. Execute and reconciliation are separate; only reconciliation may prove `not_applied` and schedule retry, while malformed post-call results become unknown. Pre-call authority/manifest/connector absence is a definite no-effect failure, and settlement uses an independent bounded context so an unsettled lease falls back to unknown reconciliation. A thread-safe no-network mock scripts success, definite failure, ambiguity and reconciliation absence. Pure tests and the applied PostgreSQL path cover the contract. Step 5 remains open for the dual-package/placement/health authority client, manifest source, one-operation secret broker, bootstrap and local Docker wiring; `integrations` and `marketing` stay non-executable.

Integrations-health-claim checkpoint (2026-08-23): cell migration 63 replaces the execute-only claim boundary so every execute and reconcile attempt atomically selects the latest health observation at or before claim time for the exact frozen connector revision, credential ID and credential generation. Healthy and degraded observations admit work only for five minutes. Missing, stale or unavailable health postpones the private queue by 30 seconds without returning provider work, changing the execution state or consuming an attempt; durable release, approval, connection and credential invalidation retain their existing cancel/manual-resolution behavior. Fresh PostgreSQL 17 coverage proves stale and unavailable deferral followed by fresh degraded-health admission. The worker still needs current global package/placement authority, payload reconstruction, one-operation secret brokering and bootstrap wiring before either package can execute.

Integrations-current-authority checkpoint (2026-08-23): the connector worker now has a concrete global workload authority adapter that reads one Account/entitlement snapshot per claim. It requires the active Account to remain assigned to the worker's exact cell with a positive placement generation and matching entitlement version, and requires exactly one enabled Marketing and Integrations package. Read-only, suspended, absent, duplicate and malformed package state plus cross-cell placement fail closed before payload reconstruction. Tests prove single-snapshot admission and each denial path. Manifest reconstruction, one-operation secret brokering, bootstrap and Docker composition remain before package execution can open.

Integrations-canonical-manifest checkpoint (2026-08-23): delivery preparation now calls the same validated byte-level manifest builder reserved for connector-runtime reconstruction. The builder validates every frozen release/connector/credential identity, requires one-to-100 unique immutable asset revision digests, sorts them by identity and emits fixed-order JSON before SHA-256. A golden-byte test prevents serialization drift, and fresh PostgreSQL coverage prepares from a genuine content-bound Marketing asset fixture. Runtime creative-object loading and provider-payload assembly, one-operation secret brokering, bootstrap and Docker composition remain; the packages stay non-executable.

Integrations-delivery-payload checkpoint (2026-08-24): a repeatable-read Account-RLS source now restores the exact approved release/version/approval, frozen connector revision and immutable Marketing asset revisions for a claimed execution. The bounded assembler restores typed scope, sorts assets, reads each object only through an opaque content boundary, enforces aggregate and exact per-object byte counts, verifies SHA-256, rebuilds the shared manifest and rejects any claimed-digest drift. Its versioned provider envelope carries only idempotency identity, capability, non-secret scope and verified creative bytes; object references and credentials do not escape. A copy-on-open no-network local content source and race tests cover deterministic certification, while fresh PostgreSQL coverage proves exact snapshot loading and stale-release rejection. At this checkpoint production immutable-object admission/reading, one-operation secret brokering, bootstrap and Docker wiring remained; the later connector checkpoints close that construction work.

Integrations-one-operation-credential checkpoint (2026-08-24): after authority and payload verification, the connector service now acquires a lease bound to the exact Account, execution, attempt, mode, capability, connection, credential identity/generation and database lease expiry. At most 64 KiB is copied into one connector call, the connector-visible slice is zeroed immediately after return, and the broker lease closes on normal and panic paths. Broker denial is a definite no-effect failure and never invokes the connector; lease-release failure is surfaced without rewriting a provider outcome. The deterministic local broker copies configured material, rejects generation drift and records only content-free identities. Tests prove denial, release and zeroing. At this checkpoint production secret/object adapters, worker bootstrap and Docker wiring remained; the later connector checkpoints close that construction work.

Integrations-worker-bootstrap checkpoint (2026-08-24): a dedicated per-cell bootstrap now composes separate bounded global/cell pools, exact-cell current authority, execute-only repository, repeatable-read payload reconstruction, injected one-operation broker/content readers and closed connector definitions. It owns bounded polling, database readiness, content-free processed/failure status and cancellation/shutdown, with no generic app, browser, tool-router, Agent-runner or arbitrary egress fallback. Unit and race tests cover fail-fast configuration, work/error accounting and idle cancellation. At this checkpoint executable mock configuration, restore-gated command wiring, ubunturojo Docker certification and production object/secret adapters remained; the later connector checkpoints close that construction work.

Integrations-local-execution checkpoint (2026-08-23): the process inventory now includes a restore-gated `integration-connector-worker` with separate bounded global/cell pools and an adapter selector that rejects the mock outside `local`/`local-secure`. Strict bounded JSON rejects unknown/trailing configuration and constructs only copied no-network content, one-operation credentials and closed email/web scripts. The opt-in Compose profile runs one internal-network worker per cell under non-owner, non-`BYPASSRLS` roles. A repeatable ubunturojo gate seeds an isolated active Account with enabled Marketing/Integrations authority and one frozen approved delivery, then proves the real worker settles exactly `execute:succeeded`; global User access and direct cell credential-metadata access are denied. This certifies local execution only. Production immutable-object, secret-broker and provider adapters remain, so the Marketing and Integrations package inventories stay non-executable.

Integrations-HTTP-read checkpoint (2026-08-23): five generated routed operations now expose Account-bound connection pages, current immutable non-secret scope plus latest health, content-free health history, filtered external-execution pages and exact ordered attempt detail through the canonical Integrations service. Connection detail carries a weak version ETag; three opaque cursor families reject cross-collection replay. Frozen payload SHA-256 values are canonical hex, while broker references, reference attestations, credentials, creative bodies and provider request/response material remain absent. The app-api bootstrap now composes the classified Integrations repository/service, and contract tests prove exact query binding, cross-Account concealment and typed OpenAPI outcomes. The customer contract now contains 166 typed operations. Construction step 6 remains open for governed mutations, MCP and the private browser workspace; production object/secret/provider adapters also remain, so both packages stay non-executable.

Integrations-HTTP-lifecycle checkpoint (2026-08-23): eight generated human-manager commands now route connection create/revise, opaque credential-attestation activation/rotation, disable/enable/revoke and exact Marketing execution preparation through the same classified Integrations service as persistence and reads. Each command binds signed route authority to the exact UUID idempotency key; every connection transition requires a weak current-version ETag. Closed JSON accepts no broker reference or provider secret, only a provider code and lowercase SHA-256 reference attestation. Creation/preparation return canonical locations, and connection results return new version ETags. Contract tests cover all eight typed outcomes plus idempotency mismatch and missing-version failure; the customer contract now contains 174 operations. The HTTP portion of construction step 6 is complete for the current delivery lifecycle. MCP, private UI, dual-controlled execution manual resolution and production object/secret/provider adapters remain, so Marketing and Integrations stay non-executable.

Integrations-MCP checkpoint (2026-08-23): thirteen typed tools now mirror the five routed reads and eight governed lifecycle commands over the canonical Integrations service. The global MCP registry explicitly classifies each tool under the Integrations package and mutation policy before routing; the cell repeats Account/package authorization. Operation UUIDs, optimistic versions, closed filters, opaque kind-bound cursors and lowercase SHA-256 credential attestations preserve HTTP semantics. Execution outputs contain frozen authority and content-free attempt evidence, never broker references, creative/provider payloads or credentials. Protocol tests prove complete deterministic schemas/annotations, exact mutation classification, command/query mapping, digest decoding, safe version failure and backend-detail redaction. The shared app-api MCP composition includes Integrations. Private UI, dual-controlled execution manual resolution, applied external-client conformance and production object/secret/provider adapters remain; both packages stay non-executable.

Integrations-recovery/private-application checkpoint (2026-08-23): migration 64 and the canonical service now close uncertain external effects through a replay-safe two-person record rather than another provider call. One current human Owner/Administrator proposes only `succeeded` or `failed`; Spyglass retains the trimmed evidence SHA-256, immutable requester and content-redacted event. A different currently authorized manager confirms the exact record, appends independent evidence and atomically settles the execution terminal. Same-actor confirmation, altered replay, stale state and direct evidence mutation fail. Forced RLS, dynamic Account movement and exact erasure include the ninth Integration table. Two generated HTTP commands raise the customer contract to 176 operations; two matching, globally classified MCP mutations raise Integrations to fifteen tools. `/app/integrations` now provides selected-Account connection scope/health/lifecycle, execution preparation/evidence and dual-control recovery, while read-only mode omits mutations and locked Accounts receive no script. Fresh PostgreSQL 17 in ubunturojo proves replay, distinct confirmer, terminal settlement, immutable evidence, event count, RLS and erasure; template/client tests and JavaScript syntax validation cover the private boundary. Current-lifecycle construction step 6 is complete. Production object, environment secret-broker and real provider adapters plus applied provider/external-client certification remain, so Marketing and Integrations stay non-executable.

Integrations-secret-attestation checkpoint (2026-08-24): cell migration 65 extends the execute-only claim boundary with the exact non-secret credential provider and SHA-256 of the opaque broker reference selected by the same transactional authority check. The worker remains denied direct credential-registry reads, and the one-operation request now carries both values. A strict reload-on-acquire mounted broker matches Account, credential, generation, provider and reference digest before opening one relative in-root secret file; it rejects unsafe material permissions and oversized material, supports projected-volume symlinks, and zeroes the lease. This gives Hostinger Docker and LKE projected/CSI secrets one vendor-neutral production boundary. At this checkpoint the adapter remained deliberately uncomposed pending immutable Marketing objects and real provider definitions; the later connector checkpoints compose that boundary while keeping package activation gated on applied certification.

Marketing-immutable-object-kernel checkpoint (2026-08-24): a governed admission service now authorizes and validates an exact creative revision, hashes and bounds its body, writes one create-only derived Account/campaign/asset/revision key, persists only a canonical immutable-version reference and deletes a newly created orphan if PostgreSQL fails. Migration 66 and the domain share the connector's 16 MiB ceiling. The production S3/MinIO adapter writes Account/kind/digest metadata and the connector content reader independently re-derives the key and requires exact version, size, digest, Account and object kind. App-api policy permits only the Marketing content key family; document-worker Marketing writes remain denied. HTTP, MCP, Agent and browser asset creation now share this admission boundary, and connector workers use a separate exact-version read-only object identity. Unit, real MinIO, policy and local worker certification pass; applied provider certification remains required before Marketing and Integrations become executable.

Marketing-human-object-admission checkpoint (2026-08-24): app-api now composes the governed creative admission service over its existing narrow S3/MinIO credential. The routed human asset operation is multipart and computes content size/digest/object identity server-side; its OpenAPI/TypeScript contract no longer accepts content references, hashes or byte counts. The global router grants the larger body limit and two-minute client only to the exact Account Marketing asset route. `/app/marketing` now selects a creative file and sends `FormData`, with no internal object metadata fields. Route, OpenAPI, template, JavaScript and object tests pass. At this checkpoint MCP/Agent admission and the distinct connector read identity remained; the next two checkpoints close both gaps.

Marketing-Vue checkpoint (2026-08-24): `/app/marketing` and durable campaign/release routes now close the final legacy-only UI6 package migration. The mobile-first Vue workspace uses the generated HTTP types for paged campaign, immutable creative and exact release inspection; member drafting; manager governance; multipart creative admission; and the submit/cancel/activate/pause/complete/archive lifecycle. It never exposes the internal release-approval command: submitted consequential work routes through Your Turn, while activation remains explicitly separate from Integration delivery. Read-only package mode removes all mutations, stale snapshots are identified before submission, release construction freezes current campaign channels/version and selected creative revisions, and exact failed retries retain one operation identity. Focused client/component tests cover durable routing, package/role authority, multipart safety and exact snapshot construction; applied UI7 browser/device/accessibility certification remains open.

Agents-Persona-Vue checkpoint (2026-08-25, corrected 2026-08-26): the durable Boardroom Vue route now closes Persona authoring and immutable successor publication over the generated Agent boundary. Owners/Administrators select only the backend allowlisted read/additive tool grants and consequential proposal capabilities; the editor supplies their closed input schemas, preserves current Persona identity/latest-version fencing, and exposes a first-class five-level complexity choice plus bounded token/cost/tool-step controls. Provider, model, fallback and reasoning targets are neither accepted from nor returned to customer clients. Members retain Run authority but no longer see Boardroom creation, Persona publication or synthesis-manager controls that the backend correctly denies. Exact failed publication retries retain one operation identity, package read-only mode is inspection-only, and the UI states that action proposals remain inert until exact human approval in Your Turn. Focused client/component tests cover replay identity, policy shape, new publication and role separation; applied Runner/browser/device/accessibility evidence remains open.

Marketing-MCP/Agent-object-admission checkpoint (2026-08-24): every remaining creative ingress now reaches the governed immutable-object admission service. The MCP draft tool accepts only canonical base64 content up to 512 KiB and its generated schema exposes no reference, digest or size input. The runner asset tool is narrower: at both global router and cell boundaries it accepts only 64 KiB of valid UTF-8 copy, fixes `copy`/`text/plain`, derives Agent provenance from the authenticated invocation plus supplied Run and rejects the legacy metadata shape. The application service additionally rejects every non-canonical object-version reference, leaving admission as the sole new-record source. Focused schema, canonical-decoding, router and transport tests pass. At this checkpoint the distinct connector read identity and real provider certification remained; the object-policy and provider checkpoints close construction while applied certification remains open.

Integration-connector-object-policy checkpoint (2026-08-24): local/Stage topology now provisions a fourth independent object identity exclusively for connector workers. Its policy permits bucket-versioning inspection and exact-version reads only under derived Marketing creative keys; bucket listing, Knowledge reads, all writes and all deletes are denied. Connector services receive only this credential, Stage secret preparation generates or preserves it separately, and the process inventory records the versioned-object-store dependency. Rendering Stage also exposed and closed an older omission: Stage now generates the connector database password/URLs and grants that role in both global and cell provisioning. The S3 adapter adds a listing-free read-only readiness check. Real MinIO policy and Go integration certificates prove app-api write, connector exact read, document-worker Marketing denial and every connector negative boundary. The later provider and health checkpoints compose production adapters; applied certification still gates Marketing and Integrations execution.

Finance-scope/kernel checkpoint (2026-08-22): [ADR-0006](decisions/0006-finance-operational-ledger.md) resolves Finance as an Account-owned internal operational ledger rather than subscription billing, a payment rail or an accidental formal accounting standard. The provider-free typed kernel now validates one-currency integer-minor-unit money, overflow-safe balanced lines, optimistic ledger/account lifecycle, evidence-bound monotonic period close, human-only posting/reversal, immutable linked reversal shape and evidence-bound reconciliation that cannot conceal a difference. Agents may prepare drafts only with exact Run/invocation provenance and cannot post. Pure tests cover currency, overflow, balance, roles, evidence, closed periods, stale versions, reversal and mismatch. Forced-RLS persistence and routed surfaces are the next Finance slice. See [Finance module](finance-module.md).

Finance-persistence checkpoint (2026-08-22): migration 56 adds ten Account-owned forced-RLS tables with composite ownership keys, per-ledger numbering, redacted immutable events, movement fencing and exact erasure accounting. PostgreSQL independently validates currency, open period, active posting accounts, two-to-100-line debit/credit equality, overflow-safe totals and Evidence before draft-to-posted transition. Posted rows and children are immutable; one linked reversal is the only allowed posted transition. Reconciliation binds external/calculated balances and cannot transition a discrepancy to confirmed. A fresh PostgreSQL 17 Docker test proves posting, mutation denial, reversal, mismatch, evidence-backed close, closed-period rejection and cross-Account denial. The classified repository/application boundary remains next.

Finance-application checkpoint (2026-08-22): the classified repository and package-authorized service now cover exact-replay Ledger/posting-account/entry creation, human-only posting and reversal, and reconciliation/confirmation. All writes use serializable Account transactions and immutable request/event UUIDs; retries return the original result without consuming a second per-ledger number. Reversal inserts/posts the swapped entry and links the original atomically with redacted events for both. The repository calculates reconciliation balance from immutable posted/reversed lines and account normal balance rather than accepting it from HTTP/MCP, detects aggregate overflow, retains explicit discrepancies and permits only one confirmed record per exact date. Containerized PostgreSQL tests prove replay, restoration, numbering, post/reverse, mismatch correction and cross-Account denial; service tests prove member-versus-manager authority and deterministic reversal IDs.

Finance-lifecycle checkpoint (2026-08-22): Owner/Administrator Ledger revision, evidence-backed monotonic period close and ordered archival are implemented through the same classified serializable boundary. Member-authorized draft revision atomically replaces balanced lines and Evidence, rejects a closed-period date and preserves provenance; posting/reversal remain Owner/Administrator-only. Posting-account revision permits hierarchy changes only to active parents in the same Ledger and transactionally rejects cycles. Archival refuses an account with active children or draft references and refuses a Ledger until all posting accounts are archived and no draft remains. Every transition uses an expected version, immutable content-redacted event UUID and exact retry replay. Focused service tests and fresh containerized PostgreSQL 17 tests cover command derivation, lifecycle restore/replay, draft replacement, cycle denial, draft/child/archive guards, trigger-ordered close evidence and final Ledger archival. Stable query pages, summaries and routed Finance surfaces remain.

Finance-query checkpoint (2026-08-22): the canonical read boundary now exposes package-authorized Ledger, chart, journal and reconciliation detail/pages with defaults, strict maximums and structurally complete cursors. Ledger summaries derive account/draft counts plus income, expense and net values; chart summaries derive balances from immutable posted/reversed lines and each account's normal-balance rule. PostgreSQL numeric aggregates are range-checked before conversion to signed minor units. Stable `(code,id)`, `(entry_date,entry_number)` and `(as_of,id)` keysets have matching indexes and containerized traversal tests, including restored posting-account/reconciliation detail and the zeroed balances produced by linked reversal. Generated HTTP/MCP contracts and private Finance surfaces remain.

Finance-HTTP-read checkpoint (2026-08-22): eight generated OpenAPI operations now route Ledger, posting-account, journal-entry and reconciliation detail/pages through the canonical Account-authorized Finance service. Every target is bound to routed Account authority; detail resources publish weak version ETags; page cursors are opaque, versioned and structurally bound to their stable keyset so cursor families cannot be exchanged. Contract tests validate all four page families, cross-Account concealment and malformed cursor rejection, while generated Go and TypeScript artifacts pass drift checks. Governed HTTP commands, MCP and private Finance surfaces remain.

Finance-HTTP-command checkpoint (2026-08-22): thirteen generated operations now expose the complete governed Ledger, chart, journal and reconciliation lifecycle through the same application service as persistence. Signed route authority and exactly matching `Idempotency-Key` values identify every mutation; existing aggregates additionally require weak `If-Match` versions. Human-manager-only close/post/reverse/confirm authority remains in the canonical service, while the transport strictly decodes bounded manual-draft and evidence inputs, publishes new versions and canonical locations, and returns both sides of an immutable reversal. Contract tests exercise all thirteen command routes, cross-Account concealment, missing preconditions and routed-operation mismatch; all outcomes validate against OpenAPI and generated Go/TypeScript contracts pass drift checks. MCP and the private Finance application remain.

Finance-erasure certification checkpoint (2026-08-22): the complete cell-erasure suite now inventories and seeds all ten Finance tables, grants their access only to the security-definer erasure role, attests exact tombstone counts, proves no Finance row survives for the erased Account, preserves a control Account and retains replay/concurrent convergence. A fresh isolated PostgreSQL 17 run passes the complete migrations suite with migration 56 included in the immutable ledger expectation.

Finance-MCP checkpoint (2026-08-22): 21 typed tools now provide eight reads and all thirteen governed mutations over the same Finance service as HTTP. Each call rebinds OAuth actor, Account and Finance-package authority; commands preserve operation UUIDs and optimistic versions; pages use bounded, versioned opaque cursors. MCP-created journal drafts are explicitly marked with `mcp` provenance and never post implicitly. Tool annotations identify irreversible close/archive/post/reversal operations, while safe errors cannot leak persistence details. Protocol tests prove deterministic complete schemas, routed claims, command mapping, cursor opacity, missing-version rejection and backend-error redaction. The shared production MCP bootstrap and private Finance application remain.

Finance-private-application checkpoint (2026-08-22): `/app/finance` now provides the selected Account a discoverable package-aware workspace over the generated HTTP boundary. Ledger summaries, chart balances, journal history and reconciliations remain available in read-only mode, while enabled mode exposes Ledger create/revise/evidence-backed close/archive, posting-account create/revise/archive, balanced draft creation, deliberate post/reversal and reconciliation create/confirm. Existing-resource commands use freshly fetched ETags, every mutation supplies a fresh route-bound idempotency UUID, and irreversible actions require explicit user confirmation. Customer values are rendered through DOM text nodes, no session or draft material is placed in browser storage, and command outcomes use an assistive-technology live region. Template/client tests prove package/read-only gating, lifecycle endpoint coverage, version preconditions and DOM/storage safety; the complete Go suite, focused vet and JavaScript syntax check pass. Shared production MCP bootstrap, Agent/Attention Finance proposals, prototype transformation and applied browser/accessibility certification remain.

Finance-Agent-draft checkpoint (2026-08-22): published Personas may now receive exact Ledger-list, chart-list and balanced-draft capabilities. The runner broker classifies the first two as read-only and draft creation as additive, while the global tool router reauthorizes current Finance package mode and Account placement on every call before minting a short-lived cell proof; runner Bearer credentials never cross that boundary. The private cell command fixes `agent` provenance, derives invocation identity from the authenticated runner and accepts only a cited Run. Migration 57 adds a composite `(Account, invocation, Run)` foreign key, so individually valid but mismatched provenance cannot be persisted. Workloads remain categorically unable to manage Ledgers/accounts, reconcile, post or reverse. Persona/OpenAPI capability allowlists, transport binding, spoof rejection and additive auditing are tested; a fresh PostgreSQL 17 container passes both the focused provenance test and the complete migrations suite. Finance is now marked executable across browser, HTTP and Agent-tool boundaries. Attention-governed posting proposals, shared production MCP bootstrap, prototype transformation and applied certification remain.

Finance-Agent-posting checkpoint (2026-08-22): tracing the existing consequential-action path found that approval created an immutable authorization but no process consumed it after the ephemeral runner exited. It also found that the 24-hour durable proposal window extended beyond the originating runner exchange, while the old projector incorrectly required the former to fit inside the latter. Migration 58 and the runner broker now provide the missing durable post-approval worker and remove that impossible credential-lifetime coupling while preserving exact invocation/digest binding. Its content-free queue is claimable across Accounts without table or BYPASSRLS access; a security-definer claim restores the exact Attention payload under Account RLS, and the application re-hashes it before any handler call. Execute admission occurs once, lost leases and unknown outcomes are reconciliation-only, retries are delayed and unresolved uncertainty stops after three total attempts for dual-control resolution. `finance.entry.post` is now an allowlisted proposed-action kind, never a runner tool: the approving Owner/Administrator becomes the posting actor, the approval operation UUID is the Finance event/idempotency UUID and reconciliation is an exact side-effect-free event lookup. Fresh PostgreSQL 17 tests prove approval beyond runner expiry, execution after runner termination, execute-versus-reconcile transitions, early/concurrent exclusion and bounded unknown handling. Finance is now executable across browser, HTTP, Agent-tool and approved-action-worker boundaries. Shared production MCP bootstrap, prototype transformation and applied stage/browser certification remain.

MCP-routing checkpoint (2026-08-22): all 53 then-existing Attention, recovery, Knowledge, Baseline and Finance tools gained one reviewed global package/mutation classification and were composed into app-api only behind its private one-use route-proof boundary. A separate global gateway/resource-server constructor uses only the global access/directory credential, current Membership/package/placement state and route-signing authority; it never receives a cell database credential. Its Account-scoped public transport validates RFC 9728 audience/scope input and protected-resource metadata, current `2026-07-28` metadata headers against the exact JSON-RPC body, then signs and forwards that unchanged body over the private workload transport without forwarding the customer Bearer token. Tests prove cross-Account and unknown-tool pre-routing rejection, header/body mismatch rejection, exact Finance mutation classification, token non-passthrough, body/path proof binding and replay denial. The subsequent OAuth/deployment checkpoint completed the executable production composition.

MCP-OAuth/deployment checkpoint (2026-08-22): the existing app origin now publishes OAuth authorization-server metadata plus same-session consent, authorization-code exchange, rotating refresh, revocation and current CIMD discovery. Exact S256 PKCE, redirect, audience, scope and issuer binding are enforced. Only opaque credential digests reach PostgreSQL; one-use codes, 15-minute access tokens, 30-day rotating refresh families, replay-family revocation and User security-version invalidation are transactional. Separate durable network-actor budgets protect authorization, token and revocation endpoints across replicas. Identity Security lists only currently usable client grants and can atomically revoke a grant plus every credential family. Malformed public-client requests, unsupported MCP methods, one-retry/fresh-proof behavior and cancellation are covered; a race-enabled 128-request gateway certificate and a two-service PostgreSQL visibility contract exercise local concurrency and replica sharing. A dedicated least-privilege `mcp-gateway` mode serves the public resource with its own database role and client-only cell identity. Local/Stage Compose, Hostinger routing, LKE workload/ingress/network-policy references and process inventory are aligned, and local Docker proves both discovery documents plus the unauthenticated challenge. The surface is executable; applied external-client acceptance remains separate release evidence.

MCP-Stage activation checkpoint (2026-08-22): admitted RC.2 is deployed from clean checkout `773f3c45cc3fb351aec44d6e52a6d279d3b5d4bb` with generated secret set `2026-08-22-03`, migrations 33-34, the dedicated database role/certificate and two healthy gateway replicas. Public OAuth and protected-resource metadata, TLS/HSTS and the RFC 9728 challenge pass. The RC.2 anonymous boundary passes 11/11 checks, and the public MCP edge passes four 128-request/16-concurrency legs with both replicas, each replica removed in turn and both restored. A rejected 32-concurrency calibration with nine timeouts remains recorded rather than being hidden. The remaining MCP gate is interactive external-client consent, authorization-code exchange, routed tool call, refresh rotation and revocation; it is an acceptance gate rather than a missing executable surface.

Integration-provider construction checkpoint (2026-08-23): the dedicated connector runtime now has two real closed provider adapters in addition to its no-network mock. Provider code `smtp` uses implicit TLS, exact sender/audience material, deterministic Message-ID and conservative DATA uncertainty that can never authorize an automatic resend. Provider code `web_https` uses an exact scope-bound create-only resource, bearer-header-only authority, public-address DNS pinning, redirect denial and response-digest verification; only an exact HEAD digest or `404`/`410` can settle reconciliation or prove non-application. `SPYGLASS_CONNECTOR_ADAPTER=production` is accepted only in Stage/preproduction/production and composes the existing exact-version S3 reader plus mounted one-operation broker. Hostinger Compose and the two-cell LKE reference mount provider material only into connector workers and add bounded provider egress. Protocol/unit, render and least-privilege object-policy gates are constructed; applied disposable SMTP/HTTPS, recovery and erasure evidence remains before Marketing/Integrations become executable.

Integration-health construction checkpoint (2026-08-23): the execution health gate now has its missing producer. Cell migration `000067` adds a lease-fenced derived probe queue and execute-only claim/completion functions; the connector worker repeats current package/placement authority, acquires the exact attested credential, runs non-mutating SMTP `NOOP` or authenticated web `HEAD`, and appends an immutable content-free observation. Credential or revision changes fence stale completion. Fresh PostgreSQL coverage proves claim/completion fencing, portability classification, movement regeneration and exact erasure. Applied disposable-provider certification remains open.

Google-Drive-scope checkpoint (2026-08-23): the previously named but fail-closed connector kind now has its first reviewed authority shape. One immutable `google_drive.read` revision freezes one-to-fifty bounded, unique, canonically sorted opaque folder IDs and excludes folder names, file IDs, OAuth material and sync cursors. Cell migration `000068` independently enforces kind/capability separation and canonical folder order; repository restoration, the complete Account-portability column certificate, generated HTTP/TypeScript contract, MCP input and private Integrations form share the same field. Pure, transport and fresh PostgreSQL tests pass. This scope becomes the authority consumed by the following exact Baseline source-grant binding; OAuth state/callback brokering, token rotation/revocation, incremental capture, document admission/deletion and provider health remain before Google Drive is executable.

Google-Drive-source-grant-binding checkpoint (2026-08-23): a Baseline source grant can no longer name an arbitrary connector identity or widen Google Drive folder authority. Before persistence, the application resolves the active current Integration revision under Account RLS and requires the matching read capability; every Drive grant folder must be a member of the connection's immutable canonical folder scope. Cell migration `000069` repeats the rule in a database trigger so direct writes fail closed, while preserving the authorized Account-movement path and exact erasure accounting. Focused lifecycle/erasure tests, the complete fresh PostgreSQL migration suite, all Go tests, `go vet` and generated API-contract verification pass. The future capture worker must recheck the still-active connection and grant before accepting provider content; OAuth/credential brokering, cursors, captures, admission/deletion and Drive provider health remain before the connector becomes executable.

Google-Drive-sync-persistence checkpoint (2026-08-23): migration `000070` constructs the private queue and immutable provenance boundary needed by the future Drive worker. One derived lease record per active Drive grant holds only bounded cursor ciphertext/digest and scheduling state; execute-only claim and completion functions independently recheck the active grant, current `google_drive.read` revision, exact folder subset, active credential generation and lease before authority or cursor advancement. Completion accepts at most 100 content-free receipts binding provider object/revision digests to one authorized opaque folder and exact Knowledge document/revision/content digest. Capture history is forced-RLS, movement-fenced, immutable, exactly erased and portable with credential identities withheld; cursor state is secret, non-portable and regenerated after movement. The dedicated connector database role receives only function execution. Pure validation and fresh PostgreSQL lifecycle tests prove bounded canonical authority, paginated resume, receipt immutability and revocation fencing, and the complete migration/portability suite passes. Cursor sealing, Google OAuth/API adapters, capture-worker composition, immutable-object admission, deletion and Drive health remain before execution.

Google-Drive-worker-kernel checkpoint (2026-08-23): the provider-neutral sync service now claims the exact persistence lease, reauthorizes current cell placement and enabled Knowledge/Integrations packages, leases the attested Drive credential under a purpose that cannot execute email/web effects, opens a grant-bound sealed cursor and accepts only a bounded canonical page from frozen folders. It hashes provider identities before the sink boundary, limits content to the Knowledge source ceiling, requires exact capture receipts before sealing/advancing the next cursor and wipes credential, cursor and content buffers. A new AES-256-GCM codec loads only an owner-readable non-symlink cell key, binds ciphertext to Account plus grant and rejects tamper, digest drift and cross-grant replay. Pure tests prove capture-before-advance, malformed/out-of-scope refusal, buffer wiping, package/placement fencing and broker-purpose separation. The service remains deliberately uncomposed until the Google OAuth/API adapter and replay-safe Knowledge revision/deletion sink are constructed; those, Drive health and Stage fixtures still gate execution.

Google-Drive Knowledge-revision checkpoint (2026-08-23): the canonical Knowledge admission boundary now supports deterministic immutable revisions of an existing ready document. It repeats package/role authorization and source verification, derives the quarantine key, writes create-only source content and removes a newly created orphan when serializable PostgreSQL admission loses a race. Exact replay returns the existing revision; changed replay conflicts; and both application and database boundaries refuse a second pending revision. The admitted source still passes through the ordinary malware scan, extraction, indexing and publication lifecycle before retrieval. Pure and fresh Docker-backed PostgreSQL tests exercise replay, concurrency fencing, cleanup, a complete second revision and exact-version deletion, and the full local test/race/migration gate passes. The provider-to-Knowledge capture/deletion sink, automatic publication policy, Google OAuth/API adapter, Drive health and Stage fixtures remain, so the sync worker is still uncomposed and Integrations remains non-executable.

Google-Drive Knowledge-capture checkpoint (2026-08-23): the replay-safe sink now maps hashed provider objects to stable connection-local Knowledge documents, hashed provider versions to immutable revisions and grant/operation tuples to distinct capture receipts. Initial and changed bytes use canonical verified immutable-object admission; deletion requires the exact latest terminal revision/content binding, while pending or deleting-document drift leaves the cursor unchanged. The Knowledge processor auto-publishes only revisions created by `integration-source-sync` and only after malware scan, extraction and indexing; human and prototype-migration sources retain explicit publication. Failed provider revisions remain immutable but no longer deadlock future capture: a later clean revision may advance across failed revision numbers. Pure, domain, application and fresh PostgreSQL tests cover cross-grant convergence, replay, update, exact deletion, workload-only publication and failed-version recovery. Google OAuth/API, the initial-crawl/change-feed contract, Drive health, worker composition, egress/secrets and local/Stage fixtures still gate execution.

Google-Drive prior-membership checkpoint (2026-08-23): official Drive v3 removal entries can omit the file resource and former parent folder, so migration `000071` adds one execute-only lookup that accepts only the hashed provider object under an exact live sync lease. It rechecks the active grant, connection, current `google_drive.read` revision and folder subset, and returns at most the last authorized opaque folder when the latest capture was admission. The worker uses it to turn known removals into exact Knowledge deletions; unknown removals have no Spyglass state, produce no receipt and may safely advance, while lookup/lease/authority failure leaves the page unsettled. Pure and fresh PostgreSQL tests cover known, unknown and revoked cases. Both persistent local cells applied migration 71; the connector role can execute the function but still cannot read capture or cursor tables. Google OAuth/API, Drive health, worker composition and environment fixtures remain.

Google-Drive provider-execution checkpoint (2026-08-23): the per-cell connector runtime now composes the real bounded Drive v3 adapter, sealed cursor, lease-fenced source-sync repository and canonical Knowledge admission/deletion sink. It freezes a change token before exact direct-child folder crawl, never follows shortcuts, bounds pages and content, exports supported Workspace documents to PDF, and processes incremental updates, moves, trash, access loss and parentless removals without advancing the cursor before durable capture settlement. Fixed Google token/API origins, redirect denial, a restrictive OAuth-client file, broker-leased refresh material and content-free health keep credentials and provider identifiers out of durable/operational surfaces. The same health path verifies change-feed access and every frozen folder. Local Docker uses a no-network provider fixture and fixture-only cursor key; production-shaped manifests mount independent cursor/OAuth material and the connector object identity now has exact source create/read/delete permission while retaining bucket-list, derived-object and Marketing-mutation denial. Removed then restored content becomes a new immutable ready revision without rewriting deleted history. The full local certificate passes ordinary/race Go suites, all 109 migrations, vet, formatting, generated API drift, object/process policies and website checks. Product-owned Google consent/callback/code exchange, durable secret rotation/revocation and its deterministic protocol fixture remain the next backend checkpoint; no Stage environment was mutated.

Integration-OAuth-session foundation checkpoint (2026-08-23): LB1 now has a one-use manager-owned authorization aggregate bound to the exact Account, Drive connection/current revision, Google provider, Drive-read scope revision, exact callback, 15-minute expiry, state digest and S256 PKCE digest; raw protocol secrets and tokens cannot enter it. Migration `000072` adds forced-RLS sessions plus immutable content-free events, independently rechecks current Drive authority, permits only pending-to-exchanging-to-terminal transitions and binds completion to one exact opaque credential identity/generation through a deferred foreign key. Account namespace fencing, erasure counts and explicit portability exclusion are constructed. Fresh PostgreSQL proves illegal state rewind denial, atomic credential-bound completion, cross-Account concealment and event immutability; ordinary Go tests, vet, formatting and the 110-migration ledger pass. The encrypted secret-store adapter, Google exchange/revocation orchestration, rotation fencing, public callback and canonical HTTP/MCP lifecycle operations remain before LB1 completion.

Integration-encrypted-secret-store checkpoint (2026-08-23): LB1 now has an encrypted provider-secret adapter for local product operation. AES-256-GCM sealed files bind ciphertext to the exact authorization or credential-generation path; the root, key and material files require restrictive non-symlink permissions. Cross-process locking, atomic synchronized replacement and exact document comparison make creation replay-safe without leaving a stale crash lock. PKCE verifiers expire and delete independently. Credential leases repeat the exact Account, operation, connection, generation, provider, reference digest, purpose, capability and expiry boundary; rotation or revocation fences a generation before idempotent purge. Focused race tests prove no plaintext at rest, ciphertext-tamper denial, exact replay/conflict, expiry, lease zeroing, ended-state monotonicity and purge replay. The existing mounted broker was also corrected and covered so Drive credentials support sync and non-mutating health but remain impossible to lease for delivery execution. The complete ordinary Go suite passes. Google consent/code exchange, transactional lifecycle orchestration, public callback, canonical HTTP/MCP operations and a deterministic OAuth fixture remain before LB1 completion.

Integration-Google-OAuth-protocol checkpoint (2026-08-23): the product authorization boundary now has a fixed-origin Google client for exact offline S256 consent, authorization-code exchange and refresh-token revocation. Consent freezes the single Drive-read scope, disables incremental scope accumulation and repeats the exact callback. Token and revocation calls never follow redirects; callback URIs with credentials, query, fragment, traversal or non-loopback cleartext origins are rejected. Bounded strict token parsing requires a Bearer access result, durable refresh material and the exact single scope, while only refresh material crosses into the application secret boundary. Focused race tests cover exact fields, real Google-style code syntax, scope widening, provider redirects/outages, restrictive client files, unknown/oversized responses and replay-safe already-revoked handling; the complete ordinary Go suite passes. Transactional lifecycle orchestration, compensation, public callback, canonical HTTP/MCP operations and the containerized OAuth-to-Drive journey remain before LB1 completion.

Integration-authorization-begin checkpoint (2026-08-23): the product lifecycle now owns its first complete application operation. A current Integrations manager with recent passkey evidence can begin consent only for the current non-revoked Drive revision carrying the single Drive-read capability. The application freezes a canonical folder-scope digest and exact callback in a 15-minute session; the Account-RLS repository persists only state and S256 challenge digests plus a redacted start event. Raw state and verifier are encrypted together so an ambiguous store result, process interruption or exact idempotent retry recovers the same pair and reproduces the same consent URL. Changed actor/connection/callback reuse and expired material fail closed. Pure race tests cover strong authentication, exact authority, replay and ambiguous writes; fresh PostgreSQL 17 covers authority restoration, insert/replay/load, event comparison, one-use transitions, credential-bound completion, RLS and event immutability. The complete ordinary Go/vet/format gate passes. Callback claim/exchange, transactional activation/rotation with compensation, status/revoke operations, transports and the containerized journey remain before LB1 completion.

Integration-authorization-callback checkpoint (2026-08-23): the Account-routed callback now claims state once, freezes a non-secret credential-generation plan and marks provider exchange before network I/O. Migration 73 adds a forced-RLS recovery ledger and prevents competing connection state/revision/credential mutation until the workflow is terminal. Authorization codes are never automatically repeated after an unknown result: concurrent callbacks respect a two-minute exchange lease, an ambiguous successful vault write is recovered through exact credential presence, and a stale exchange without sealed material becomes `provider_exchange_outcome_unknown`. Successful first consent seals generation one and activates the connection; rotation seals N+1, fences N and records that fence before a serializable transaction rotates/rebinds the durable credential and completes the session. PostgreSQL failure after fencing remains convergent through `previous_fenced`. Pure race tests cover completion replay, generation-two fencing, ambiguous writes, stale outcomes and provider scope failure. Fresh PostgreSQL 17 proves activation, rotation, required prior fence, connection mutation exclusion, one-use state, RLS, exact portability omission and immutable redacted events across all 111 migrations. Status/revocation, lifecycle transports and the containerized OAuth-to-Drive journey remain before LB1 completion.

Integration-authorization-status/revocation checkpoint (2026-08-23): LB1 now has its complete application-owned credential lifecycle. The authorized status read exposes only safe identities, status, bounded error and timestamps, and serializably persists overdue sessions before deleting their encrypted verifier. Google `access_denied`, `server_error` and `temporarily_unavailable` callbacks become closed redacted terminal outcomes without a code exchange and replay exactly. Migration `000074` adds a forced-RLS recovery ledger freezing the exact active connection and credential generation. Revocation records provider confirmation before vault fencing and requires that fence before a guarded serializable transaction revokes the durable credential and connection; provider outage remains retryable with the credential still active, and exact completion replay cannot call Google twice. The vault then purges only unusable material. Unsettled authorization or revocation blocks competing connection authority changes. Focused race tests, the ordinary Go/vet/format gate and the complete fresh PostgreSQL migrations suite prove denial/expiry, outage retry, generation-two revocation, required ordering, RLS, portability omission and explicit exact-erasure coverage across all 112 migrations. HTTP/MCP lifecycle transports and the deterministic containerized OAuth-to-Drive journey remain before LB1 completion; no Stage or release environment was mutated.

Integration-OAuth transport/local-journey checkpoint (2026-08-23): LB1 is complete. Four canonical routed HTTP operations expose begin, callback, secret-free status and credential revocation over the application-owned lifecycle; three matching MCP tools expose only begin, status and revoke, so MCP cannot receive an authorization code or token. Recent passkey evidence is copied from the authenticated passkey session into the signed route context and is rechecked with current Integrations manager authority at the cell. The customer contract now contains 186 typed operations, and Integrations publishes 18 typed MCP tools when authorization support is configured. Local Docker generates distinct private 32-byte vault keys and serves a deterministic OAuth/Drive fixture through the Caddy-routed `oauth.infiniteocean.localhost` origin. `make verify-google-oauth` creates an ephemeral passkey session, creates a Drive connection, completes exact consent/S256 exchange/callback, reads safe status, revokes the provider material and proves durable connection revocation. The certificate also found and fixed sub-microsecond expiry drift between sealed Go timestamps and PostgreSQL `timestamptz`. Focused transport, fixture and race tests plus API drift and process-inventory gates pass. This work is local only: no Git push, GHCR release, Hostinger Stage mutation or LKE action occurred. LB2 still requires the complete OAuth-authorized Drive-to-Knowledge sync journey.

Integration-OAuth-to-Drive completion checkpoint (2026-08-24): LB2 is complete locally. The deterministic OAuth/Drive service is now consumed through the real Google token and Drive adapter by the composed per-cell connector worker. The journey creates an exact Baseline source grant, authorizes its Drive connection through consent/callback, leases only the resulting encrypted credential generation, admits exactly one immutable source capture and observes the ordinary Knowledge worker settle that revision through scan, extraction, indexing and source-workload publication. It then revokes the credential, proves the connection and queue are closed, and proves the capture remains immutable. Wider protocol and worker suites retain pagination, changes, moves, removal/access loss, restoration, Workspace export, bounded-content, health/outage and cursor-restart coverage. Certification found and fixed stale mock-worker competition, removed an invalid dependency on routed user authorization from the internal source-publication path, and added the narrow Account-RLS-protected document update privilege required by that worker. `make verify-google-oauth` passes the joined certificate. No Git push, GHCR build, Hostinger Stage mutation or LKE action occurred; inbound email capture is the next local backend construction block.

Integration-inbound-email-foundation checkpoint (2026-08-24): LB3 has begun but is not complete. Migration 75 generalizes the private source-sync claim/completion boundary to exact email grants with frozen mailbox folders and optional date bounds while retaining current revision, credential generation, revocation, cursor and immutable capture fencing. The application kernel now carries the claimed source kind/scope to the provider and leases `email.read` rather than hard-coded Drive authority. Encrypted and mounted brokers accept provider `imap` material only for read sync/health and reject it for send/reconcile effects. The Knowledge sink retains its committed Drive identity derivation and uses a distinct email namespace. Focused race tests and the complete fresh PostgreSQL suite pass across 113 migrations. The implicit-TLS protocol adapter, UIDVALIDITY/UID cursor, bounded MIME body/attachment admission, expunge/reset semantics, provider health and deterministic local TLS-mail certificate remain; no deployment environment was touched.

Integration-inbound-email completion checkpoint (2026-08-24): LB3 is complete locally. The implicit-TLS IMAP adapter consumes only exact email-read generations and frozen mailbox/date scope, pages through UIDVALIDITY plus UID cursors, admits a reviewed bounded MIME body/attachment subset into immutable Knowledge and keeps threading/source attribution out of operational surfaces. Cursor advancement follows complete capture settlement. Expunge, folder movement, UIDVALIDITY reset, malformed/oversized content, partial-page failure, provider outage and revocation fail closed without duplicating documents, revisions or deletion effects. The deterministic TLS fixture and `make verify-imap` certify initial/incremental capture, attachment, deletion/reset, outage and read/send credential separation. No deployment environment was touched.

Integration-web-research completion checkpoint (2026-08-24): LB4 is complete locally. Migrations 77-78 add immutable reviewed research scope and Account-owned capture evidence under forced RLS. A fixed-purpose Firecrawl adapter and hardened retriever enforce canonical HTTPS authority, public-address DNS pinning, redirect revalidation and bounded DNS/connect/header/body/decompression/type/time work. Search returns safe metadata; read freezes URL, retrieval time, digest, type and attribution before Knowledge admission. HTTP adds `integrationWebResearchSearch` and `integrationWebResearchRead`; MCP adds `spyglass_integrations_web_search` and `spyglass_integrations_web_read`, with read additionally requiring Knowledge mutation authority. Exact replay reuses one capture, changed content creates a new immutable revision, and research cannot become an accepted Fact without ordinary human governance. `make verify-web-research` passes the HTTPS, SSRF, health, replay, changed-content, export/erasure and secret-boundary certificate. The generated customer contract contains 188 operations and the complete cell MCP composition contains 89 tools.

Phase-3-local-backend-boundary checkpoint (2026-08-24): LB5-LB8 are complete for construction. The outbound connector certificate passes exact Marketing execution preparation, fresh health, execute settlement and database/object/credential least authority; real SMTP and web-HTTPS adapter suites retain conservative uncertainty and digest reconciliation. Marketing and Integrations are now executable in `deploy/package-surface-inventory.json`, so all six product packages have registered boundaries. Movement, portability, restore and erasure inventories include the new OAuth/email/research state and exclude credential/lease/cursor material. The complete ordinary and race-enabled Docker gate, fresh migration/RLS suites, object policies, generated contracts and process/package inventories pass. The revision-controlled backend completion report and API/MCP guide establish the final customer-UI boundary. Per the stop rule, no customer-facing UI, Git push, GHCR, Hostinger Stage, LKE or production action begins in this checkpoint.

Complete every intended launch package rather than leaving preview shells:

- Scheduling: durable definitions, time zones, missed-run policy, leases, pause/resume and idempotent dispatch.
- Finance: governed records, summaries, reconciliations, evidence and approved external effects.
- Marketing: campaign/asset/workflow capabilities defined by final product requirements.
- Email: inbound/outbound boundaries, threading, credentials, retries and approval rules.
- Google Drive and document sources: scoped credentials, sync cursors, revocation and deletion.
- Web research: safe retrieval, source attribution, policy and failure handling.
- Integrations package: connector health, capability grants, provider degradation and credential lifecycle.

Every package receives Catalog features, entitlement/downgrade behavior, HTTP/MCP/UI surfaces, schedules/workers/tools, observability, retention and acceptance tests.

Exit: the Catalog, website claims and application capabilities reconcile exactly.

## P3.5 — Final transports and product surface

- Implement one canonical application boundary per use case.
- Publish complete generated HTTP contracts and sanitized OpenAPI artifacts.
- Implement production MCP tools over the same commands/queries and prove HTTP/MCP outcome parity.
- Build the complete mobile-first Vue customer surface: an excellent public landing page, complete feature/package breakdown, GDPR-capable landing/checkout/onboarding analytics, Affiliate enrollment and recurring commission attribution, Catalog-backed pricing and checkout, plus the private feature-organized SPA for Account, Work, Your Turn, Knowledge, Baseline, Agents, Finance, Marketing, integrations, billing and security. Complete Your Turn first and make it the authenticated default route.
- Add reliable streaming/event replay, optimistic concurrency, draft preservation and session/network recovery.
- Complete keyboard, screen-reader, contrast, zoom/reflow, forced-colors, reduced-motion and supported-device behavior.
- Remove prototype compatibility adapters only after migration evidence and rollback windows close.

Exit: no production use case depends on prototype runtime code, manually duplicated models or transport-specific authorization.

## P3.6 — Data migration and operational completion

Account-portability foundation checkpoint (2026-08-24): [ADR-0009](decisions/0009-account-portability-export.md) now fixes a deterministic private ZIP with canonical stable-key JSONL projections, exact versioned-object streams, per-entry and whole-artifact SHA-256 evidence, bounded snapshot timing and hard record/object/artifact ceilings. Its executable builder rejects source/registry drift, noncanonical or unordered records, unsafe or unordered objects, size/digest mismatch and incomplete sources. The reviewed launch registry gives every current Account-owned table an included, derived, operational or secret disposition; a fresh PostgreSQL 17 test applies all global/development/cell migrations and fails if any direct Account table appears without policy or any registered table disappears. Encrypted provider, notification and runner payloads, credential attestations, anti-replay receipts and processing queues are expressly not portable content. This checkpoint established the artifact and schema-coverage foundation; the request checkpoint below closes the first of its then-open orchestration gates.

Account-portability request checkpoint (2026-08-24): migration 35 and the Account-level application boundary now provide one active request per Account, active-Owner plus recent-passkey creation/cancellation, owner-only status/history access, stable placement and Account-version capture, leased build/recovery, three-attempt bounded retry, terminal failure, available-artifact evidence and lease-fenced expiry deletion. The build processor accepts only exact snapshot identity and private artifact digest/size/reference evidence; the expiry processor deletes the exact object before clearing its durable reference, so an unknown database commit safely retries idempotent deletion. PostgreSQL tests prove cross-Account concealment, immutable content-minimized events, active-lease exclusion, retry, premature-expiry refusal, exact digest retention, expired-unbuilt cleanup and publication rejection across an Account movement/version race. Global erasure and signed restore replay remove request/event rows, preserve the other Account and add their exact counts to the content-free tombstone. At this checkpoint sanitized global/cell projection readers, coordinated repeatable-read snapshots, private S3 staging/publication/reconciliation, short-lived download capability and customer HTTP/MCP/UI surfaces remained; the object checkpoint below closes publication/deletion only, and the machine inventory therefore continues to mark `customer-export-api` absent.

Account-portability object checkpoint (2026-08-24): a distinct S3-compatible adapter now publishes an already-built archive create-if-absent under the sole derived key `exports/<validated-export-id>/artifact.zip`, binds content type, object kind, export identity and SHA-256 metadata, requires immutable bucket version identity and reconciles a retry only when byte count and digest match. Its opaque durable reference encodes only the validated export identity and exact object version. Expiry deletion first resolves that fixed key, stats the exact version, verifies retained byte/digest evidence and removes only that version; a missing version is success for unknown-commit retry. Local and Stage Compose now create a separate versioned `spyglass-account-exports` bucket and independent worker credential whose policy has no bucket listing and reaches only exact export artifact keys. Pure, race and real encrypted-MinIO tests prove publication replay, conflict/integrity cleanup, reference confinement, exact-version deletion, idempotence and the deployed policy. The projection/snapshot producer, private local staging lifecycle, customer download signer/transport and LKE object manifests remain; no export worker or `customer-export-api` is enabled by this checkpoint.

Account-portability pipeline checkpoint (2026-08-24): the build producer now owns the exact callback lifetime for coordinated global/cell sources, validates snapshot identity against leased Work, constructs only the launch-registry builder, writes the bounded archive into a private stage, rewinds and publishes the computed byte/digest identity, verifies the returned artifact and removes staging on every ordinary outcome. Stable failure codes distinguish permanent invalid Work/snapshot/registry/source/artifact conflicts from retryable snapshot, source, staging and object-store availability; a cleanup failure after successful publication deliberately retries and reconciles the stable object key. The filesystem stager accepts only an absolute unchanged non-symlink directory with no group/other permissions, creates random export-ID-bound `0600` files, seals writes on rewind and makes cleanup idempotent. Pure and race tests prove full archive publication, digest binding, placement drift refusal, callback-protocol closure, permanent source/conflict classification, post-publication cleanup recovery, root replacement detection and zero residue on cancellation. Concrete PostgreSQL snapshot coordination/sanitized sources and workload composition remain, so this kernel still enables no executable surface.

Account-portability snapshot checkpoint (2026-08-24): the PostgreSQL coordinator now opens simultaneous read-only repeatable-read transactions on global and the worker's one configured cell, rechecks active Account state/version plus active directory cell/generation in the global snapshot, installs the exact Account RLS context and rechecks the active namespace generation in the cell snapshot, then exposes both transactions only for one source-factory callback. Separate transaction timestamps become artifact snapshot evidence. Drift or absence is a permanent identity failure; database/startup failure and either read-only commit's unknown result are retryable, allowing stable object reconciliation after publication. Fresh PostgreSQL 17 integration proves both isolation/access modes, exact RLS context, callback identity and directory drift refusal. The explicitly sanitized projection and exact-object source factory is still absent, and no worker is composed.

Account-portability global-projection checkpoint (2026-08-24): a streaming PostgreSQL section cursor now derives each stable record key from a hex-encoded canonical primary-key tuple, reads tables in fixed qualified-name order, selects only explicit identifiers, remarshal-checks canonical JSON and never buffers an unbounded section. The reviewed global allowlist covers all twelve included Account, Billing and Entitlements tables. Every physical column is classified as exported or omitted, and fresh-migration certification compares that exact union to `information_schema`; adding, removing or renaming a column fails CI. Invitation token hashes, checkout session/hosted capabilities, Stripe customer/subscription/object identifiers, entitlement source references and closure retry/lease controls are omitted while customer-visible lifecycle, membership, billing and entitlement state remains. Real PostgreSQL output proves strict record ordering/canonicalization and that an actual stored invitation token cannot appear. The Account descriptor now accurately declares its global plus cell audit stores. Cell feature projections and exact object sources remain.

Account/Work/Schedules cell-projection checkpoint (2026-08-24): the first cell cohort explicitly covers Account audit, Work items/events/Agent-execution links, and schedule definitions/events/occurrences/triggers. Fresh-schema certification proves the exported-or-omitted union for all eight included tables. Internal capacity reservation identities are withheld while customer-visible release timing remains. Review also corrected `work_capacity_release_operator_events` from portable content to operational state, matching ADR-0009's exclusion of internal staff intervention evidence. Remaining Agents, Attention, Baseline, Finance, Integrations, Knowledge, Marketing and migration cohorts plus exact objects remain.

Attention/Finance/Migration cell-projection checkpoint (2026-08-24): the next certified cohort covers all four Attention aggregates/events, all nine included Finance ledgers/accounts/journal/evidence/reconciliation/event tables and all three prototype migration run/receipt/event tables. Canonical action payloads, questions, decisions, ledger entries and migration reconciliation evidence remain portable customer data; the derived Finance number allocator remains excluded. Fresh migrations prove exact exported-or-omitted column unions for every table. Agents, Baseline, Integrations, Knowledge and Marketing plus exact objects remain.

Baseline/Integrations/Marketing cell-projection checkpoint (2026-08-24): nine Baseline assessment/interview/requirement/evidence/plan/source-grant tables, seven non-secret Integration connection/revision/health/execution/attempt/resolution/event tables and all eight Marketing campaign/asset/release/event tables now have exact fresh-schema allowlists. Customer source scopes, delivery outcomes and creative metadata remain portable. Credential identities/generations, execution leases/current attempts, internal Marketing object references and other broker/storage bindings are explicitly omitted; the separate exact-object source will carry creative bytes. Projection-table sorting is centralized so multi-table sections cannot drift from canonical key order. Agents, Knowledge and both exact object sources remain.

Knowledge/Agents database-projection checkpoint (2026-08-24): all nine included Knowledge fact/claim/evidence/citation/document metadata/history tables and all sixteen included Agent conversation/Persona/Run/message/action evidence tables close the database inventory. Knowledge object/extracted keys and versions are withheld for exact-object handling. Agent provider response IDs, runner-result digests, execution-plan operation IDs/expiry, executor identities/policy versions, leases, next-attempt/current-attempt controls, idempotency keys and pod IDs are explicitly omitted while customer content, governed policies, token/cost evidence, result payloads, action authority/outcomes and dual-control resolutions remain. Fresh PostgreSQL proves every included global and cell table has an exact exported-or-omitted column union. Database projections are complete; only the Knowledge and Marketing exact-version object sources remain before source-factory composition.

Account-portability exact-source checkpoint (2026-08-24): the launch source factory now composes all thirteen global/cell database sections and both exact-version object sections from the reviewed registry inside the coordinator's transaction lifetime. The cell snapshot installs both the application and Spyglass Account contexts, closing an RLS mismatch found during composition. Knowledge streams every non-deleted immutable original revision through its recorded object key/version/size/digest while excluding derived extracted text and search chunks; Marketing reconstructs and validates each immutable asset revision, derives its storage key/version through the domain contract and verifies Account/object metadata in the object adapter. Archive paths use only validated immutable IDs, not customer filenames or internal storage references. Both readers expose only exact-version read authority and cannot list, write or delete objects. A fresh PostgreSQL 17 end-to-end certificate applies every migration, exercises forced RLS, builds all fifteen registry descriptors, opens each expected exact version once, verifies archive bytes/digests/paths and proves internal Knowledge/Marketing locators remain absent from database JSON. Source coverage is complete; workload composition, environment manifests and short-lived customer download/transport remain.

Account-portability worker-composition checkpoint (2026-08-24): the executable process inventory, local and Hostinger Compose graphs, and review-only LKE reference now include one build worker per cell plus a separately credentialed global expiry worker. Build claims are cell-scoped, so an older request in another cell cannot be consumed by the wrong worker. Global and cell database roles grant only the reviewed projection/request/event tables; source-object, artifact-build and artifact-expiry identities are distinct. The source identity can read only exact Knowledge originals and Marketing revision content, build can create/read/delete only derived `exports/<export-id>/artifact.zip` versions, and expiry can read/delete but cannot create or list artifacts. Local certification applies fresh migrations and grants, verifies positive and negative database/object privileges, runs both build workers and expiry healthy as UID/GID 65532 on read-only roots, and gives each build worker a private bounded `0700` staging filesystem. Local stress retains a 34 GiB tmpfs ceiling; the 7.7 GiB Hostinger acceptance host fails closed at 1 GiB per cell instead of advertising a RAM ceiling that could exhaust it. The Kubernetes build manifest uses a bounded 34 GiB disk-backed `emptyDir`, creates only its final `0700` child at startup and runs without a service-account token; generated two-cell Linode overlays and the global expiry Deployment render without Secrets or mutable images. Prometheus now certifies all 23 local targets; boolean operational fields render as 0/1 gauges instead of making `/metrics` unavailable. The remaining Account-portability construction is the short-lived customer download capability and shared HTTP/MCP/private-UI request, status, history, cancellation and download surfaces; Stage/LKE application of this checkpoint is release evidence, not yet claimed.

Account-portability Stage-application checkpoint (2026-08-23): the admitted,
same-revision `0.3.0-rc.4` pair is active from clean deployment checkout
`ba68ecc64804be3e357794ef4af6301121d726f9` and immutable secret set
`2026-08-23-01`. The application of RC.3 first exposed two fail-closed defects:
the expiry worker received the ordinary unprefixed global restore gate, and two
Marketing capability identifiers contained underscores forbidden by the shared
broker/token/approval grammar. The Stage verifier now asserts the expiry
worker's prefixed gate; all exported runner capabilities have a grammar
regression test; RC.4 rebuilt and admitted both images from corrected source
`069b25992fb50f82aacbebf24ee6ab7a2cdfccb0`. Stage now has 45 running
containers and 44 healthy healthchecks, including both build workers, expiry,
both runner brokers and both MCP replicas. The export workers run as
UID/GID 65532 with read-only roots; build staging is private mode `0700` and
1 GiB per cell. Global/cell migration ledgers are 35/66/66, all three reviewed
database role-policy assertions pass, the complete positive/negative object
policy gate passes, and all three public origins return HTTP 200. The mode-600,
content-free certificate is
`/opt/spyglass-stage/evidence/0.3.0-rc.4/account-export-workers.json`, SHA-256
`f6367057000f1346ed58c061280788638257198f336b024e1c25a284b773fa3c`.
No persistent volume was removed or replaced. The remaining Account-portability
construction is still the short-lived customer download capability and the
shared HTTP/MCP/private-UI surfaces.

Account-portability customer-surface construction checkpoint (2026-08-23):
the global Account API and private browser shell now share canonical Owner-only
request, bounded history, status, optimistic cancellation and download
capability services. Creation, cancellation and capability issuance require a
recent user-verified passkey; status objects omit the durable artifact
reference and digest. A rotating HMAC capability binds Account, User, export,
request version, artifact size and SHA-256 for at most five minutes (two by
default). The artifact endpoint rejects cookies and query strings, accepts only
the custom Authorization header, reloads durable request/object identity and
streams the exact immutable version from a dedicated no-list/no-write object
identity. The browser consumes that capability server-side so it never enters
HTML, history or a URL. Five MCP tools expose the same list/get/request/cancel/
capability outcomes at the global gateway while all cell tools retain signed
routing. OAuth consent snapshots a current passkey timestamp; access rotation
and refresh preserve but never advance it, so privileged MCP export operations
fail after the shared ten-minute policy. Migration 36, least-privilege gateway
grants, generated OpenAPI/TypeScript contracts, local Compose and review-only
LKE configuration are constructed and locally verified. This closes the
Account-portability construction backlog; a later immutable release must still
apply migration 36 and certify HTTP/MCP/browser behavior on Stage before the
Phase 3 release gate can close.

- Inventory and characterize every retained prototype route, MCP tool, workflow, schedule, table and external object.
- Build Account-cohort migration tools with checksums, row/object/index counts and rollback checkpoints.
- Reconcile Membership, placement, entitlements, Work, Knowledge, Baseline, Agents, schedules, actions and provider references.
- Extend the Phase 2.5 PostgreSQL Account-movement and existing erasure/restore foundations across every new enabled object, search, workflow and provider-reference store; no store may ship without copy/reconciliation/rollback/retirement and erasure/replay handlers.
- Complete dashboards, alerts, runbooks, capacity limits, security/privacy review and support procedures.
- Run clean-environment, restored-environment, load/fairness, provider-degradation, game-day and soak suites.

Backup scheduling/storage is configured by the project owner per environment after the application/database topology is present. Phase 3 must supply consistent snapshots, restore checkpoints, replay tooling and documented hooks so that configuration is verifiable.

Exit: one synthetic and one internal Account complete migration, operation, backup/restore verification and rollback without prototype dependencies.

## P3.7 — Linode production certification and release

1. Apply the reviewed LKE controllers and overlays.
2. Hostinger-certify the final Phase 3 application and website digests, then deploy that exact pair to a non-customer Linode environment.
3. Apply production migrations and containerized PostgreSQL clusters.
4. Run NetworkPolicy, workload identity, admission, RuntimeClass, HPA, pod/node loss, database failover, key/CA rollover and runner-compromise tests.
5. Complete real provider, accessibility, isolation, load, restore and full-product journeys.
6. Obtain engineering/product/security/operations approval; in this one-person project the approvals are explicit recorded owner decisions, not implied by a successful command.
7. Deploy internal Accounts, then a bounded customer canary, then general availability after the observation window.
8. Retain and rehearse rollback for both application and website digests plus compatible schema/Catalog/configuration.

## Dependency order

```text
Phase 2.5 platform/deployment final form
  -> Work + Attention
  -> Knowledge + Baseline
  -> complete Agent workspace/execution
  -> Scheduling + Finance + Marketing + integrations
  -> final HTTP/MCP/Vue SPA surfaces
  -> prototype migration + operational completion
  -> LKE certification + production release
```

Some implementation can proceed in parallel, but no downstream package may invent a second identity, Account, entitlement, placement, action or execution boundary.

## Phase 3 completion rule

Phase 3—and therefore application construction—is complete only when:

- every prototype capability marked `retain` or `replace` maps to a production use case and acceptance test;
- Work, Attention, Knowledge, Baseline, Agents, Scheduling, Finance, Marketing and integrations are complete;
- HTTP, MCP, schedules, workers, runners and UI share canonical authorization and entitlement outcomes;
- all production data is Account-isolated, movable, exportable, restorable and erasable;
- the final Vue SPA, public landing/feature surfaces, GDPR analytics/rights lifecycle, Affiliate attribution/commission flows and complete free/paid acquisition journeys pass the accessibility/device, privacy, performance, metadata, link and checkout-recovery matrices;
- containerized PostgreSQL and all application workloads pass local, Hostinger and LKE certification appropriate to each environment;
- production backup/restore configuration has recorded owner verification;
- the exact signed artifact pair passes migration, load, resilience, security/privacy, canary and rollback gates; and
- no production process imports, executes or depends on the prototype or preview-hosting runtime.

Customer privacy-status checkpoint (2026-08-26): the Vue Privacy workspace now lets a customer refresh the authoritative rights-request history without reloading the application, shows the last update time, and explains every submitted, review, completed, partial, declined and canceled state in customer-safe language. Refresh failures preserve the already visible history and expose a retryable error. The customer contract still excludes fulfillment evidence identifiers and digests, staff identity, operational reasons and case material. The exact local Docker gate passes 393 browser checks with eleven intentional compact-menu skips, 72 private, 22 public and 42 API-client tests, all seventeen rendered public routes, type checking, lint, production builds, gzip budgets and a zero-vulnerability high-severity audit. This is communication of recorded outcomes, not proof that the underlying legal or operational fulfillment occurred; Stage and production remain untouched.

Human-review boundary (2026-08-26): local UI, Affiliate and GDPR construction evidence and every remaining owner/external decision are consolidated in the [UI-focused phase human-review handoff](ui-phase-human-review-handoff.md). The record is deliberately unsigned, preserves closed Affiliate flags and does not authorize Stage, GHCR, LKE or production work.

Owner-policy closure checkpoint (2026-08-26): the owner approved the AI Token refund policy and delegated the remaining aligned software-policy defaults. The resulting register fixes team ownership, manual strong-authenticated purchase authority, no automatic top-up, deterministic paid-period issuance, no negative balance, input-preserving insufficient-balance denial, 30-day material rate-increase notice with narrow emergency exceptions, bounded immutable promotions, payment-adversity handling and no Affiliate earnings on top-ups/promotional grants. Affiliate economics/settlement product policy is therefore complete. The exact Catalog/Stripe encoding, token ledger/admission implementation, `account_credit_with_support_check` mode, local tests and legal/operational release approvals remain Phase 3 work; no environment was changed.

Commercial-Catalog and AI-Token foundation checkpoint (2026-08-26): the effective launch Catalog now has one `team-monthly-v2` `$50` monthly offer covering every package, and registration creates an inactive zero-entitlement team shell rather than a Free Account. Catalog-governed included grants, bundle candidates and five provider-neutral complexity rates feed a persistent Account-owned AI Token ledger with exact-once paid-renewal issuance, deterministic cohort allocation, reservation/settlement/release and unused-remainder reversal. The Account API safely exposes shared totals; Billing presents balances and configured terms; Agent customer surfaces expose only the five-stop complexity control and estimates. The Agent domain/OpenAPI/generated-client/Vue path now persists `complexity` directly, rejects caller-owned execution targets, removes selected-model output and excludes private execution targeting from portability exports; the complete private five-level target map is startup-validated. Launch, checkout and browser fixtures are aligned to the new offer and no-Free copy. The exact local Docker evidence passes the complete Go suite, 72 private/22 public/42 API tests, lint, both builds, seventeen-route accessibility and bundle budgets, plus the prior 393-check cross-browser/device matrix and a new 69-pass desktop/phone critical-path check with one intentional desktop-only skip. This milestone does not complete the revamp: moving exact target/rate freezing into Agent-run token admission, top-up/refund/promotion commerce, commissioning, Affiliate settlement, subscription-remediation lifecycle and human visual review remain local Phase 3 construction. See the [UI revamp milestone report](ui-revamp-milestone-report.md). No provider or remote environment was contacted.

Agent AI-Token execution-admission checkpoint (2026-08-26): the private provider/model/fallback/reasoning map now belongs to the global admission service and is overlaid onto complete public Catalog publications prospectively. Dispatch resolves the invocation's frozen complexity, reserves its maximum Account-owned charge before runner provisioning, then atomically records the reservation, immutable rate snapshot and private execution target. Insufficient balance therefore fails before provider contact. The runner reports normalized input, cached-input, output and actual tool-call usage; successful projection settles that exact charge once, while failed, rejected, unstarted and terminally dead-lettered work releases the reservation. Replays retain the original frozen rate across later Catalog changes. Dedicated dispatch/projection workload identities reach only the private reserve/close surface, and portability exposes customer grants/ledger history without operational reservation or event-correlation keys. The complete local UbuntuRojo Docker gate passes all Go and migration tests normally and under the race detector, vet, formatting and API-contract checks, 72 private/22 public/42 generated-client tests, type checking, lint, both production builds, seventeen public accessibility routes, bundle budgets, the prior 393-check device/browser matrix and a zero-vulnerability dependency audit. Token commerce/refunds/promotions, commissioning, Affiliate settlement, subscription remediation and human visual review remain local Phase 3 work. No provider, Stage, GHCR, LKE or production environment was contacted.

AI-Token-commerce and commissioning checkpoint (2026-08-26): the local Catalog and private Price mapping boundary now govern both one-time AI Token bundles and the optional `$250` commissioning service. Owner/billing-admin Checkout requires recent passkey proof, freezes item version, Catalog version, amount and token quantity before Stripe, and durably resumes exact retries. Verified payment-mode Checkout completion issues a top-up from that local snapshot exactly once; only final full-charge Refund evidence reverses the surviving unused remainder. Commissioning may share initial subscription Checkout or use a later payment-mode Checkout, creates no entitlement or Affiliate state, and one Account-owned purchase record rejects every second self-service attempt. Catalog promotions now freeze quantity, window, grant lifetime, per-Account limit, total issuance cap, disclosure and a no-stacking rule; redemption is strongly authenticated, transactionally capped, expiring and idempotent even after a later Catalog publication. The customer contract contains 212 typed operations, including one-time purchase and promotion redemption. Ordinary Go and the complete fresh-PostgreSQL migration suite pass locally; final full Docker/race/UI evidence is recorded in the milestone report. Affiliate Account-credit settlement, subscription remediation, final Billing/Checkout presentation and human visual review remain. No provider or remote environment was contacted.

Affiliate settlement checkpoint (2026-08-27): the approved 20% discounted pre-tax recurring rule, rolling successor maturity, terminal pending void and commissioning exclusion are now encoded in immutable versioned evidence. Available earnings reserve oldest-first for either an Affiliate-owned Account invoice or a Support-selected check accounting amount after recent Affiliate passkey confirmation and a versioned threshold. Every reservation freezes its settlement-policy version; release, settlement and replay cannot double spend it. Succeeded Refunds, final lost disputes and issued line-complete subscription credit notes append one whole-earning reversal, while commissioning-only credit notes do nothing and incomplete credit-note line pages fail closed. Already-applied Account credits produce idempotent Stripe customer-balance debits; already-settled Support checks produce provider-free recovery against future earnings. The complete Go/vet/contract and fresh-PostgreSQL suites pass in UbuntuRojo Docker. The exact UI gate passes 393 browser checks with eleven intentional skips, 72 private, 22 public and 44 generated-client tests, type checking, lint, builds, seventeen rendered-route accessibility checks, budgets and a zero-vulnerability audit. At this checkpoint Affiliate retention/minimization remained open; the following checkpoint closes it. Enrollment and attribution stay closed while subscription remediation and external legal/Support/provider gates remain open. No remote environment was contacted.

Affiliate retention checkpoint (2026-08-27): terminal closure and verified erasure are now distinct. The latter is a one-way, exact-version operator transition that removes the Affiliate graph from ordinary customer status, statements, exports and support while leaving referred subscriptions and constrained billing-credit settlement untouched. The Vue route communicates restricted retention and links to privacy-request history without exposing retained evidence. Scoped legal holds use a separate audited version. A dedicated restore-gated global worker applies the fixed seven-calendar-year deadline after the later of closure or final relevant activity; open cases, legal holds, in-flight recovery and unsettled value block it. Each locked/rechecked graph is deleted atomically, leaving only an immutable random identity-free tombstone, per-currency totals and an identity-free SHA-256 code fingerprint that permanently rejects reuse. Focused application/operator/worker/API/Vue contracts, fresh PostgreSQL destruction/least-authority tests and an actual local Docker role/restore/health ceremony pass. Enrollment and attribution remain closed for external legal/Support/provider gates; subscription remediation remains the next local construction item. No remote environment was contacted.
